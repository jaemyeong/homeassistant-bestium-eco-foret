# 작업 인계 문서

작성 시각: 2026-08-31 · 대상 커밋 `dd98a83` (푸시 완료) · 애드온 `0.3.3`

이 문서는 **지금 멈춘 지점**만 담습니다. 프로젝트 규약은 `AGENTS.md`,
증거 원장은 `.agent/progress.md`, 실측 결과는 `.agent/measured-capabilities.md`에
있으므로 여기서 되풀이하지 않습니다.

---

## 1. 가장 먼저 해야 할 일 — 사용자 몫입니다

**애드온을 0.3.3으로 갱신해도 동작은 달라지지 않습니다.** 이번에 바꾼 것이 전부
기본값인데, Home Assistant Supervisor는 애드온의 기본값을 사용자가 저장한 값
**아래에** 병합하고 저장한 쪽이 이깁니다.

```python
# supervisor/apps/app.py — App.options
return _OPTIONS_MERGER.merge(
    deepcopy(self.data[ATTR_OPTIONS]),      # 애드온이 배포한 기본값
    deepcopy(self.persist[ATTR_OPTIONS]),   # 사용자가 저장한 값 — 이쪽이 이깁니다
)
```

`transmit_enabled`와 `transmit_user_id`를 켜려면 구성 화면을 저장해야 했으므로,
그때 화면에 있던 값 전부가 저장되어 있습니다. **Home Assistant → 애드온 → 구성
화면에서 직접 고쳐야 합니다.**

| 항목 | 현재 추정 | 바꿀 값 | 근거 |
| --- | ---: | ---: | --- |
| `tx_observation_timeout_ms` | 3000 | **4600** | 가장 느린 폴링 2,300 ms의 두 배 |
| `capture_duration_ms` | 5000 | **600000** | 5초면 캡처가 시작 직후 끝납니다 |
| `maximum_bytes` | 65536 | **1048576** | 10분치 약 63 KiB의 16배 여유 |
| `maximum_records` | 1000 | **20000** | 10분치 약 3,200회의 6배 여유 |

**"현재 추정"은 미검증입니다.** 구성 화면이 병합된 실효값을 보여주므로 거기서
확인하면 됩니다. 애드온 화면의 배너 아래 "최대 N.N초" 문구도 같은 값입니다.

`speculative_transmit_enabled`은 켜도 꺼도 **아무 동작을 좌우하지 않습니다.**
후보 프레임을 만드는 `protocol-debug.ts`의 `inferred()` 함수에 호출자가 하나도
없습니다. 서브폰 회선을 아직 캡처하지 않아 나중에 쓰려고 남겨둔 것입니다
(`m2.ts:1344` 주석).

---

## 2. 이번 세션에서 끝낸 것

### 0.3.3 — 배포 기본값을 실측 규모로

`capture_duration_ms`가 5,000 ms였습니다. 한 기기 폴링 두 번 만에 캡처 파일이
닫힙니다. 10분으로 올리고, 그 시간 동안 버스가 싣는 양을 담도록 바이트·레코드
한계도 함께 올렸습니다.

`settings.ts`의 관측 창 주석이 "가장 느린 기기의 세 폴링"이라고 적혀 있었으나
상수는 두 폴링(`max(DEVICE_POLL_MS) * 2`)입니다. 둘이 필요한 이유를 주석에
적었습니다. 프레임이 나가고, 그다음 폴링은 아직 명령 이전 상태를 실어 올 수
있으며, 그 다음 폴링에 가서야 효과가 나타납니다.

### `test/addon-defaults.test.ts` 신설

**`config.json`을 검증하는 시험이 하나도 없었습니다.** 모든 시험이 자기 설정
객체를 만들어 썼기 때문에, 배포 기본값과 `settings.ts`의 대체값과 실측이 서로
어긋나도 아무것도 말하지 않았습니다. 실제로 어긋나 있었습니다. 지금은 네 가지를
봅니다.

- 각 기본값이 자기 스키마 범위 안에 있는가
- 관측 창이 근거를 담은 상수와 같은가
- 세 캡처 한계가 실측 버스 속도에서 서로 모순되지 않는가
- `config.json`과 `settings.ts`의 두 기본값 표가 일치하는가

### 실측 원자료 34개를 저장소에 보존

`.gitignore`가 게이트웨이 주소 파일과 원자료를 한 줄로 묶어 막고 있었는데,
캡처 파일에는 주소가 없었습니다. 36,676줄의 모든 문자열 필드를 IPv4·MAC·호스트명·
긴 키 패턴으로 검사해 한 건도 나오지 않았습니다. `tools/buslab/config.json`은
계속 막혀 있습니다.

**절대 시각(2026-08-30)이 포함된 채 공개 저장소에 올라갔습니다.** 사용자가 그
사실을 알고 선택했습니다.

### 원장 항목 `M5-E05`~`M5-E15`

이름이 어느 문서에도 인용되지 않던 10개 런에 로케이터를 복원했습니다.

---

## 3. 미해결 — 다음에 이어갈 자리

### 침묵 질의 게이트가 손상 바이트를 없앤다

이번 정리에서 나온 가장 큰 발견이고, **결론이 나지 않았습니다.**

| 송신 방식 | 실행 | 송신 | 손상 바이트 |
| --- | ---: | ---: | ---: |
| 침묵 질의 게이트 경유 | 16 | 194 | **0** |
| 정숙 구간 대기 | 10 | 183 | **959** |
| 송신 없음 (관측만) | 8 | 0 | 0 |

관측만 한 8개 런은 읽기 10,797건에 손상이 없고, 손상의 89%가 송신 500 ms
이내(중앙값 298 ms)에 발생합니다. 송신 빈도로는 설명되지 않습니다.

| 런 | 방식 | 송신 간격 | 손상 |
| --- | --- | ---: | ---: |
| `quiet30` | 정숙 대기 | 3,217 ms | 160 |
| `halfduplex` | 정숙 대기 | 3,228 ms | 172 |
| `quiet60` | 정숙 대기 | 3,248 ms | 31 |
| `gate20` | 게이트 | 3,470 ms | **0** |
| `gate20b` | 게이트 | 3,471 ms | **0** |
| `lights-all` | 게이트 | 3,481 ms | **0** |

가장 촘촘하게 보낸 `light1-discovery`(235 ms 간격)가 정숙 대기 그룹에서 손상률이
가장 낮아 버스트 가설과 반대 방향입니다. `ab-quiet60`(02:45, 383건)과
`ab-gate`(02:47, 0건)는 2분 간격으로 붙은 통제된 A/B입니다.

**아직 모르는 것이 둘입니다.**

1. **게이트가 왜 통하는지 측정되지 않았습니다.** 두 코드 경로가 그 밖에 무엇이
   다른지 분리한 실험이 없습니다. `ab-gate`의 `tx` 레코드는 `achievedQuietMs`가
   `null`이라 정숙 구간을 아예 재지 않습니다.
2. **애드온에서도 문제가 되는지 미측정입니다.** 애드온은 정숙 구간 대기로
   송신하며 게이트가 없습니다(`m2.ts`, `lastRxByteAtMs + tx_quiet_ms`). 위
   자료는 전부 buslab 도구가 낸 것이고, **애드온을 버스에 올려 잰 것이 하나도
   없습니다.**

`quiet30`(160) → `quiet60`(31)은 정숙 구간을 30 ms에서 60 ms로 늘리면 손상이
5분의 1로 준다는 것을 보여줍니다. `tx_quiet_ms: 60`의 근거이면서, 정숙 구간만으로
0에 이르지 못한다는 신호이기도 합니다.

**다음 단계로 적당한 것**: 애드온 쪽에서 손상 바이트를 세어 노출하는 것.
`protocol-debug.ts`의 디코더가 이미 인식 못한 바이트를 구분하므로, 그 수를
`getTxStatus`에 실으면 실제 설치본에서 이 문제가 있는지 배포 한 번으로 알 수
있습니다. 기기 조작이 필요 없어 승인 부담이 작습니다.

### `.agent/measured-capabilities.md` §4의 미측정 항목

난방 41 °C 이상 거부 여부, 승강기 호기 필드 `0b`의 의미, 승강기 변형 0이 무효인
이유, 일괄소등이 조명 외에 무엇을 끄는지, 출입구 전체, `0x7F` 서브폰.

---

## 4. 재개할 때 지켜야 할 것

`AGENTS.md`가 규약이고 아래는 이번 세션에서 반복해 걸린 것들입니다.

- **매번 새 승인이 필요한 행위**: 푸시, Home Assistant 갱신, Ingress 변경, 캡처,
  EW11·사설 LAN 접근, 기기 조작, 패키지 설치, Docker, force push, 릴리스.
  한 번의 승인이 다음으로 이어지지 않습니다.
- **게이트웨이 주소는 저장소와 산출물 어디에도 들어가면 안 됩니다.**
  `tools/buslab/config.json`은 계속 gitignore입니다.
- `~/Downloads/EW11.xml` 백업에 관리자 비밀번호와 WiFi 키가 평문으로 있습니다.
  **내용을 출력하지 마십시오.**
- 거부 목록(`0x7F`, 가스 열기, 난방 40 °C 초과, 관측되지 않은 `0x1E 02` 변형)은
  어떤 플래그로도 열리지 않습니다.
- 사용자 별도 지시가 있기 전까지 **서브에이전트를 쓰지 않고 메인 에이전트에서만**
  작업합니다.
- 릴리스 버전은 `bestium-eco-foret/config.json`이 단일 출처입니다. Dockerfile
  라벨과 두 `package.json`이 그것을 따르는지 `addon-image.test.ts`와
  `m2.test.ts`가 검증합니다.

---

## 5. 이 프로젝트에서 되풀이된 실수

세 번 같은 모양으로 나왔습니다. **검사가 실제로 배포되는 산출물이 아니라 그
옆에 있는 것을 재고 있었습니다.**

1. **UI 시험이 문자열만 읽었습니다.** 클라이언트 스크립트가 한 번도 실행되지
   않아, 브라우저로 열자 첫 스크린샷에서 결함 세 개가 나왔습니다.
   → `tools/design/preview-server.mjs`로 실제 페이지를 띄워 확인합니다.
2. **시험은 저장소에서 돌고 애드온은 이미지에서 돕니다.** 허용 목록 시험이
   목록과 시험 파일의 상수를 비교했을 뿐, 진입점이 실제로 무엇을 import하는지
   보지 않아 0.3.0이 초록 상태로 배포되어 기동에 실패했습니다.
   → `test/addon-image.test.ts`가 import를 따라가 폐포를 유도합니다.
3. **`config.json`을 아무도 검증하지 않았습니다.** 이번에 드러났고
   `test/addon-defaults.test.ts`가 막습니다.

그리고 이번 세션 안에서도 한 번 더 걸렸습니다. 캡처 한계의 근거를
`DEVICE_POLL_MS`에서 유도했더니 **실측의 절반**이 나왔습니다. 애드온이 해독하지
않는 기기도 버스에 있고 프레임 평균 길이가 11바이트가 아니라 16.6바이트이기
때문입니다. **유도할 수 있어 보여도 실측이 있으면 실측을 씁니다.**

---

## 6. 현재 상태 요약

- 브랜치 `main`, 원격과 차이 0, 작업 트리 깨끗함
- 시험 323개 중 320개 통과 · 실패 0 · 건너뜀 3
- 애드온 `0.3.3` (config·package·Dockerfile 라벨 일치)
- 실측 원자료 34개 런 4.9 MB가 `tools/buslab/runs/`에 추적됨
