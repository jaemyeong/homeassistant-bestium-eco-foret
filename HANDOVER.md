# 작업 인계 문서

작성 시각: 2026-09-01 · 대상 커밋 `0f57e81` (푸시 완료) · 애드온 `0.5.4`

프로젝트 규약은 `AGENTS.md`, 증거 원장은 `.agent/progress.md`, 실측 결과는
`.agent/measured-capabilities.md`에 있으므로 여기서 되풀이하지 않습니다.

---

## 1. 지금 상태 — 세 표면이 전부 실측으로 동작합니다

| 표면 | 상태 |
| --- | --- |
| 웹 UI (Ingress) | 읽기 6종 · 쓰기 8종 실측 확인 |
| Home Assistant MQTT | 엔티티 16개, 자동 발견. 읽기·쓰기 실측 확인 |
| Apple HomeKit (Homebridge) | 액세서리 13개. 전 기능 실측 확인 |

시험 375개 중 372 통과 · 실패 0 · 건너뜀 3(캡처 파일을 요구하는 buslab 인수 시험).
브랜치 `main`, 원격과 차이 0.

**앞선 세션이 남긴 가장 큰 질문 둘이 닫혔습니다.**

- 캡처를 시작해야 송신이 되던 문제 — 원인은 `getState()`가 복구된 캡처 파일의 설명을
  링크 상태로 내보낸 것이었습니다(`M6-E01`, 0.4.1에서 수정).
- 일괄소등 해제가 무엇을 복원하는지 — **각 방의 점등 상태를 복원하고 조명만 제어합니다.**
  단 등 1은 이전 상태와 무관하게 켜집니다(`M6-E12`).

---

## 2. 다음 작업 — 서브폰 `0x7F`

사용자가 정한 순서입니다. 월패드 때와 같은 흐름입니다.

1. **로컬(개발 머신)에서 패킷 캡처를 먼저 시작합니다.** `tools/buslab`입니다.
2. **동작을 하나씩 시험하며 프레임을 측정합니다.** 벨 누르기, 통화, 문열기 등.
3. **웹 UI에 기능을 추가합니다.**
4. **MQTT 자동 발견으로 노출합니다.**
5. **Homebridge 액세서리로 추가합니다.**

### 서브폰에 대해 지금 아는 것

- **이 회선에 없습니다.** 44,986 프레임 중 `0x7F` 클러스터는 **0회**입니다.
- 벨·통화·영상이 전부 서브폰 회선에 있다는 것을 **세 번 확인**했습니다
  (`M4-E152`). 초인종을 눌러도 이 회선은 30초 전과 같은 열두 프레임만 실었습니다.
- 세대현관 문열림 `0x1E 02`는 **월패드가 문을 열 때 나오는 통지**이고, 그것이
  명령인지 "호출이 문열림으로 끝났다"는 통지인지 `protocol-debug.ts:250`이
  미결로 남겨 두었습니다. 서브폰 회선을 봐야 갈립니다.

### 서브폰 작업이 먼저 부딪힐 것 셋

**1. 물리 회선입니다.** 서브폰이 이 RS485 회선에 없다는 것이 실측입니다. 즉 EW11을 하나 더
놓거나 배선을 바꾸어야 캡처가 시작됩니다. **이것이 첫 관문이고 코드 문제가 아닙니다.**

**2. buslab이 `0x7F`를 영구 거부합니다.** `tools/buslab/guard.ts:252`:

> `0x7F is a subphone macro and opens a door; this tool never sends one`

읽기는 막지 않으므로 캡처는 지금도 됩니다. **송신은 어떤 플래그로도 열리지 않고**,
`--allow-all`도 통하지 않습니다. 서브폰 송신이 필요해지면 그 거부를 여는 것이 별도 결정이며,
문이 열리는 프레임이라 그 결정의 무게가 다릅니다.

**3. 추측 경로가 살아 있고 비어 있습니다.** 서브폰을 위해 남겨 둔 것들입니다.

| 무엇 | 어디 | 상태 |
| --- | --- | --- |
| `sevenFProof` — 세 프레임 시퀀스 인식 | `protocol-debug.ts:481-530` | 동작 중. `7fb90000ee` 등 두 매크로를 압니다 |
| `inferred()` — 후보 프레임 생성 | `protocol-debug.ts:521` | **호출자 0** |
| `inferredAction` 분기 | `m2.ts:1384-1393` | 살아 있으나 아무것도 그 등급을 만들지 않음 |
| `speculative_transmit_enabled` | `settings.ts` | 타입과 상수에는 남았고 **구성 패널에서는 제거됨. 항상 false** |
| 출입구 매크로 | `protocol-debug.ts:610-621` | `unsafe_candidate`. 전송 불가 |

`m2.ts:1388`의 주석이 이유를 적고 있습니다. 서브폰 회선이 무엇을 실든 그것은
`inferred_candidate`로 시작할 것이고, 그때 이 경로가 쓰입니다. **구성 패널에 플래그를 다시
꺼내야 할지가 그 시점의 결정입니다.**

---

## 3. 이번 세션에서 한 일

커밋 23개, `66090a5`(0.3.3) → `0f57e81`(0.5.4).

### 0.4.x — 실사용에서 드러난 결함들

- **구성 옵션 16개를 4개로.** Supervisor가 사용자 저장값을 배포 기본값 위에 얹으므로,
  좋은 값을 배포해도 닿지 않았습니다. 저장된 `tx_observation_timeout_ms: 3000`이 확인 창을
  한 폴링으로 줄여 성공한 명령을 미확인으로 만들고 재전송을 부르고 있었습니다(`M5-E16`).
- **침묵 질의 게이트 이식.** 실측에서 정숙 대기 75.4% 대 게이트 94.4%, 손상 바이트 959 대
  0이었습니다. 게이트웨이가 50 ms 갭 타이머로 바이트를 붙들어 "조용하다"는 판단이 언제나
  50 ms 낡았다는 것이 기전입니다(`M5-E19`).
- **손상 바이트 계수**를 만들어 상태 응답과 배너에 실었습니다(`M5-E18`).
- **캡처 없이 송신되지 않던 문제**의 원인을 찾아 고쳤습니다(`M6-E01`).
- **일괄소등이 ingress에서 400으로 막히고 있었습니다.** 인코더와 ingress가 각자 허용
  목록을 들고 있었고 양방향으로 어긋나 있었습니다(`M5-E21`).

### 0.5.x — MQTT와 HomeKit

- **MQTT 자동 발견**. 손수 구현한 MQTT 3.1.1 클라이언트 약 900줄, 시험 40개(`M6-E03`).
  npm을 쓰면 전이 의존성 16개가 가스 밸브를 닫을 수 있는 버스에 붙고, 유일한 무의존성
  클라이언트를 이식하면 `addon-image.test.ts`의 폐포 검사가 통과하면서 아무것도 덮지 못합니다.
- **디코더가 폴링의 사본을 따로 보관**하게 했습니다. 기기 트리가 직접 응답으로도 갱신되어
  있어서, 그것을 진실로 읽는 쪽은 에코를 읽습니다(`M6-E02`).
- **Homebridge 설정** 13개 액세서리. `docs/homekit-accessories.json`과
  `docs/homekit-mqttthing.md`(`M6-E10`, `M6-E11`).

---

## 4. 이번 세션에서 되풀이된 실수

`HANDOVER.md`의 앞선 판이 "검사가 배포되는 산출물이 아니라 그 옆에 있는 것을 재고 있었다"를
세 번 기록했습니다. **이번에 다섯 번째와 여섯 번째가 나왔습니다.**

**넷째 — 화면이 세 릴리스째 `0.3.0`을 찍고 있었습니다.** 버전을 네 곳에서 검사하는데
`ui.ts`는 그 넷이 아니었습니다. 운영자가 읽는 유일한 표면이 아무도 검증하지 않는 곳이었습니다.

**다섯째 — 무캡처 제어 시험이 통과하고 있었습니다.** 시험용 store의 `recover()`가 무조건
`null`을 돌려주므로, 시험 대상 런타임이 배포되는 런타임이 아니었습니다.

### 그리고 새로운 형태 하나 — 진실이 두 곳에 있었습니다

**사용자 결정을 명세 문서 뒤에 별도 절로 붙이고 본문을 갱신하지 않아, 구현할 때 본문만 보고
두 번 놓쳤습니다.** 승강기 버튼이 비활성으로(0.5.1에서 수정), 꺼진 존의 목표 차단이 통째로
누락된 채로(0.5.2에서 수정) 배포되었습니다. 둘 다 결정 절에는 정확히 적혀 있었습니다.

명세 맨 앞에 읽는 순서를 넣어 두었습니다. **다음에 결정이 나오면 본문을 고치십시오.**

### 리서치 산출물을 그대로 믿으면 안 됩니다

워크플로가 낸 결과에서 두 가지를 잡았습니다.

- Homebridge 설정의 `apply` 표현식 **전부가 빈 페이로드에서 예외를 던졌습니다.** JSONPath의
  파싱 취약점을 피하라며 `apply`를 처방해 놓고 같은 병을 안고 있었습니다.
- 승강기 호출 실패의 진단은 정확했으나, **가스 Valve를 반대한 근거는 과했습니다.**
  `getActive`가 상태를 교정하므로 낙관적 표시가 고정되지 않습니다.

**실측 자료나 실행으로 검증할 수 있으면 검증하고 나서 쓰십시오.**

---

## 5. 재개할 때 지켜야 할 것

`AGENTS.md`가 규약이고 아래는 이번 세션에서 반복해 걸린 것들입니다.

- **매번 새 승인이 필요한 행위**: 푸시, Home Assistant 갱신, Ingress 변경, 캡처,
  EW11·사설 LAN 접근, 기기 조작, 패키지 설치, Docker, force push, 릴리스.
  한 번의 승인이 다음으로 이어지지 않습니다.
- **게이트웨이 주소는 저장소와 산출물 어디에도 들어가면 안 됩니다.**
  `tools/buslab/config.json`은 계속 gitignore입니다. 푸시 전에 추가된 줄을 사설 주소
  패턴으로 훑는 것이 이 세션의 관행이었습니다.
- `~/Downloads/EW11.xml` 백업에 관리자 비밀번호와 WiFi 키가 평문으로 있습니다.
  **내용을 출력하지 마십시오.**
- **서브에이전트는 사용자가 `ultracode`로 지시할 때만 씁니다.** `AGENTS.md`가 기본으로
  금지하고, 그 지시가 해제입니다. 원장에 어느 환경이 무엇을 했는지 적으십시오.
- 릴리스 버전은 **다섯 곳**입니다: `config.json`, 두 `package.json`, Dockerfile 라벨,
  그리고 `ui.ts`의 화면 문구. 앞의 넷은 시험이 강제하고 다섯째는 이번에 추가했습니다.
- **`origin/main`에 게이트웨이 주소가 한 건 남아 있습니다**(`M4-E135`). 히스토리
  재작성과 force push는 별도 결정이며 아직 내려지지 않았습니다.

---

## 6. 남은 미측정

`.agent/measured-capabilities.md` §4가 정본입니다.

- 난방 41 °C 이상을 기기가 거부하는지 (도구가 막고 있어 선택에 의한 미측정)
- 승강기 호기 필드 `0b`의 의미
- 승강기 변형 0이 왜 무효인지
- 출입구 전체 — **서브폰이 선행되어야 합니다**
- `0x7F` 서브폰 — **다음 작업**

### 그리고 애드온 자신에 대한 것 하나

**손상 바이트가 아직 관측되지 않았습니다.** 좋은 신호지만 답은 아닙니다. 게이트를 만든 근거는
194회 대 183회의 대조였고, 지금 있는 것은 횟수가 명시되지 않은 무손상 구간입니다.
배너 아래 "해독하지 못한 바이트" 문구가 계속 뜨지 않는다면 게이트가 실제 설치본에서도
통한다고 말할 수 있습니다.

### Homebridge 쪽 미검증 둘

- 플러그인이 `apply`의 `undefined`에 발행을 억제하는지는 추론입니다. 가스 열기가 무해한 것은
  확인되었으나, 세 겹의 방어 중 어느 것이 막았는지는 갈리지 않았습니다.
- Siri가 가스 열기 요청에 무엇이라 답하는지는 재지 않았습니다.
