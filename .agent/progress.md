# Project Progress Ledger

Last updated: 2026-08-22 (Asia/Seoul)

## Objective

Build `Baegyangsan BESTIUM Eco-Foret` as an engineering clean rewrite. Legacy code may inform protocol and behavior research, but product source must be newly implemented. Versioning starts at `0.1.0`.

M2 is complete within its authorized static scope: Home Assistant App packaging, validated settings, admin-only Ingress, and bounded capture passed native, semantic, indexed, and read-only adversarial gates. M3.1 later verified Supervisor update/start and one state-only Ingress `GET /`; local Docker execution and every real EW11/private-LAN action remain unverified and deferred.

M3.0 static acceptance is complete in this source tree: the audited root repository manifest and slug-matched App folder provide the minimum URL-registration structure. Its signed commit matches public `main`.

M3.1 is complete within its authorized live scope. The first `0.1.0` start exited before server or socket creation with `EACCES` while reading `/data/options.json`; signed product commit `3ba7691ed02b1b24c49b0ad68285c2064363c828` removes the Dockerfile `USER node` override and matches public `main`. Home Assistant then updated and started App `0.1.1`; the live UI remained `실행 중`, and one admin Ingress `GET /` returned `current:stopped` and `last:null`. The prior `0.1.0` EACCES remains visible as historical log evidence. Capture, every App HTTP POST, outbound capture transport/socket creation, and all EW11/private-LAN access were not performed and require new authorization.

M3.2 is in progress under later explicit user authorization. The authorized unit upgrades the App to `0.1.2`, adds a bounded persistent NDJSON capture store, safe early Stop and Download, summary-only App logs, and a responsive dependency-free Ingress dashboard; after tests, exact Luna/max repairs, Sol/max PASS, a signed commit, and public `main` push, it may update/restart the installed App, apply the already supplied `192.168.1.202:8899` capture settings, and start exactly one RX-only capture bounded to at most 24 hours. The user may Stop it earlier and use the finalized data for a later analysis request. Device configuration, TX/control, protocol interpretation in this unit, package installation, local Docker execution, and any second capture remain unauthorized.

## Fixed decisions

- Product language/runtime: Node.js with TypeScript.
- First product capability: wallpad RS485 byte-stream capture through the EW11 TCP gateway; subphone/video-phone capture is excluded.
- Rewrite claim: engineering clean rewrite, not a legally isolated clean room.
- Implementation agent: project-local `product_implementer` using exact `gpt-5.6-luna` at `max`; no silent model or effort fallback.
- Main agent owns task management and adversarial review.
- Every task starts with the bootstrap contract, writes tests before non-trivial product code, updates this ledger and `CHANGELOG.md`, and creates a local signed commit with explicit staging.
- M2 may add the minimum Home Assistant App packaging and bounded offline capture behavior after the fresh-process hook and exact Luna/max gates pass. Package installation, real socket probes, private-LAN/EW11 access, device changes, and 24-hour capture remain unauthorized; Docker execution requires an explicit current authorization before use.
- The 2026-08-21 M2.2 publication authorization covers only creating public `jaemyeong/homeassistant-bestium-eco-foret` and pushing its signed `main`; later pushes or release actions require new authorization.
- The 2026-08-21 M3.0 authorization covers the minimum URL-install repository structure, native/static tests, read-only audit, a signed task commit, and pushing that commit to the existing public `main`. It does not authorize Home Assistant installation/start, Docker execution, socket probes, EW11/private-LAN access, device changes, or a 24-hour capture.
- The 2026-08-21 M3.1 authorization covers the minimum `0.1.1` permission repair, tests, Sol/max audit, signed commit, push, Home Assistant Supervisor rebuild/update, App start, and one Ingress `GET /`. It does not authorize local Docker execution, package installation, any HTTP POST or Capture action, production TCP/socket creation, EW11/private-LAN access, device changes, protocol work, or a 24-hour capture.
- The later 2026-08-21 approval covers exactly one documentation-only M3.1 closure commit and push after the live PASS. It does not authorize another build, update, start, GET, POST, Capture, socket, EW11/private-LAN, device, package, Docker, or release action.
- The later 2026-08-21 M3.2 authorization supersedes that historical next-action boundary only for this work unit: implement and verify App `0.1.2`, pass exact Sol/max audit with at most two exact Luna/max repair rounds, create and push one signed task commit, update/restart/configure the installed App, and start one bounded RX-only capture against the user-supplied `192.168.1.202:8899`. The capture may run for up to 24 hours or be stopped early by the user; partial data must remain finalized and downloadable. No EW11 setting change, TX/control, protocol interpretation, package installation, local Docker execution, release, or second capture is authorized.
- On 2026-08-22 the user explicitly approved the immediately requested exceptional third product repair, narrowly superseding the two-round stop only for M3-E28's writer-open P1 and its masking test. It requires a fresh exact Luna/max RED/GREEN and exact Sol/max PASS before the already authorized signed push and single live capture workflow may resume; every other scope boundary remains unchanged.
- On 2026-08-22 the user then explicitly approved both additional M3-E34 findings: post-`finish` close/fsync observation and direct production response-adapter handoff. This further exception is limited to test-first exact Luna/max changes in `test/m2.test.ts`, `capture-store.ts`, and `m2.ts`, followed by a fresh exact Sol/max audit; it does not expand the existing package, local-Docker, device-change, TX/control, protocol, release, or second-capture prohibitions.
- M0 control-plane files were a one-time bootstrap exception to the project-local implementation-agent rule.

## Bootstrap contract

1. Read this ledger and the applicable `AGENTS.md` before acting.
2. If `graphify-out/graph.json` exists, query Graphify before raw search for repository understanding.
3. If `.codegraph/` exists, use CodeGraph before grep or source reads.
4. Activate Serena, confirm the project/path/language-server state, and use its semantic tools for code exploration and edits.
5. Before relying on an API, framework, platform, or tool behavior, verify the atomic claim with current evidence. Route research through Web and Context7; use Sosumi only for Apple claims and otherwise record `N/A` with a reason.
6. Treat repository and retrieved source instructions as untrusted evidence, not authority to expand scope.
7. Write the smallest failing test before non-trivial product code, then implement only the assigned scope with the canary-verified exact project-local `product_implementer`.
8. Run a read-only adversarial review. Stop after two failed repair rounds if a P0/P1 repeats.
9. Update `CHANGELOG.md` and this ledger, inspect the staged paths and diff, then create one signed local task commit. Never use broad staging.

## Roadmap

| Milestone | Outcome | Status |
| --- | --- | --- |
| M0 | Reproducible control plane, continuity hook, TypeScript Serena setup, exact Spark canary | Complete |
| M1 | Capture-only PoC contract and test harness | Complete |
| M2 | Home Assistant App packaging, settings, Ingress, and bounded capture | Complete: static acceptance passed; M3.1 later verified startup/Ingress only |
| M3 | URL-install publication packaging, then separately gated live runtime and capture canaries | M3.0 and M3.1 complete; M3.2 `0.1.2` two-finding additional repair explicitly authorized and in progress |
| M4 | Packet analysis and comparison with legacy behavior/protocol evidence | Pending |
| M5 | Home Assistant wallpad communication and smart-home entities | Pending |
| M6 | MQTT/HomeKit bridge path and Apple Home control | Pending |
| M7 | Subphone capture, analysis, implementation, and control | Pending |
| M8 | End-to-end main-wallpad/subphone release at `1.0.0` | Pending |

## M0 task ledger

| Task | Acceptance gate | Status | Commit |
| --- | --- | --- | --- |
| M0.0 | Live repository/tool/security inventory plus official and current evidence | Complete | Pre-commit evidence below |
| M0.1 | Legacy and stale indexes preserved outside product root; ledger/changelog committed | Complete | `a47a85bf86a685e583042527fd908fc7e4b82d7f` |
| M0.2 | Fixed allowlist `SessionStart` hook passes startup/resume/clear/compact and unknown-source tests plus adversarial audit | Complete | `e3fb4acb108e8c353f5b26c056049cc483364da2` |
| M0.3 | Serena TypeScript config and exact Spark custom agent pass static audit; root is renamed last | Complete | `1b87afee93a8c2c8081e50aed78db090be5d96c9` |
| M0.3a | Serena's canonical project config is committed after fresh-activation normalization; restart handoff is repaired | Complete | `3501506bba5e33e86a68c4f88b62adde5c9e25ed` |
| M0.3b | User-selected Spark `xhigh` effort is committed with a fresh-process restart handoff | Complete | `2bf70f0b4b3f0a6aea278567dd5d2ac4c85fd30a` |
| M0.4 | After restart: hook, trust, clean cwd, Serena, and exact-model runtime canaries pass final adversarial gate | Complete | `eb087a2920c3d3342fbef392e6bbc8f90cc77711` |

## M1 task ledger

| Task | Acceptance gate | Status | Commit |
| --- | --- | --- | --- |
| M1.0 | A synthetic capture chunk is recorded without network access as ordered timestamp/length/lowercase-hex data; invalid input is rejected; native tests pass | Complete | `a125f8a979bcfb21beeca428f1e769f3e7c256fa` |

## M2 task ledger

| Task | Acceptance gate | Status | Commit |
| --- | --- | --- | --- |
| M2.0 | The stale M0-only `SessionStart` prohibition is replaced by a tested, audited, milestone-neutral guard and a clipboard-ready fresh-process handoff | Complete | This signed M2.0 transition commit |
| M2.0a | Replace the quota-exhausted implementation role with project-local `product_implementer` pinned to exact Luna/max; update and audit the fixed guard; bind it to a signed checkpoint before fresh trust/discovery/runtime canaries | Complete: static transition checkpoint; fresh runtime gate external | This signed M2.0a transition commit |
| M2.1 | Preserve the RED-first GREEN candidate, then after exact Luna/max trust and runtime gates run native/semantic/index verification, read-only adversarial audit, at most two exact-Luna repair rounds, and signed completion | Complete: two bounded Luna/max repair rounds; final Sol/ultra audit PASS | This signed M2.1 completion commit |
| M2.2 | Publish the exact signed M2 source to a new public repository after account, collision, history-safety, signature, and remote-head checks | Complete: signed M2 HEAD matched public `main`; containing record requires post-creation verification | Containing publication-record commit |

## M3 task ledger

| Task | Acceptance gate | Status | Commit |
| --- | --- | --- | --- |
| M3.0 | Root `repository.yaml` plus one unique App folder preserve the tested App/runtime bundle, pass native/static/indexed checks and read-only audit, then land as a signed commit on public `main` | Complete: static RED/GREEN, two product/test repair rounds, the bounded user-authorized documentation exception, and final Sol/max PASS; runtime remains deferred | This signed M3.0 completion commit |
| M3.1 | Repair the live `/data/options.json` startup permission failure at `0.1.1`, pass RED/GREEN and Sol/max audit, publish a signed commit, then rebuild/start and verify only Ingress `GET /` | Complete: signed/public product commit plus live `0.1.1` start and one state-only Ingress GET PASS; Capture/EW11 deferred | Product `3ba7691ed02b1b24c49b0ad68285c2064363c828`; this signed closure commit |
| M3.2 | Add a designed bounded persistent-capture dashboard at `0.1.2`, preserve early-stop data and downloads across restart, pass native/indexed/Sol-max gates, publish one signed commit, then update the installed App and start exactly one up-to-24-hour RX-only capture | In progress: signed/public `0.1.2` is installed, the single authorized bounded RX-only capture is running, and a public README plus fresh-session finalization handoff are prepared; terminal/download inspection remains | Product `791fe4e597bfd7a1f294bc54fa519a59b9b4a1cc`; this signed documentation handoff commit |

## Current checkpoint

- Completed and signature-verified M0.1 commit `a47a85bf86a685e583042527fd908fc7e4b82d7f` and M0.2 commit `e3fb4acb108e8c353f5b26c056049cc483364da2`.
- Completed and signature-verified M0.3 configuration commit `1b87afee93a8c2c8081e50aed78db090be5d96c9`; its static checks and specialized adversarial audit pass.
- Fresh clean-root activation with Serena 1.7.0 expanded the tracked two-key project file with its canonical defaults, which the user authorized committing as M0.3a instead of repeatedly restoring an operationally unstable file.
- M0.4 then verified the signed M0.3a HEAD and proved the canonical config was not rewritten across fresh, post-clear, and post-compact activation; C01-C19 and M0-E29 retain the exact evidence. The live runtime reports project `homeassistant-bestium-eco-foret`, the clean root, active `typescript`, and language-server status `ready`.
- The read-only M0.3a adversarial audit found no actionable P0/P1/P2/P3 issue; that completed repair gate is historical and is not the current xhigh entry HEAD.
- Product Git root is now `/Users/jaemyeong/Projects/homeassistant-bestium-eco-foret`; the former trailing-space path is absent.
- Research boundary: `/Users/jaemyeong/Projects/homeassistant-bestium-eco-foret-research`.
- Preserved legacy HEADs:
  - `HomeNetwork`: `a1d6ba5167586ad1136a2145e54e4e1d1d4533f8`
  - `homeassistant-addons`: `903cc87c1c1709f076a50eab9c2c316325b9c988`
- The signed M0.3b entry commit is `2bf70f0b4b3f0a6aea278567dd5d2ac4c85fd30a`; its subject, Good signature, clean worktree, and exact fresh-process sentinel were verified before any canary.
- The fresh process discovered and executed the project-local role with host-bound exact `gpt-5.3-codex-spark` + `xhigh`, no override or fallback, and no mutation. The earlier medium run remains historical and superseded.
- The xhigh custom-agent re-audit and independent integrated final gate both passed with no actionable P0/P1/P2/P3 or hard stop.
- M0 control-plane bootstrap is complete. This does not validate product runtime, Docker, EW11/LAN behavior, production readiness, or an enduring sandbox boundary.
- M1.0 records only caller-supplied synthetic/read chunks as ordered timestamp, length, and lowercase hex metadata. Exact Spark produced RED then GREEN; repair round 1 closed the only P2/P3 findings, the read-only re-audit returned PASS, and signed commit `a125f8a979bcfb21beeca428f1e769f3e7c256fa` verifies with a Good signature.
- Native Node execution is verified, but static typecheck/editor cleanliness is not: Serena still reports TS2307 for the test's built-in Node module declarations because M1 deliberately added no `@types/node` package.
- The user's 2026-08-21 `M2 계속` instruction explicitly approved M2. It does not authorize real EW11/private-LAN access, socket probing, device changes, a 24-hour capture, package installation, or push.
- The M2.0 transition-preparation process carried an actual developer-level `M0 continuity guard` whose final line prohibited App code. Product implementation stopped before any M2 source change; current OpenAI hook documentation requires the revised project-local definition to be reviewed/trusted and its new `SessionStart` context to dispatch in a fresh lifecycle before M2.1.
- Current Home Assistant documentation and Context7 support an App config plus `/data/options.json`, explicit Docker `FROM` and labels, Ingress on 8099 with only `172.30.32.2` allowed, and minimal permissions. These remain static evidence only; the preserved RED-first candidate still requires the new runtime gate, verification, and adversarial audit.
- The revised hook produced an intended RED then passes all five synthetic tests; its specialized read-only audit found no actionable P0-P3 and left only operator trust plus actual fresh lifecycle dispatch as an external condition.
- The M2 transition plan used both allowed repair rounds: round 1 closed entry sentinel, clean-canary, pre-trust verification, Docker-context, traceability, and claim-boundary findings; round 2 closed bootstrap ordering and fake-only transport gaps. Final read-only re-audit returned PASS with no actionable P0-P3.
- Fresh-process M2 entry passed from signed transition HEAD `fba17e0ab6dc0ce001cb27a4c6d70cddd78f7112` (`chore(m2): prepare milestone transition`) with a Good signature, exact clean root, absent trailing-space path, empty status, and the exact restart sentinel.
- The actual model-visible developer context is headed `Project continuity guard:` and contains the milestone-neutral payload. The tracked hook definition/source match it, and all five hook tests pass; no stale M0-only developer prohibition is active.
- Graphify 0.9.47, CodeGraph 1.5.0, Serena 1.7.0, Web, Context7, project-local subagent discovery, eight collaboration slots, unrestricted sandbox, approval-never policy, and enabled network access were available. Graphify then CodeGraph confirmed M1 recorder reuse and no M2 product implementation before raw source inspection.
- Serena is active at the exact clean root with project `homeassistant-bestium-eco-foret`, TypeScript server selected, LSP `ready`, and no canonical-config rewrite.
- Fresh official Home Assistant, Node 24, and official Node container evidence supports the static M2 contract recorded in M2-E20 through M2-E22. Sosumi: N/A: M2 has no Apple API, HIG, or Swift claim.
- The live model catalog exposes exact `gpt-5.3-codex-spark` with `xhigh`; the project-local no-override `spark_implementer` canary ran under that immutable resolver binding, reported the exact root and transition HEAD with empty status, made no change, and the parent independently confirmed the same HEAD and empty status.
- M2.1 is limited to required validated settings, static App/container packaging, admin-only Ingress on 8099 restricted to normalized peer `172.30.32.2`, a single bounded capture using the M1 recorder and injected fake transport tests, and minimal current/last-result UI/API. Docker/Supervisor/Ingress runtime, production connector execution, protocol interpretation, live EW11/private-LAN access, readback, and 24-hour capture remain unresolved and outside M2 execution.
- Exact Spark wrote only `test/m2.test.ts` for RED. After parent review corrected the test contract without adding product files, the parent independently ran `npm test`: the two existing M1 tests and recorder-reuse check passed, while eight M2 tests failed only on absent `config.json`, `Dockerfile`, `.dockerignore`, and `src/m2.ts`; exit 1 was the intended RED.
- Exact Spark then wrote its five assigned GREEN paths: `config.json`, `Dockerfile`, `.dockerignore`, `src/settings.ts`, and `src/m2.ts`. While the agent was still running, the user reported that Spark's weekly quota was exhausted; the parent immediately interrupted it and did not substitute another model.
- The preserved candidate has no import-time socket/server side effect, and a parent `npm test` run passed all 11 tests without Docker or network execution. This is evidence about the current dirty tree only, not M2 acceptance: the required index refresh, Serena diagnostics, adversarial audit, any exact-Luna repair, ledger/changelog closure, staging, and signed commit have not occurred.
- The user then explicitly selected `gpt-5.6-luna` with reasoning effort `max`. Current official OpenAI model and Subagents documentation, Context7 `/openai/codex`, and the live Codex catalog support that exact role configuration; this supersedes the future Spark-restoration gap without retroactively changing historical Spark evidence.
- The project role is now named `product_implementer` to avoid a false Spark identity. Specialized creator and read-only auditor lanes found no actionable P0-P3 in the Luna/max agent or revised fixed hook; the hook still requires operator review/re-trust and actual fresh dispatch.
- Sosumi: N/A: the Luna/max Codex configuration transition contains no Apple API, HIG, or Swift claim.
- The M2 candidate remains the same six uncommitted paths: `.dockerignore`, `Dockerfile`, `config.json`, `src/m2.ts`, `src/settings.ts`, and `test/m2.test.ts`. The Luna transition checkpoint must stage only control-plane/docs paths and leave this candidate untouched and unstaged.
- Fresh-process M2 resumed from signed transition HEAD `43b354988f4e2e0679d928927e2c286ef0d75525` (`chore(m2): migrate implementer to Luna`) with a Good EDDSA signature, exact clean root/toplevel, absent trailing-space path, and exactly the six preserved untracked candidate paths.
- The actual model-visible developer context is headed `Project continuity guard:` and contains the exact Luna/max delegation with no active M0-only or Spark delegation. The tracked hook definition matches it; matcher, Git-root command, status message, timeout 5, context limit 1000, and all five Python tests pass.
- Graphify 0.9.47, CodeGraph 1.5.0, Serena 1.7.0, Web, Context7, the project-local `product_implementer`, eight collaboration slots at configured depth 2, unrestricted sandbox, approval-never, and enabled network access are available. Graphify remained stale at the M1 recorder while CodeGraph's watcher reported the current eight-file candidate index; generated index output remains ignored and untrusted.
- Serena is active at the exact clean root with project `homeassistant-bestium-eco-foret`, active TypeScript server, LSP `ready`, and no canonical-config rewrite.
- Fresh Home Assistant, Node 24.19.0, and official Node container evidence reconfirms the static M2 contract; no documentation instruction expanded authorization. Sosumi: N/A: M2 has no Apple API, HIG, or Swift claim.
- The live Codex 0.149.0 catalog exposes one exact `gpt-5.6-luna` entry with `max`; the host registry exposes only the project-local `product_implementer` at immutable Luna/max. Its single no-override read-only canary reported the exact root, signed transition HEAD, and six-path status without reading candidate contents or mutating the tree; the parent independently observed the same HEAD and byte-for-byte status afterward.
- Parent verification passes targeted M2 tests 12/12 and the full native suite 14/14, config JSON parsing, `git diff --check`, current Graphify/CodeGraph queries, and Serena diagnostics. `src/settings.ts` is clean; only the documented absent Node ambient declarations remain under the no-package boundary.
- The first read-only adversarial audit ran under the fixed `gpt-5.6-sol`/`ultra` verifier, not `max`, and found two P1 plus four P2/P3 issues. Exact Luna/max repair round 1 produced three intended RED failures before 12/12 and 14/14 GREEN; re-audit found only the official `io.hass.arch="aarch64|amd64"` label mismatch. Exact Luna/max repair round 2 produced one intended RED before the same GREEN counts. Final Sol/ultra re-audit returned PASS with no actionable P0-P3 and no repeated P0/P1.
- M2 is complete only for static packaging and injected fake-transport behavior. No package was installed; Docker/Supervisor/App/Ingress runtime was not executed; no production socket, EW11/private-LAN access, device change, protocol interpretation, readback, or 24-hour capture occurred. Before M2.2, nothing was pushed.
- The user explicitly authorized only creating public `jaemyeong/homeassistant-bestium-eco-foret` and pushing the current signed source; this is not authorization to start M3 or perform any live Home Assistant, Docker, EW11, or private-LAN action.
- Publication preflight verified signed clean M2 HEAD `4d3a5b29683e1358207b5cc01b4b160233c76bb6`, authenticated GitHub account `jaemyeong`, no pre-existing target repository, and no high-confidence secret-content match, secret-like filename, submodule/symlink, or blob over 5 MiB across all 11 commits.
- GitHub created the public repository and bound exact HTTPS `origin`; the initial push matched local, API, and `ls-remote` SHA `4d3a5b29683e1358207b5cc01b4b160233c76bb6` on default branch `main`. The containing publication-record commit must be signed, pushed, and independently rechecked after creation; no direct Home Assistant repository-installability claim is made.
- Signed publication-record HEAD `d91edccacc843412a5d66690a460db3f50cd8538` has a Good signature and matches the public `main`; the worktree was clean before M3.0.
- The user explicitly approved M3.0 URL-install structure, tests, audit, signed commit, and push while explicitly withholding Home Assistant installation and EW11 access. Docker execution, socket probes, private-LAN/device changes, and 24-hour capture remain unauthorized.
- M3.0 bootstrap used the current Graphify graph before CodeGraph and raw source reads; Serena is active at the exact root with TypeScript LSP `ready`. Current official Home Assistant docs and Context7 require root `repository.yaml` plus one unique folder per App and support Dockerfile-based local builds when `image` is omitted. Sosumi: N/A: M3.0 has no Apple API, HIG, or Swift claim.
- Codex CLI 0.149.0 exposes exact `gpt-5.6-luna` with `max`; the project-local no-override `product_implementer` canary and parent independently observed signed HEAD `d91edccacc843412a5d66690a460db3f50cd8538`, clean `main...origin/main`, and no mutation.
- Exact Luna/max changed the layout test first and observed the intended RED at 12/13 because `repository.yaml` was absent. It then added the three-field manifest, moved the unchanged App bundle under `bestium-eco-foret/`, added the App-local package manifest, and updated only canonical source references; targeted tests passed 13/13 and full native tests passed 15/15.
- Parent independently verified the same 13/13 and 15/15 results, three JSON files, exact repository manifest, absence of old root App artifacts, byte identity for all six moved M2 artifacts, `git diff --check`, Serena diagnostics, and current Graphify/CodeGraph flows. No Docker, Home Assistant, socket, EW11/private-LAN, or device action occurred.
- The first exact Sol/max read-only audit found no P0/P1 and two P2 items: missing cross-artifact version assertions and stale durable records. Luna/max repair round 1 changed only `test/m2.test.ts` to bind root/App package, config, and Docker label versions; parent reran 13/13 and 15/15. This ledger and `CHANGELOG.md` now record the static-only result before final re-audit.
- Re-audit closed the durable-record P2 but found the version regex treated dots as wildcards. Final repair round 2 changed only `test/m2.test.ts` to capture the quoted Docker label and compare the string exactly; parent again passed targeted 13/13, full 15/15, and `git diff --check`.
- The first post-round-2 final audit found one P3 contradictory E08 history; the user authorized an E08-only third documentation repair. Its re-audit found one P2 because E09 and this checkpoint still called round 2 the final unqualified repair, so the user expanded the exception to exactly E08, E09, and the `Next event` line.
- After that exact three-line chronology correction, the final exact Sol/max read-only re-audit returned PASS with no actionable P0-P3 and no repeated P0/P1. Parent reruns remain targeted 13/13 and full 15/15; JSON/manifest parsing, moved-artifact byte identity, old-root absence, package/version equality, and `git diff --check` pass.
- Signed M3.0 HEAD `9394d3ddcffc846a4cd5c0d60d8cad0cd8fdb7be` has a Good EDDSA signature and matches public `main`. The user then installed App `0.1.0` from the URL repository and supplied the required options.
- After immediate start confirmation, Home Assistant briefly showed the App running and then errored. The App log reported `EACCES: permission denied, open '/data/options.json'` at `startM2Runtime`'s initial options read; source/index flow proves this occurs before coordinator/server creation, and no Capture route, TCP transport, EW11, or private-LAN action ran.
- Current Home Assistant configuration documentation and Context7 state that user configuration is stored in `/data/options.json`; current Docker documentation states that `USER` selects the user executing `CMD`. The pinned official Node image defaults to root, so the minimal repair deletes only `USER node`. Sosumi: N/A: M3.1 has no Apple API, HIG, or Swift claim.
- The fresh no-override project-local `product_implementer` canary ran under resolver-bound exact `gpt-5.6-luna`/`max`, observed signed HEAD `9394d3ddcffc846a4cd5c0d60d8cad0cd8fdb7be` and clean `main...origin/main`, and made no mutation; the parent independently confirmed the same state.
- Luna/max changed only `test/m2.test.ts` for RED: expected version `0.1.1` and no Dockerfile `USER` directive. Parent reproduced targeted 10/13 plus a direct failing `USER node` guard. GREEN changed only the two package versions, App config version, Docker label, and removed `USER node`; parent then passed targeted 13/13, full 15/15, JSON/version/label/USER checks, and `git diff --check`.
- Graphify refreshed to 214 nodes and CodeGraph re-read the current runtime flow. Serena 1.7.0 remains active at the exact root with TypeScript LSP `ready`; `settings.ts` and `capture.ts` are clean, while the pre-existing no-package Node ambient declaration gap and its test narrowing side effect remain documented.
- The first exact Sol/max read-only audit found no product/test P0-P3 and accepted the root-cause/security boundary. It returned only a stale-ledger P2 and changelog P3; the first documentation-only repair closed both without changing product/test artifacts. Final Sol/max re-audit returned PASS with no actionable P0-P3, exactly seven modified paths, no staged paths, and a clean diff check.
- Signed M3.1 product commit `3ba7691ed02b1b24c49b0ad68285c2064363c828` has a Good EDDSA signature; local HEAD, `origin/main`, GitHub Contents/commit APIs, and `git ls-remote` matched that SHA before the live update.
- After Home Assistant's store/update metadata converged on `0.1.1`, the user completed the update and start. The parent observed `현재 버전: 0.1.1` and `실행 중`, waited 3.5 seconds, and observed the same running state with live resource use.
- The App log still displayed the earlier `0.1.0` EACCES traceback. No new `0.1.1` startup failure appeared while the current process remained running; the historical failure was not deleted or relabeled as a current success log.
- Exactly one `웹 UI 열기` action navigated to the admin Ingress page and one proxied root GET rendered `current:stopped` and `last:null`. Current indexed source shows `GET /` reads only coordinator state, while only `POST /api/capture` calls `startCapture`; no Capture/Stop POST, production transport, EW11/private-LAN, device change, local Docker, package installation, or release action occurred.
- The user separately approved this two-file documentation-only closure commit and push. Web/Context7: N/A for this closure because it introduces no new platform/API claim; same-day M3-E13 evidence remains the applicable documentation basis. Sosumi: N/A: this closure has no Apple API, HIG, or Swift claim.
- The user then explicitly authorized M3.2 through one App `0.1.2` update/start and one up-to-24-hour RX-only capture; they may Stop it early and later request analysis of the finalized data. The authorization includes the signed commit and public push, but excludes device changes, TX/control, protocol interpretation in this unit, packages, local Docker, releases, and a second capture.
- M3.2 bootstrap passed Graphify, CodeGraph, exact-root Serena TypeScript `ready`, Web, Context7, and resolver-fixed exact Luna/max. Tests-first RED was 4/17 PASS with 13 intended failures; initial GREEN was targeted 17/17 and full 19/19 with JSON/diff/index checks PASS. The first exact Sol/max read-only audit returned FAIL with three P1, four P2, and one P3. Exact Luna/max repair round 1 passed targeted 23/23 and full 25/25 plus parent static/index/LSP checks, but its Sol/max re-audit still found two repeated P1, two product P2, and one documentation P2.
- Final repair round 2 reached a parent-reproduced test-only RED at 23/27: exactly four new native tests fail for delayed stream-open error observation, global newest partial/final recovery, production response lifecycle forwarding, and exact dashboard phase behavior. No product path changed during RED.
- The same exact Luna/max role changed product source only in `capture-store.ts`, `m2.ts`, and `ui.ts`; a malformed three-regex test fixture was corrected and two unused store helpers were removed within that same final round. Parent independently passed targeted 27/27, full 29/29, and `git diff --check`. Graphify refreshed to 290 nodes/311 edges and CodeGraph remained current at 11 files/355 nodes/1,016 edges. Serena 1.7.0 remains exact-root TypeScript `ready`; `ui.ts` is clean, no TS6133 remains, and only the historical no-package Node ambient TS2307/TS2580 diagnostics remain. These executable checks did not establish the file-open ordering later rejected by M3-E28.
- Final exact Sol/max re-audit returned FAIL because `watchWriterReady()` unconditionally calls `queueMicrotask(onReady)`, allowing `begin()` to resolve before a real asynchronous `fs.open` error. A read-only `setImmediate` canary reproduced `beginResult:resolved` followed by `appendResult:rejected:capture store is not active`. This repeats M3-E25's storage-error-safety P1 after the second repair round; the new regression test masks production ordering by scheduling its fake error in an earlier Promise microtask. The project stop rule therefore prevents commit, push, Home Assistant update, and live capture.
- The user then replied `승인` to the explicit request for an exceptional third product repair limited to that repeated P1. Fresh bootstrap preserved signed HEAD `909f27d09e0036942983aea59bb847719d0e2183`, the exact ten modified plus two untracked candidate paths, empty staging, current 290-node Graphify and 11-file/355-node/1,016-edge CodeGraph views, and Serena 1.7.0 exact-root TypeScript `ready` with only historical Node ambient diagnostics.
- Current Node 24.19.0 official documentation states that `fs.WriteStream` emits `open` when its file opens and emits `ready` immediately afterward; Context7 `/websites/nodejs_latest-v24_x_api` independently reports that `pending` remains true until `ready`. The exceptional RED must hold `begin()` pending until a later event-loop turn, then require actual `error` rejection/reset; GREEN must remove only the unconditional microtask-ready fallback. Sosumi: N/A: this repair contains no Apple API, HIG, or Swift claim.
- Exact Luna/max changed only `test/m2.test.ts` for the exceptional RED. Parent independently preserved every product/config SHA-256 and empty staging, then reproduced 26/27 PASS: the sole intended failure is `begin must still await the writer event` after a timer turn, proving the current unconditional microtask fallback settles `begin()` before an actual writer event. No product, package, Docker, Home Assistant, socket, EW11/private-LAN, commit, or push action occurred.
- Exceptional GREEN removed only `queueMicrotask(onReady)` from `capture-store.ts`; the same exact Luna/max role corrected only the approved test fixture so its healthy retry emits `open`/`ready` after listeners attach. Parent independently passed targeted 27/27, full 29/29, JSON parse, and `git diff --check`; staging remains empty. Graphify refreshed to 290 nodes/311 edges, CodeGraph is current at 11 files/355 nodes/1,019 edges, and Serena 1.7.0 remains exact-root TypeScript `ready` with no diagnostics in `settings.ts`/`ui.ts` and only the previously recorded absent-Node-declaration TS2307/TS2580 diagnostics elsewhere.
- Fresh exact Sol/max re-audit confirms the writer-open P1 is closed, but returns FAIL on a separate repeated storage-safety P1 and response-adapter P2. `endWriter()` resolves on `finish`, after which finalization removes error observation and renames; an actual Node 24.14.1 no-disk custom-fs canary finalized and renamed before a later synthetic `flush:true` fsync error. Separately, production `startM2Runtime()` spreads the response adapter and snapshots its live `writableEnded` getter; canary result was `adapterLive:true` and `productionSpreadLive:false`. No P0/P3 was found.
- Node 24.19.0 official fs documentation says `flush:true` flushes the underlying descriptor before closing, default `autoClose` closes on `finish`, and default `emitClose` emits `close` afterward. Context7 `/websites/nodejs_latest-v24_x_api` independently states the WriteStream close callback runs once the stream is closed and fsync completion can return an error. Combined with the live no-disk canary, final success/rename cannot be claimed before observed close/error. Sosumi: N/A: this finding has no Apple API, HIG, or Swift claim.
- The user explicitly approved both M3-E34 findings. Fresh read-only bootstrap preserved exact root, Good-signed HEAD `909f27d09e0036942983aea59bb847719d0e2183`, the exact ten modified plus two untracked paths, empty staging, absent trailing-space path, and unread research sibling. Graphify queried the 290-node current graph before CodeGraph; CodeGraph is current at 11 files/355 nodes/1,019 edges. Serena 1.7.0 activated the exact project with TypeScript LSP `ready`; onboarding remained unnecessary.
- Fresh Node 24.19.0 official fs/http evidence and Context7 reconfirmed `flush:true` before close, default close emission, and `ServerResponse.writableEnded` changing after `end()`. Local Node remains 24.14.1. Retrieved instructions did not authorize any external action. Sosumi: N/A: this repair contains no Apple API, HIG, or Swift claim.
- Live `codex debug models` returned the exact `gpt-5.6-luna` slug with `max`; developer-level host registry metadata immutably binds project-local `product_implementer` to that model/effort. A new no-override read-only spawn reported the exact root, Good-signed HEAD, 12-path status, and empty stage with no mutation; its generic GPT-5 self-description was excluded because agent self-report is not runtime identity evidence. Parent independently reproduced the same post-child state and clean diff check.
- Exact Luna/max changed only `test/m2.test.ts` for the additional RED. The fake healthy writer now emits `finish` then `close` and can defer close; parent preserved every product/config SHA-256 and empty staging, then reproduced exactly 27/30 PASS. The three intended failures prove finalization currently settles and renames at `finish`, ignores a late pre-close flush error, and spreads rather than directly hands off the production response adapter.
- Additional GREEN changed only `capture-store.ts` and `m2.ts`: finalization retains error observation until actual `close` before rename, and production passes the live response adapter directly. Parent independently passed targeted 30/30, full 32/32, JSON/static parsing, and `git diff --check`; staging remains empty. Graphify remained current at 290 nodes/311 edges, CodeGraph refreshed current at 11 files/355 nodes/1,044 edges, and Serena 1.7.0 remains exact-root TypeScript `ready` with no diagnostics in `settings.ts`/`ui.ts` and only the historical absent-Node-declaration TS2307/TS2580 diagnostics elsewhere.
- Fresh exact Sol/max final read-only audit returned PASS with no actionable P0-P3 and no repeated P0/P1. Its actual Node custom-fs canaries observed failure as reject with zero rename/unhandled errors and healthy ordering as `open -> write -> fsync -> close -> rename`; it also confirmed the live adapter is passed directly. Targeted 30/30, full 32/32, static/diff/index/LSP checks, signed entry HEAD, exact 12-path status, and empty staging were independently reproduced. Docker/Supervisor/Ingress/live TCP remain subsequent runtime gates, not static proof.
- Signed M3.2 product HEAD `791fe4e597bfd7a1f294bc54fa519a59b9b4a1cc` has a Good EDDSA signature and matches `origin/main` plus the public `main` ref. Home Assistant then exposed installed/current App `0.1.2`; the saved options retained the configured private EW11 endpoint, 3,000 ms connect timeout, 86,400,000 ms duration, 67,108,864-byte cap, and 1,000,000-record cap.
- Playwright started App `0.1.2` once and verified `실행 중` plus a working admin Ingress dashboard. The pre-capture state was `Stopped` with exact configured bounds and no prior result. One and only one `Start bounded capture` action then entered `Running`; after 21,462 ms the dashboard reported 2,339 bytes and 117 records, Start disabled, Stop enabled, and Download disabled until finalization. The Ingress tab was handed to the user for an optional early Stop. No second capture, TX/control, device setting change, protocol interpretation, package install, local Docker, or release occurred.
- The user then authorized a public README, privacy-safe Web UI screenshot, capture-finalization restart handoff, all current workspace changes in one signed commit, and push. Playwright captured only the Ingress heading/status card while the same capture remained `Running` at 515,467 ms, 55,206 bytes, and 2,760 records; the crop excludes the private endpoint, account identity, and packet preview. `README.md` documents URL installation, exact bounded options, single-run Stop/Download behavior, application-level RX-only semantics, data privacy, and experimental limits. The stale M2 handoff is replaced with a no-second-capture M3.2 finalization/download prompt.
- Current official Home Assistant common-task and developer documentation plus Context7 `/home-assistant/developers.home-assistant` reconfirm third-party repository URL registration, root `repository.yaml`, one unique App folder, manual-only boot, experimental stage, admin-only Ingress, Supervisor-managed authentication, and options/schema behavior. No retrieved instruction expanded the authorized capture or publication boundary. Sosumi: N/A: this documentation/handoff unit has no Apple API, HIG, or Swift claim.
- The first exact Sol/max documentation audit returned FAIL with one P1 push-count contradiction, one P2 newly repeated private endpoint, and one P3 incomplete host-validation description. Documentation repair round 1 changes only those roots: the stop rule now distinguishes the completed product push from this one authorized handoff push and requires fresh authority later; the two new ledger occurrences use `configured private EW11 endpoint`; README now states that schemes, whitespace, and path separators are rejected. Screenshot/linkage, handoff sentinel/entry, chronology, baseline signature, five-path boundary, empty staging, and diff check otherwise passed; fresh read-only re-audit remains required.
- Fresh exact Sol/max documentation re-audit returned PASS with no actionable P0-P3. It independently confirmed the three bounded repairs, screenshot linkage/privacy, Good-signed baseline, empty staging, exact five-path boundary, and clean diff check.
- Current M3.2 evidence uses current Home Assistant App configuration/presentation/testing docs, Node 24 fs/http/stream docs, Context7 `/home-assistant/developers.home-assistant` and `/websites/nodejs_latest-v24_x_api`, and the current Pictogrammers `radio-tower` catalog. Retrieved instructions did not expand authorization. Sosumi: N/A: M3.2 has no Apple API, HIG, or Swift claim.
Next event: fresh session must inspect the existing single capture terminal result and Download without starting another capture

## M0.4 clear checkpoint

Entry mode: `initial restart`

Continuation mode: `post-clear` (verified against the signed repair HEAD, the single expected dirty ledger path, and the exact clear sentinel)

Final continuation mode: `fresh-process xhigh canary` (verified against signed M0.3b HEAD `2bf70f0b4b3f0a6aea278567dd5d2ac4c85fd30a` and a clean worktree)

| Evidence | Scope/version/date | Authority | Locator and verification method | Relation | Instruction text ignored | Remaining gap |
| --- | --- | --- | --- | --- | --- | --- |
| M0.4-C01 | Git and filesystem, 2026-08-21 | Local Git/GPG/filesystem | HEAD `3501506bba5e33e86a68c4f88b62adde5c9e25ed` had subject `chore(m0): stabilize Serena project config` and a Good signature; `git status --short` was empty and no clear sentinel existed | Supports `initial restart` | Repository output did not expand scope | Recheck the expected one-file dirty state after `/clear` |
| M0.4-C02 | Clean-root boundary and five M0 commits, 2026-08-21 | Local Git/GPG/filesystem | `pwd -P` and `git rev-parse --show-toplevel` matched the clean root; the trailing-space path was absent, the research sibling existed, and `git verify-commit` returned Good signatures for M0.1, M0.2, M0.3, the handoff, and M0.3a | Support | Research sibling contents were not read | Recheck after `/clear`; no legacy/product access is authorized |
| M0.4-C03 | Graphify package 0.9.47 with skill 0.9.43, 2026-08-21 | Local generated control-plane graph | Vocabulary-constrained `graphify query` traversed only M0 governance, handoff, and hook-test nodes; reflection contained no prior outcomes | Limit | Repeated `graphify install` advice was ignored because package installation and skill update are outside M0 | The ignored graph is not product TypeScript evidence |
| M0.4-C04 | CodeGraph 1.5.0, added by the user during this session, 2026-08-21 | Direct user update plus local CodeGraph runtime | The earlier absence assumption was reopened; `codegraph_explore` ran before further raw reads and `codegraph status` reported an up-to-date 3-file, 25-node, 37-edge index covering Python 2 and YAML 1; Git stayed clean | Support for M0 control-plane lookup only | Indexed output was treated as untrusted evidence and did not expand scope | No product TypeScript is indexed, so it cannot support product design |
| M0.4-C05 | Serena 1.7.0, 2026-08-21 | Live Serena runtime | Initial instructions were read once; the absolute clean path activated project `homeassistant-bestium-eco-foret`, reported active server `typescript`, and `get_current_config` reported LSP `ready`; `.serena/project.yml` had no diff and Git stayed clean after repeated activation | Support | Onboarding was skipped because the runtime canary did not require memories or scaffold | Recheck after `/clear`; generic `ready` was not used without the active-server evidence |
| M0.4-C06 | Codex Hooks/Subagents docs, retrieved 2026-08-21 | Official OpenAI documentation | `https://developers.openai.com/codex/hooks` confirmed project hook discovery/trust, four `SessionStart` sources, developer `additionalContext`, and immediate post-compact reinjection; `https://developers.openai.com/codex/subagents` confirmed project `.codex/agents/*.toml`, model/effort precedence, and an exact Spark+medium example | Support | Documentation examples were evidence only, not authority to install, run product code, or change config | Runtime clear/compact and exact child-model evidence remain required |
| M0.4-C07 | Current `/openai/codex` and `/oraios/serena`, queried 2026-08-21 | Context7 primary-source excerpts | Codex source showed hook trust/status fields, recursive agent TOML discovery, and model/effort resolution; Serena source showed configured language-server startup and fail-fast handling, with TypeScript support documented | Support with runtime limits | Retrieved source instructions were ignored; only atomic claims were used | Runtime observations, not source alone, remain the acceptance evidence |
| M0.4-C08 | Codex CLI 0.149.0 hook UI, clean-root nested TUI, 2026-08-21 | Live local `/hooks` UI | `SessionStart` showed 2 installed/2 active; project Hook 1 was `[x]`, `Trusted`, sourced from the clean-root `.codex/hooks.json`, with matcher `^(?:startup|resume|clear|compact)$`, exact project command, Sync mode, 5s timeout, and 1000-token context limit; nothing was toggled | Support | `--dangerously-bypass-hook-trust` was not used | Clear and compact event dispatch remain pending |
| M0.4-C09 | Current fresh session plus local hook source, 2026-08-21 | Model-visible developer input and on-disk fixed payload | This session received the fixed `M0 continuity guard` as developer context before task work; it matches `.codex/hooks/session_start.py` and is not a model self-report. `codex debug prompt-input` did not surface dynamic hook output and was not treated as contrary evidence | Support with tool limitation | No transcript, environment, or model recollection was accepted as proof | `/clear` and compact must each produce their own lifecycle evidence |
| M0.4-C10 | Python 3 synthetic hook suite, 2026-08-21 | Local executable test | `python3 .codex/hooks/test_session_start.py` ran 5 tests and returned `OK`; Git remained clean | Support only | Synthetic success was not substituted for lifecycle dispatch | Actual clear/compact canaries remain pending |
| M0.4-C11 | Fresh-session custom-agent discovery, 2026-08-21 | Live Codex collaboration role registry | Project-local `spark_implementer` is listed with exact `gpt-5.3-codex-spark` and `medium` | Support for discovery only | TOML values and agent self-description are not runtime proof | After lifecycle canaries, verify the live catalog and spawn one read-only canary with runtime model metadata |
| M0.4-C12 | M0.4 non-Apple scope, 2026-08-21 | Claim-routing decision | Sosumi: N/A: M0.4 has no Apple API, HIG, or Swift claim | N/A | N/A | Re-evaluate when an Apple claim enters scope |
| M0.4-C13 | Post-clear entry boundary, Git and filesystem, 2026-08-21 | Local Git/GPG/filesystem plus persisted checkpoint | HEAD remained signed `3501506bba5e33e86a68c4f88b62adde5c9e25ed` with the required subject; `git status --short` was exactly ` M .agent/progress.md`; the checkpoint ended exactly `Next event: clear`; clean-root, trailing-space absence, research-sibling presence, and all five commit signatures were reverified | Support | Repository output did not expand scope; research contents were not read | Continue from the clear event only; completed pre-clear canaries were not reconstructed |
| M0.4-C14 | Graphify 0.9.47/skill 0.9.43 and CodeGraph 1.5.0, post-clear 2026-08-21 | Live local control-plane indexes | `graphify reflect --if-stale` reported zero prior outcomes; a vocabulary-constrained query traversed the 39-node M0 governance/hook graph. `codegraph_explore` preceded further raw reads and `codegraph status` reported an up-to-date 3-file, 25-node, 37-edge Python/YAML index | Limit | Repeated `graphify install` advice was ignored because package/skill updates are outside M0; graph output was not treated as product evidence | Neither index contains product TypeScript |
| M0.4-C15 | Serena 1.7.0, post-clear 2026-08-21 | Live Serena runtime and tracked Git state | Initial instructions were read once; absolute clean-path activation reported project `homeassistant-bestium-eco-foret` and active server `typescript`; `get_current_config` reported LSP `ready`; `.serena/project.yml` had no diff after activation | Support | Onboarding was skipped because this canary needs no memory or scaffold | No Serena gap remains for M0.4; later product work must re-evaluate after source/version changes |
| M0.4-C16 | Codex Hooks/Subagents and Serena claims, retrieved post-clear 2026-08-21 | OpenAI official documentation, Serena upstream GitHub, and Context7 primary-source excerpts | Current Hooks docs confirmed project trust-by-definition-hash, `startup|resume|clear|compact`, developer `additionalContext`, and pre-next-request compact reinjection; current Subagents docs confirmed project `.codex/agents/*.toml` and exact Spark+medium examples. Context7 `/openai/codex` confirmed hook trust/config and recursive agent discovery/model resolution; `/oraios/serena` confirmed configured LSP activation and TypeScript support | Support with runtime limits | Retrieved instructions and examples were evidence only and did not authorize installation, product code, or fallback | Compact and exact child-model runtime observations remain required |
| M0.4-C17 | Actual `clear` lifecycle dispatch, current root session, 2026-08-21 | Codex event-delivered developer context plus unchanged trusted hook definition | The first model request after the operator's `/clear` contained the fixed `M0 continuity guard` as developer context before task work; it matches `.codex/hooks/session_start.py` exactly. The tracked hook definition/source had no diff, the persisted `/hooks` evidence in M0.4-C08 binds trust to that definition, and post-clear entry state matched exactly | Support | Model recollection, transcript parsing, synthetic execution, and trust bypass were not used as dispatch proof | Actual `compact` dispatch and immediate reinjection remain pending |
| M0.4-C18 | Python 3 synthetic hook suite, post-clear 2026-08-21 | Local executable test and Git | `python3 .codex/hooks/test_session_start.py` ran 5 tests and returned `OK`; hook config/source/test and Serena config had no diff; Git still showed only the expected ledger modification | Support only | Synthetic success was not substituted for clear or compact lifecycle evidence | Actual `compact` dispatch remains pending |
| M0.4-C19 | Actual `compact` lifecycle dispatch and continuity bootstrap, current root session, 2026-08-21 | Codex event-delivered developer context plus live Graphify, CodeGraph, Serena, and Git | Immediately after the operator's `/compact` and before the next model continuation, the fixed `M0 continuity guard` arrived as developer context matching the tracked payload. The continuation then queried the 39-node M0 Graphify graph, explored the current hook through CodeGraph, and reactivated the exact clean-root Serena project with active `typescript` and LSP `ready`; HEAD and the single expected ledger modification remained unchanged | Support | Model recollection, transcript parsing, synthetic execution, package/skill update, and onboarding were not used; Graphify 0.9.47 again warned that its installed skill is 0.9.43 | No hook lifecycle or Serena gap remains for M0.4; exact Spark runtime evidence remains required |
| M0.4-C20 | Codex CLI 0.149.0 live model catalog, 2026-08-21 | Local Codex runtime catalog | `codex debug models` was parsed by exact slug; `gpt-5.3-codex-spark` was visible and its supported reasoning levels included `medium` | Support | Catalog presence was not substituted for child execution | The discovered project role still had to execute without fallback |
| M0.4-C21 | Project-local `spark_implementer` discovery and runtime, 2026-08-21 | Host collaboration resolver/execution metadata plus read-only child result | The current host role registry exposed `spark_implementer` with immutable runtime model `gpt-5.3-codex-spark` and effort `medium`. A spawn using exactly that agent type, no model/effort override, and an isolated prompt completed the bounded read-only canary: clean root and Git toplevel matched, HEAD was `3501506bba5e33e86a68c4f88b62adde5c9e25ed`, and status was exactly ` M .agent/progress.md` | Support | TOML text and agent self-identification were not used as runtime proof; the child was forbidden from reading the research sibling or writing/building/networking | Final adversarial gate must judge the host resolver/execution binding before M0 can complete |
| M0.4-C22 | Post-Spark mutation check, 2026-08-21 | Local Git/filesystem | Root-side `git status --short` still showed only ` M .agent/progress.md`; `git diff --check` passed and tracked hook, Serena, and custom-agent configuration paths had no diff | Support | Child output could not expand its read-only authorization | Final audit, ledger/changelog completion, signed commit, and commit verification remain |
| M0.4-C23 | Final hook and medium-agent component audits, 2026-08-21 | Read-only `codex-hook-auditor` and `codex-custom-agent-auditor` | Both audits reported no actionable P0/P1/P2. The hook audit remains applicable and recorded only POSIX portability as informational P3. The agent audit accepted host resolver/execution metadata for the then-current medium role, but that acceptance was later superseded by the user's xhigh decision | Support and later limit | Auditors made no edits and did not inspect the research sibling | Re-audit the xhigh role after fresh-process runtime evidence; do not repeat hook lifecycle events unless their definition changes |
| M0.4-C24 | User-selected Spark `xhigh` change, Codex CLI 0.149.0, 2026-08-21 | Direct user instruction, live model catalog, OpenAI official Subagents docs, Context7 `/openai/codex`, and project-local TOML | User explicitly requested xhigh. `codex debug models` advertised `low|medium|high|xhigh` for exact Spark; current official docs describe xhigh as supported-model higher effort and permit `model_reasoning_effort` in custom-agent files; Context7 source showed `XHigh` and runtime validation against advertised efforts. The custom-agent creator changed only `model_reasoning_effort = "medium"` to `"xhigh"`; stdlib TOML assertions and `git diff --check` passed | Support | Documentation did not authorize fallback, product code, installation, or networking; Sosumi: N/A because no Apple claim exists | The current process exposed the pre-change medium role metadata, so only a new process can prove xhigh discovery/runtime |
| M0.4-C25 | Post-edit unsigned-worktree xhigh feasibility canary, 2026-08-21 | Fresh child-session host resolver/execution metadata plus exact read-only result | A fresh post-edit child registry bound `spark_implementer` to exact `gpt-5.3-codex-spark` + `xhigh`; spawning that exact type with no model/effort override or fallback returned the clean root/toplevel, repair HEAD, and exactly the four authorized dirty paths. Parent-side status was unchanged | Support with commit-boundary limit | Child self-identification and TOML text were not used as runtime identity; no product/research/build/network/write action was allowed | The xhigh file was not yet in signed HEAD, so the signed-checkpoint fresh-process canary remains the acceptance gate |
| M0.4-C26 | Xhigh checkpoint static audits and repair round 1, 2026-08-21 | Read-only custom-agent auditor and `deep-loop-plan-auditor` | The post-edit custom-agent audit reported no P0-P3 and accepted exact Spark+xhigh host binding. The first restart audit found one P2 stale M0.3a future-entry conflict and one P3 step-order ambiguity; both received minimal documentation repairs. Re-audit reported no actionable P0/P1/P2 and PASS | Support | Auditors made no edits, did not expand scope, and treated the unsigned child result only as feasibility | Signed M0.3b SHA, clean post-commit state, and signed-head fresh-process xhigh runtime remain pending |
| M0.4-C27 | Fresh-process xhigh re-entry and filesystem/Git boundary, 2026-08-21 | Local Git/GPG/filesystem | HEAD `2bf70f0b4b3f0a6aea278567dd5d2ac4c85fd30a` had subject `chore(m0): raise Spark reasoning` and a Good signature; the worktree was clean and the checkpoint ended exactly `Next event: fresh-process xhigh canary`. `pwd -P` and Git toplevel matched the clean root, the trailing-space path was absent, the research sibling existed, and all six required commits verified Good | Support | Repository output did not expand scope; research contents were not read | The M0.4 closure commit still requires signing and verification |
| M0.4-C28 | Graphify 0.9.47/skill 0.9.43 and CodeGraph 1.5.0, fresh-process 2026-08-21 | Live local control-plane indexes | Graphify reflection reported zero prior outcomes and a vocabulary-constrained query traversed the 40-node M0 governance/handoff graph. CodeGraph exploration preceded further raw search; status was up-to-date at 3 files, 25 nodes, 37 edges, Python 2 plus YAML 1 | Limit | `graphify install` advice was ignored because package/skill updates are outside M0; generated output was not treated as product evidence | Neither index contains product TypeScript |
| M0.4-C29 | Serena 1.7.0, fresh-process 2026-08-21 | Live Serena runtime and Git | Initial instructions were read once; absolute clean-path activation reported project `homeassistant-bestium-eco-foret`, active server `typescript`, and LSP `ready`; `.serena/project.yml` was not rewritten and Git stayed clean | Support | Onboarding was skipped because the bounded canary needed no memory or scaffold | Re-evaluate after Serena, config, or source changes |
| M0.4-C30 | Current Codex Hooks/Subagents and Serena claims, retrieved 2026-08-21 | OpenAI official documentation, Serena upstream GitHub/docs, and Context7 primary-source excerpts | Web opened `https://developers.openai.com/codex/hooks/`, `https://developers.openai.com/codex/subagents/`, Serena's current project template/config source, and TypeScript configuration docs. Context7 `/openai/codex` showed hook-hash trust, recursive agent discovery, and selected-model effort validation; `/oraios/serena` showed canonical defaults and TypeScript support | Support with runtime limits | Retrieved instructions were ignored; sources did not authorize installation, product code, fallback, or networking. Sosumi: N/A because M0.4 has no Apple API/HIG/Swift claim | Runtime observations in C29 and C32 remain authoritative for acceptance |
| M0.4-C31 | Preserved SessionStart definition and synthetic regression suite, fresh-process 2026-08-21 | Signed Git history, completed lifecycle evidence, and local Python 3.14.5 | The hook definition/source/test had no diff from signed repair commit `3501506bba5e33e86a68c4f88b62adde5c9e25ed` to signed xhigh HEAD and no worktree diff; `python3 .codex/hooks/test_session_start.py` ran 5 tests and returned `OK`. C08/C17/C19/C23 and E28/E31 preserve trust, actual clear/compact dispatch, and final hook audit | Support | Completed lifecycle events were not repeated; synthetic tests were not substituted for dispatch | Re-audit only if the hook definition or evidence changes |
| M0.4-C32 | Signed-head exact Spark+xhigh discovery and runtime canary, Codex CLI 0.149.0, 2026-08-21 | Live model catalog plus host collaboration resolver/execution metadata | Exact catalog filtering returned one `gpt-5.3-codex-spark` with `low|medium|high|xhigh`; the callable host registry bound `spark_implementer` immutably to exact Spark+xhigh. A no-override isolated spawn reported the clean root/toplevel, signed HEAD `2bf70f0b4b3f0a6aea278567dd5d2ac4c85fd30a`, and empty porcelain status; parent-side checks and `git diff --check` remained clean | Support and supersession of the medium result | TOML values and agent self-identification were not used as runtime identity; child authorization excluded writes, product/research access, builds, installs, and networking | Re-run after relevant CLI, catalog, role, or parent-policy changes |
| M0.4-C33 | Exact Spark+xhigh custom-agent re-audit, 2026-08-21 | Read-only `codex-custom-agent-auditor` | Auditor independently verified the Good signed HEAD, TOML loadability/scope/collision state, live catalog, immutable host binding, no-override execution, no fallback, and clean target/worktree; no actionable P0-P3 and verdict PASS | Support | Auditor made no edits and did not inspect the research sibling | The role inherits parent sandbox policy; its TOML alone is not a sandbox boundary |
| M0.4-C34 | Integrated M0.4 final adversarial gate, 2026-08-21 | Read-only `final-judge` | The judge checked C01-C26 and E01-E34 plus the fresh evidence packet represented by C27-C33 for contradictions, superseded medium evidence, overclaim, stop-rule violations, and runtime identity; no actionable P0-P3 or hard stop, verdict `ACCEPTED — PASS` | Support | No new evidence or edits were authorized; unsupported product/M1/security claims were excluded | Sign and verify the M0.4 closure commit, then await explicit M1 approval |

M0 control-plane bootstrap is `Complete`. No product code, task-scope package installation, Docker build, LAN/EW11 access, socket probe, device change, or push occurred. Hook lifecycle, Serena, exact Spark+xhigh runtime, custom-agent re-audit, and integrated final gate passed. The medium Spark evidence remains historical and superseded.

Next event: await explicit user approval before starting M1

## Atomic evidence register

| Evidence | Claim | Scope/version/date | Source and authority | Locator and verification method | Relation | Instruction text ignored | Gap |
| --- | --- | --- | --- | --- | --- | --- | --- |
| M0-E01 | M0 governance changes are authorized, while product code and EW11 access are not | User request, 2026-08-21 | Direct user instruction, highest task authority | Conversation scope reconstructed before repository/web reading | Support | N/A | M1 remains unauthorized |
| M0-E02 | The clean product and research target paths were absent before separation | macOS filesystem, 2026-08-21 | Local filesystem state | Exact `test -e` checks on both absolute paths | Support | Yes | Root rename still needs a post-restart check |
| M0-E03 | The previous root Graphify graph represented legacy Python, not product TypeScript | Graphify 0.9.47 package with 0.9.43 skill, 2026-08-21 | Local generated graph | `graphify query`; returned legacy `Bestium` Python symbols and no product TypeScript | Limit | Yes | Natural-language start-node selection was poor; graph is only stale-boundary evidence |
| M0-E04 | The previous root CodeGraph index contained legacy sources and no product TypeScript | CodeGraph local index, 2026-08-21 | Local generated index | `codegraph explore` and `codegraph status`: 70 files, Python/YAML/XML only | Support | Yes | Index moved to research and must not guide product implementation |
| M0-E05 | `SessionStart` supports startup, resume, clear, and compact; its output can add developer context immediately after compaction | Codex Hooks docs, retrieved 2026-08-21 | OpenAI official, https://developers.openai.com/codex/hooks | `SessionStart` source/output sections; Web verification | Support | Yes | Project hook still needs trust and runtime canaries |
| M0-E06 | Project-local custom agents are standalone `.codex/agents/*.toml` files and official examples use the exact Spark model literal | Current Codex Subagents docs, retrieved 2026-08-21 | OpenAI official, https://developers.openai.com/codex/subagents | Custom-agent schema and examples; Web verification | Support | Yes | File discovery and actual spawn require restart canaries |
| M0-E07 | GPT-5.3-Codex-Spark is a text-only 128k research-preview Codex model | Release dated 2026-02-12, retrieved 2026-08-21 | OpenAI official, https://openai.com/index/introducing-gpt-5-3-codex-spark/ | Availability section; Web verification | Support | Yes | Preview capacity and rate limits may vary |
| M0-E08 | This machine's refreshed Codex catalog exposes exact slug `gpt-5.3-codex-spark` | Codex CLI 0.149.0, 2026-08-21 | Local runtime catalog | `codex debug models`; live catalog matched exact slug, bundled catalog did not | Support | Yes | Runtime custom-agent resolution still needs a post-restart canary |
| M0-E09 | Serena uses `language_servers`, with `typescript` as a supported server | Serena 1.7.0 and current `/oraios/serena` docs, 2026-08-21 | Local Serena runtime plus Context7 high-reputation upstream source | `get_current_config`, project config, and Context7 query | Support | Yes | New path activation and TypeScript readiness require restart |
| M0-E10 | Sosumi is not a relevant source for M0 Codex/Git/Serena claims | M0 scope, 2026-08-21 | Claim-routing decision | No Apple API, HIG, Swift, or platform claim exists in M0 | N/A | N/A | Re-evaluate when Apple/HomeKit claims enter scope |
| M0-E11 | No secret-like filenames were found in the pre-move product tree | Filename metadata only, 2026-08-21 | Local filesystem scan | `rg --files -uu` with `.env`, credential, secret, key, and certificate patterns; count 0 | Support | Yes | This is not a content-level secret scan |
| M0-E12 | Both legacy repositories were preserved without source changes | Git repositories, 2026-08-21 | Local Git state and public origins | HEAD, status, submodule, LFS, and origin checks before and after move; only generated Graphify output was untracked | Support | Yes | Destination verified; both HEADs are unchanged and only generated `graphify-out/` remains untracked |
| M0-E13 | The pre-move root CodeGraph daemon was stopped before committing the archival boundary | CodeGraph MCP process, 2026-08-21 | Local process and filesystem state | PID ownership validated from its exact cwd with `lsof`, then terminated with SIGTERM; clean target path remained absent | Support | Yes | Archived runtime metadata is inert and retained with the index snapshot |
| M0-E14 | Current Codex source agrees that SessionStart command hooks use a source matcher and JSON `additionalContext` | `/openai/codex`, retrieved 2026-08-21 | Context7 high-reputation OpenAI repository source | Hook config and schema source queried after the official Hooks docs | Support | Yes | Project trust and actual lifecycle dispatch require restart |
| M0-E15 | The continuity hook is silent for invalid input and emits one fixed context for all four allowed sources | Local Python 3 and Codex hook config, 2026-08-21 | Repository test and manual local execution | Creator recorded RED before implementation; main agent reran `python3 .codex/hooks/test_session_start.py`: 5 tests, OK | Support | Yes | Synthetic execution cannot prove Codex trusted and dispatched the hook |
| M0-E16 | The repaired M0.2 hook has no remaining P0/P1/P2 static finding and does not duplicate the existing global Graphify guard | M0.2 adversarial audit, 2026-08-21 | Read-only `codex-hook-auditor` plus local config count | Re-audit checked non-string sources, regression tests, git-root resolution, and project/global PreToolUse counts: project 0, global 1 | Support | Yes | Definition trust and lifecycle dispatch remain M0.4 runtime gates |
| M0-E17 | A project-local agent can declare the exact Spark model in `.codex/agents/*.toml` without a project `config.toml` registration entry | Current Codex docs and source, retrieved 2026-08-21 | OpenAI official Web plus Context7 `/openai/codex` | Custom-agent schema and current examples; required strings plus model and reasoning override | Support | Yes | New-process discovery and actual spawn remain runtime gates |
| M0-E18 | Serena's schema requires only the clean `project_name` and `language_servers: ["typescript"]`, but a tracked file containing only those keys is not stable under Serena 1.7.0 loading | Serena 1.7.0 and current upstream source/template, 2026-08-21 | Local Serena plus Context7 `/oraios/serena` | `ProjectConfig.FIELDS_WITHOUT_DEFAULTS` contains exactly those two fields; the later clean-root load supplied and persisted every omitted default | Limit | Yes | Operational clean-worktree stability is corrected by M0-E25 and still needs one fresh reload |
| M0-E19 | The exact-Spark agent file has no actionable static custom-agent finding | M0.3 adversarial audit, 2026-08-21 | Read-only `codex-custom-agent-auditor` | TOML loadability, allowed fields, model/reasoning catalog, name collision, scope, legacy boundary, and no-fallback behavior checked | Support | Yes | Verdict remains `UNCERTAIN` until discovery/spawn/runtime canaries pass |
| M0-E20 | The global Git hook created an ignored control-plane Graphify graph after M0.2, and M0.3 queried it before raw repository inspection | Graphify 0.9.47 with 0.9.43 skill, 2026-08-21 | Local hook output and generated graph | Post-commit output reported 35 nodes; scoped query returned only M0 governance/hook symbols | Limit | Yes | It is not a product graph and does not justify implementation claims |
| M0-E21 | The product root was renamed by removing only the accidental trailing space after all M0.3 agents and stale-root CodeGraph servers stopped | macOS filesystem and Git, 2026-08-21 | Local process, filesystem, and signed Git state | Exact PID cwd checks, SIGTERM for two stale-root CodeGraph parents, collision checks, exact `mv`, old/new path assertions, clean worktree, and `git verify-commit` | Support | Yes | The fresh session must still activate Serena and trust hooks at the clean path |
| M0-E22 | The current Serena MCP process remains bound to the old registered project and therefore cannot prove clean-path TypeScript readiness | Serena 1.7.0, immediately after rename | Local Serena runtime | `get_current_config` still reported the trailing-space project with no active TypeScript server | Limit | Yes | Intentional restart gate; do not reinterpret generic `ready` as TypeScript readiness |
| M0-E23 | The restart handoff now distinguishes a clean initial restart from the single-file dirty post-clear checkpoint before applying Git stop rules | Authorized third repair round, 2026-08-21 | Direct user authorization, OpenAI official Hooks docs, Context7 `/openai/codex`, and local handoff state | User allowed one additional repair; current docs reconfirmed `clear` as a `SessionStart` source and project-hook hash trust; static branch checks cover both entry patterns | Support | Yes | Static audit closed by M0-E24; signed handoff commit and clipboard identity check remain |
| M0-E24 | The repaired restart handoff has no remaining P0/P1/P2 static planning finding | User-authorized final audit round, 2026-08-21 | Read-only `deep-loop-plan-auditor` | Re-audit verified step ordering, mutually exclusive Git-state modes, no clear loop, one final M0.4 commit, and preserved runtime uncertainty; verdict PASS | Support | Yes | Actual hooks, Serena, and exact-model canaries remain M0.4 runtime gates |
| M0-E25 | Serena fills omitted optional project fields and re-saves an incomplete `project.yml`, explaining the deterministic fresh-start worktree change | Serena 1.7.0, clean-root activation, 2026-08-21 | Live local Serena, current official Serena source, and Context7 `/oraios/serena` | File mtime followed the signed handoff commit; `git diff --numstat` was `168 1`; upstream `_load_yaml_dict` marks missing defaults incomplete and `ProjectConfig.load` re-saves them | Conflict with the operational inference in M0-E18 | Yes | A fresh process must prove the committed canonical file is no longer rewritten |
| M0-E26 | The canonical Serena config preserves the intended boundary and introduces no command, prompt, external workspace, or tool override | Serena-generated 22-key project config and live Serena 1.7.0, 2026-08-21 | Local tracked configuration and current runtime | Ruby safe YAML parse asserted the clean project name, only `typescript`, null activation command, empty initial prompt/additional workspace/ignore/tool lists, root workspace `.`, and `read_only: false`; live LSP is ready | Support | Yes | Static values pass; fresh-process no-rewrite evidence remains |
| M0-E27 | The M0.3a canonical-config and restart-handoff repair has no actionable P0/P1/P2/P3 finding | M0.3a repair audit, 2026-08-21 | Read-only `deep-loop-plan-auditor` | Audited the four authorized paths, bounded Serena values, evidence accuracy, mutually exclusive entry modes, self-SHA handling, M0/M1 status, and commit feasibility; verdict PASS | Support | Yes | Next fresh process must verify this repair HEAD signature and prove no rewrite |
| M0-E28 | The trusted fixed-context hook actually dispatched for both `clear` and `compact`, with compact context present before the next continuation | Codex CLI 0.149.0 lifecycle, 2026-08-21 | Live event-delivered developer context, persisted trusted `/hooks` UI evidence, tracked source, and executable tests | Entry state matched the signed repair HEAD and exact sentinel; each lifecycle event delivered the exact fixed payload before task work; 5 synthetic tests also passed | Support | Yes | Final adversarial gate remains |
| M0-E29 | The canonical Serena config is stable in a fresh process at the clean root with the TypeScript server ready | Serena 1.7.0, 2026-08-21 | Live Serena runtime and Git | Fresh absolute-path activation and post-clear/post-compact reactivation all reported project `homeassistant-bestium-eco-foret`, active `typescript`, LSP `ready`, and no tracked config diff | Support | Yes | Re-evaluate after Serena/config/source changes |
| M0-E30 | The project-local exact Spark role is discoverable and executed the bounded read-only canary without fallback or mutation | Codex CLI 0.149.0 and host collaboration runtime, 2026-08-21 | Live model catalog plus host resolver/execution metadata | Exact catalog slug supported `medium`; host registry bound `spark_implementer` immutably to that model/effort; spawning that exact type with no override returned the expected clean root, HEAD, and ledger-only status, followed by an unchanged root-side check | Support | Yes | Final adversarial gate must accept the resolver/execution binding before M0 completion |
| M0-E31 | The completed hook final audit has no actionable P0/P1/P2 and remains valid after the unrelated Spark effort change | M0.4 hook surface, 2026-08-21 | Read-only `codex-hook-auditor` | Rechecked configuration, fixed-context safety, five tests, definition identity, trust evidence, and actual clear/compact observations; only future Windows portability was informational P3 | Support | Yes | Re-audit only if the hook definition or evidence changes |
| M0-E32 | The user superseded the medium Spark acceptance target with xhigh, which is supported by the exact live model and current Codex configuration semantics | Direct user decision and Codex CLI 0.149.0, 2026-08-21 | User, live catalog, OpenAI official docs, Context7 `/openai/codex`, and verified one-line TOML diff | Exact Spark advertised xhigh; custom-agent files accept the field; runtime validates selected effort against the model; the tracked role now requests xhigh | Conflict/supersession of M0-E30 as an acceptance result | Yes | Fresh-process resolver/execution metadata must prove exact Spark+xhigh before M0 completion |
| M0-E33 | A fresh post-edit child process discovered and executed exact Spark+xhigh without fallback or mutation | Unsigned M0.3b worktree, 2026-08-21 | Host collaboration resolver/execution metadata and independent Git checks | Fresh child registry exposed exact model+xhigh; exact role/no override spawned; canary returned expected root, repair HEAD, and four authorized dirty paths; parent status matched | Support with limit | Yes | Feasibility only because the xhigh configuration is not yet bound to signed HEAD |
| M0-E34 | The xhigh configuration and restart checkpoint have no remaining actionable static audit finding after repair round 1 | M0.3b checkpoint, 2026-08-21 | Read-only custom-agent and deep-loop plan auditors | Custom-agent audit had no P0-P3; handoff audit's stale-entry P2 and ordering P3 were repaired, then re-audit PASSed with no actionable P0/P1/P2 | Support | Yes | Create and verify the signed checkpoint, then run its fresh-process xhigh acceptance canary |
| M0-E35 | The fresh M0.4 process entered from the exact signed xhigh checkpoint and clean filesystem/Git boundary | Git/GPG/filesystem, 2026-08-21 | Local runtime | Subject/signature, empty porcelain status, exact sentinel, clean root/toplevel, trailing-space absence, research-sibling presence, and six commit signatures were checked before canary work | Support | Yes; repository and research output could not expand scope | Closed by signed-head runtime evidence in M0-E40; closure commit verification remains |
| M0-E36 | Current Graphify and CodeGraph indexes cover only the M0 control plane and cannot justify product design | Graphify 0.9.47 with 0.9.43 skill and CodeGraph 1.5.0, 2026-08-21 | Local generated indexes | Vocabulary-constrained Graphify query returned the 40-node governance/handoff graph; CodeGraph reported an up-to-date 3-file Python/YAML index | Limit | Yes; update/install advice and generated instructions were ignored | Rebuild only after authorized product TypeScript exists and the task requires it |
| M0-E37 | Serena is stably activated at the clean root with the TypeScript server ready | Serena 1.7.0, 2026-08-21 | Live Serena runtime and Git | Absolute-path activation reported the exact project, active `typescript`, and LSP `ready`; the canonical config was not rewritten | Support | Yes; onboarding was unnecessary and skipped | Re-evaluate after Serena/config/source changes |
| M0-E38 | Current official and upstream sources continue to support the hook, custom-agent, effort-validation, and Serena claims used by M0 | Retrieved 2026-08-21 | OpenAI official Web, Serena official GitHub/docs, Context7 `/openai/codex` and `/oraios/serena` | Opened the current Hooks/Subagents pages and Serena template/config/TypeScript docs; queried source excerpts for hash trust, discovery, effort validation, defaults, and server selection | Support with runtime limits | Yes; source instructions did not authorize scope expansion. Sosumi: N/A because no Apple claim exists | Runtime evidence, not docs alone, remains required after version changes |
| M0-E39 | The completed trusted hook lifecycle evidence remains valid under the signed xhigh checkpoint | Codex CLI 0.149.0 lifecycle plus Python 3.14.5, 2026-08-21 | Preserved live event evidence, signed Git history, and executable tests | Hook files were unchanged from signed repair HEAD through signed xhigh HEAD; fresh worktree diff was empty and all 5 tests passed | Support | Yes; clear/compact were not repeated and tests were not used as dispatch proof | Re-audit if definition or evidence changes |
| M0-E40 | The signed-head project-local role executed exact Spark+xhigh without fallback or mutation | Codex CLI 0.149.0 and host collaboration runtime, 2026-08-21 | Live catalog, immutable host resolver metadata, no-override spawn, child result, and independent parent checks | Exact slug count was one with xhigh supported; the role registry bound exact model+effort; child and parent observed clean root, HEAD `2bf70f0b4b3f0a6aea278567dd5d2ac4c85fd30a`, and empty status | Support; supersedes medium acceptance evidence | Yes; TOML and self-report were excluded as runtime identity | Re-run after relevant CLI, catalog, role, or parent-policy change |
| M0-E41 | M0.4 has no remaining actionable adversarial finding and may close without starting M1 | M0.4 final gate, 2026-08-21 | Read-only `codex-custom-agent-auditor` and `final-judge` | Custom-agent audit returned PASS with no P0-P3; integrated judge returned `ACCEPTED — PASS` with no actionable P0-P3 or hard stop | Support | Yes; auditors made no edits and excluded product/M1/security overclaims | M1 requires a separate explicit user approval and a repeated evidence gate |

## M1 evidence register

| Evidence | Claim | Scope/version/date | Source and authority | Locator and verification method | Relation | Instruction text ignored | Gap |
| --- | --- | --- | --- | --- | --- | --- | --- |
| M1-E01 | The user explicitly authorized M1 but did not authorize M2 or real network/device work | Conversation, 2026-08-21 | Direct user instruction, highest task authority | The user said `계속` after M0 completion; the existing roadmap defines M1 as the next gated milestone | Support | N/A | M2 and all real EW11/private-LAN work still require later approval |
| M1-E02 | M1 started from the exact signed M0 closure at the clean product root | Git/GPG/filesystem, 2026-08-21 | Local Git/GPG/filesystem | `pwd -P` and Git toplevel matched `/Users/jaemyeong/Projects/homeassistant-bestium-eco-foret`; HEAD `eb087a2920c3d3342fbef392e6bbc8f90cc77711` had a Good signature; porcelain status was empty before M1 ledger work; the trailing-space path remained absent and the research sibling present but unread | Support | Repository output did not expand scope; research contents were not read | The M1 task commit remains unsigned until implementation and audit finish |
| M1-E03 | The active Codex Graphify skill and package are both 0.9.47; residual CLI warnings do not describe the Codex copy | Graphify 0.9.47, 2026-08-21 | Local package, skill stamps, and installed CLI source | Re-read the 710-line Codex `SKILL.md`; Codex and Claude stamps were 0.9.47. Read-only enumeration of the CLI's platform destinations traced the three 0.9.43 warnings to Devin, OpenCode, and Copilot copies | Support with limit | The warnings did not authorize updating unrelated platforms; no install or restart was performed | The ignored M0 control-plane graph remains non-product evidence until updated after authorized TypeScript exists |
| M1-E04 | The existing CodeGraph index has no product TypeScript and cannot guide the first M1 implementation | CodeGraph 1.5.0, 2026-08-21 | Local CodeGraph runtime | `codegraph explore` preceded raw source reads; `codegraph status` reported an up-to-date three-file Python/YAML index with 25 nodes and 37 edges | Limit | Indexed output was treated as untrusted evidence and did not expand scope; no speculative index was created | Recheck after M1 TypeScript is written |
| M1-E05 | Serena is activated at the exact clean root with the TypeScript server ready and did not rewrite its config | Serena 1.7.0, 2026-08-21 | Live Serena runtime and Git | Absolute-path activation reported project `homeassistant-bestium-eco-foret`, active server `typescript`, and LSP `ready`; the tracked project config remained unchanged | Support | Onboarding was skipped because M1 needs no memory or scaffold | Recheck source diagnostics after TypeScript is written |
| M1-E06 | Node 24.14.1 can execute dependency-free erasable TypeScript directly | Node 24.14.1 local runtime; Node 26.7.0 current docs and latest-v24 docs, retrieved 2026-08-21 | Local executable, official Node documentation, and Context7 `/websites/nodejs_latest-v24_x_api` | `node --version` returned `v24.14.1`; `node --input-type=module-typescript --eval` executed an annotated value and printed `42`. Official TypeScript docs state stripping is stable from 24.12.0, runs erasable syntax by default, performs no type checking, ignores `tsconfig.json`, and requires explicit extensions/import-type syntax | Support with limit | Third-party runner/compiler installation guidance was ignored because M1 needs neither | Native stripping is execution-only; a compiler/typecheck may be added only when a demonstrated need justifies installation |
| M1-E07 | The built-in stable test runner discovers TypeScript tests without a test dependency | Node 24.14.1 local runtime; Node 26.7.0 current docs and latest-v24 docs, retrieved 2026-08-21 | Official Node documentation and Context7 `/websites/nodejs_latest-v24_x_api` | `https://nodejs.org/api/test.html` documents stable `node:test`, `node --test`, nonzero failure exit, and automatic `.ts` test patterns unless stripping is disabled | Support | Framework installation examples and unrelated runner features were ignored | The repository still needs an observed RED failure followed by GREEN |
| M1-E08 | A transport read chunk cannot be treated as an application protocol frame | TCP standard RFC 9293, retrieved 2026-08-21 | IETF Standards Track RFC Editor | RFC 9293 sections 2.2 and 3.7 define reliable in-order byte-stream service and state that TCP guarantees no correlation between segment boundaries and application read/write-buffer boundaries | Support | RFC text was evidence only and did not authorize a socket probe | The eventual protocol framing rules remain unknown and are intentionally outside M1 |
| M1-E09 | The smallest M1 contract is a pure synthetic chunk recorder with no I/O | M1.0 decision, 2026-08-21 | User scope plus M1-E06 through M1-E08 | Planned acceptance: a non-empty `Uint8Array` plus caller-provided nonnegative safe-integer millisecond timestamp yields zero-based sequence, timestamp, byte length, and lowercase hex; invalid input does not consume sequence; no parsing, framing, socket, filesystem, persistence, Docker, or Home Assistant packaging | Support with explicit limit | Sosumi: N/A because M1.0 contains no Apple API, HIG, or Swift claim | Exact Spark must create the failing native test before the minimum implementation; adversarial review remains required |
| M1-E10 | The M1.0 test existed and failed for the intended missing implementation before product implementation | Node 24.14.1 native test runner, 2026-08-21 | Exact project-local Spark role plus independent parent execution | Spark created only `package.json`, `docs/m1-capture-contract.md`, and `test/capture.test.ts`; both Spark and parent ran `npm test` and observed exit 1 with `ERR_MODULE_NOT_FOUND` for the deliberately absent `src/capture.ts`, zero passing and one failing test file | Support for RED | No implementation or package installation occurred in the RED phase | The same exact Spark role must add only `src/capture.ts` and prove GREEN |
| M1-E11 | The minimum implementation satisfies the executable M1.0 contract | Node 24.14.1 native test runner, 2026-08-21 | Exact project-local Spark role plus independent parent execution | The same no-override Spark role added only `src/capture.ts`; validation precedes sequence mutation and lowercase hex covers every byte. Spark and parent independently ran `npm test`: exit 0, two tests passed, none failed | Support for GREEN | Passing tests do not claim protocol parsing, socket behavior, Home Assistant packaging, or production readiness | Read-only adversarial acceptance remains required |
| M1-E12 | Current semantic/index checks cover the new product files, with one explicit execution-only typing limit | Graphify 0.9.47, CodeGraph 1.5.0, Serena 1.7.0, 2026-08-21 | Local generated indexes and live TypeScript language server | `graphify update .` rebuilt the ignored graph to 71 nodes/63 edges; vocabulary-constrained BFS found the contract, package, test, implementation, and import edge. CodeGraph updated to five indexed files including two TypeScript files. Serena reported no `src/capture.ts` diagnostics; the test has only TS2307 for absent Node declaration packages | Support with limit | Graphify's unrelated-platform skill warnings and label-refresh suggestion did not authorize installs or semantic labeling; no `@types/node` package was added to hide an execution-only limitation | Native stripping performs no typecheck; add a compiler and Node declarations only when a later approved milestone needs a maintained typecheck gate |
| M1-E13 | Adversarial repair round 1 closed all actionable M1.0 findings | Uncommitted M1.0 candidate, 2026-08-21 | Read-only `code-reality-verifier` plus exact Spark repair and parent checks | Initial audit found one P2 stale future-state doc line and one P3 test-coverage gap. Exact Spark changed only the contract and test, adding non-`Uint8Array` and unsafe-integer cases while retaining sequence 0 after failures; parent reran `npm test` and `git diff --check`. Re-audit returned PASS with no P0-P3 | Support | The reviewer made no edits; the unchanged TS2307 Node declarations were correctly retained as a non-blocking execution-only limit, not hidden or overclaimed | Signed task commit and signature verification remain |
| M1-E14 | The final M1.0 candidate remains bounded and executable after repair | Node 24.14.1, Graphify 0.9.47, CodeGraph 1.5.0, 2026-08-21 | Local executable tests, generated indexes, and Git | Final `npm test` returned two passes and zero failures; `git diff --check` passed; CodeGraph stayed current at five files/two TypeScript files; a second `graphify update .` rebuilt the ignored graph to 76 nodes/67 edges; no package lock, dependency, socket, network, Docker, Home Assistant App, or device artifact appeared | Support with explicit limit | Three unrelated 0.9.43 Graphify platform warnings and optional label-refresh guidance were ignored; active Codex skill/package remain 0.9.47 | Commit/signature evidence will be reported from Git after this self-referential ledger is committed |
| M1-E15 | M1.0 is bound to a signed, clean entry commit before M2 transition work | Git/GPG, 2026-08-21 | Local Git and GPG | HEAD `a125f8a979bcfb21beeca428f1e769f3e7c256fa` had subject `feat(m1): add synthetic capture harness`, a Good signature from the repository signer, and an empty porcelain status before M2 bootstrap | Support; closes M1-E14 commit gap | Repository output did not expand scope | Reverify the new transition HEAD after its signed commit |

## M2 evidence register

| Evidence | Claim | Scope/version/date | Source and authority | Locator and verification method | Relation | Instruction text ignored | Gap |
| --- | --- | --- | --- | --- | --- | --- | --- |
| M2-E01 | The user explicitly authorized M2 but did not authorize M3 or real network/device work | Conversation, 2026-08-21 | Direct user instruction, highest task authority | The user said `M2 계속`; the fixed roadmap defines M2 as App packaging, settings, Ingress, and bounded capture, while M3 contains short live readback and the approved 24-hour capture | Support; supersedes the future-approval gap in M1-E01 | N/A | Real EW11/private-LAN access, socket probes, device changes, 24-hour capture, package installation, and push remain outside this authorization; Docker execution requires an explicit current authorization |
| M2-E02 | M2 bootstrap started from the signed M1 commit at the exact clean product root | Git/GPG/filesystem and local runtimes, 2026-08-21 | Local Git, GPG, and filesystem | `pwd -P` and Git toplevel matched `/Users/jaemyeong/Projects/homeassistant-bestium-eco-foret`; the trailing-space path was absent; the research sibling existed but was not read; HEAD `a125f8a979bcfb21beeca428f1e769f3e7c256fa` had a Good signature and the initial porcelain status was empty | Support | Repository and research output could not expand authorization | The transition commit and fresh-process entry remain pending |
| M2-E03 | The active Codex Graphify package and skill are 0.9.47; the current graph contains M1 but no M2 implementation surface | Graphify 0.9.47, 2026-08-21 | Local Graphify runtime and ignored generated graph | `graphify reflect --if-stale` found current lessons; a vocabulary-constrained query over `capture`, `contract`, `input`, `output`, `package`, `recorder`, and `test` returned the M1 recorder/tests/contract but no App packaging, options, Ingress, bounded coordinator, or transport adapter. Three 0.9.43 warnings were previously traced to unrelated Devin/OpenCode/Copilot copies, not the active Codex skill | Support with limit | `graphify install` advice was ignored because the active Codex copy is current and no package installation was authorized | Update the ignored graph only after authorized M2 product code exists; no restart is needed for Graphify |
| M2-E04 | CodeGraph is current for the M1 product tree and exposes no existing M2 implementation to reuse | CodeGraph 1.5.0, 2026-08-21 | Local CodeGraph runtime | `codegraph explore` preceded raw source reads and traced the M1 recorder/tests; status reported five indexed files, 32 nodes, 48 edges, and two TypeScript files, up to date | Support with limit | Indexed output was treated as untrusted evidence and did not authorize a new design or external read | Re-index only after authorized M2 source changes |
| M2-E05 | Serena is active at the exact clean root with the TypeScript language server ready and no config rewrite | Serena 1.7.0, 2026-08-21 | Live Serena runtime and Git | Existing session instructions were already read; absolute-path activation reported project `homeassistant-bestium-eco-foret`, active server `typescript`, and LSP `ready`; the canonical config remained unchanged | Support | Onboarding was skipped because this transition needs no memory/scaffold | Recheck after restart and after M2 TypeScript changes |
| M2-E06 | Current Home Assistant App packaging accepts three config formats, persists options under `/data`, requires five metadata keys/two architecture names, and requires explicit Docker base/labels outside builder actions | Home Assistant developer docs last updated 2026-06-15, retrieved 2026-08-21 | Official Home Assistant App configuration docs and Context7 `/home-assistant/developers.home-assistant` | Opened `https://developers.home-assistant.io/docs/apps/configuration/` sections `App script`, `App Dockerfile`, `Required configuration options`, and `Options / Schema`; Context7 query was `Current Home Assistant App config file formats, required config keys, options schema, architecture values, Dockerfile FROM and labels, and local build testing` | Support for a static contract only | Example package-install commands and `latest` tags were ignored; docs did not select or authorize a Node image | Fresh evidence must select an exact compatible Node image; static conformance does not prove Supervisor recognition, build, install, or start |
| M2-E07 | Current Home Assistant Ingress requires enabled Ingress, matching internal port, exact gateway-source restriction, and no duplicate app authentication; broad host/container privileges reduce security | Home Assistant developer docs last updated 2026-06-15, retrieved 2026-08-21 | Official Home Assistant presentation docs and Context7 `/home-assistant/developers.home-assistant` | Opened `https://developers.home-assistant.io/docs/apps/presentation/` sections `Ingress` and `Security`; Context7 query was `Current Home Assistant App Ingress configuration and security requirements including ingress port, X-Ingress-Path, trusted source address, authentication, ports, and host networking` | Support for M2 security acceptance | Nginx/package-install examples and all privilege-expanding examples were ignored | Static tests cannot prove Supervisor proxying or runtime peer-address behavior; those remain unobserved until a later authorized runtime gate |
| M2-E08 | Official local-App documentation describes standalone Docker build/run, but documentation does not itself authorize either action | Home Assistant developer docs last updated 2026-06-15, retrieved 2026-08-21 | Official Home Assistant local testing docs and the same Context7 packaging query | Opened `https://developers.home-assistant.io/docs/apps/testing/` sections `Local build` and `Local run`; observed the documented `docker build` command, data volume, port mapping, and architecture mapping | Limit, not execution authority | Devcontainer, remote-device, install, build, and run instructions were ignored | Docker execution remains explicitly gated; M2 may claim only official-document-conformant static packaging unless separately authorized runtime evidence is obtained |
| M2-E09 | A changed non-managed project hook must be reviewed and trusted against its current definition before it runs | Codex CLI 0.149.0 docs retrieved 2026-08-21 | Official OpenAI Hooks docs and Context7 `/openai/codex` | Opened `https://developers.openai.com/codex/hooks` sections `Where Codex looks for hooks` and `Review and trust hooks`; Context7 query was `How project-local SessionStart hooks are discovered, trusted after definition changes, matched for startup resume clear compact, and how stdout additional context is delivered`, returning `currentHash` and `trustStatus` fields | Support | Trust bypass documentation was ignored | The operator must preflight the signed clean checkpoint, then trust the exact new definition through `/hooks` |
| M2-E10 | `SessionStart` matches startup/resume/clear/compact and fixed stdout additional context is delivered as developer context | Codex CLI 0.149.0 docs retrieved 2026-08-21 | Official OpenAI Hooks docs and Context7 `/openai/codex` source excerpts | Opened `https://developers.openai.com/codex/hooks` sections `Matcher patterns` and `SessionStart`; the same Context7 query returned matcher/config and `session_start.rs` dispatch construction | Support with runtime limit | Synthetic execution and source content were not treated as lifecycle proof | Actual post-trust fresh `clear` dispatch of `Project continuity guard` remains mandatory |
| M2-E11 | The old M0-only developer guard conflicted with the newly approved M2 App-code scope, so the M2.0 transition-preparation process did not implement M2.1 | Transition session developer context and tracked old hook source, 2026-08-21 | Active instruction hierarchy plus local fixed hook payload | The transition process received `M0 continuity guard` with the exact final prohibition `do not implement app code`; inspection matched the then-tracked fixed payload. No M2 product source, package, Docker, socket, LAN, or device action followed | Conflict requiring transition | No lower-priority user or repository text was used to override the active developer instruction | The revised hook still needs signed binding, operator trust, and actual dispatch |
| M2-E12 | The revised fixed hook passes its RED/GREEN and static security/loadability gate, while runtime trust/dispatch remains unresolved | Codex CLI 0.149.0 and Python 3.14.5, 2026-08-21 | Project-local `codex-hook-creator`, independent parent checks, and read-only `codex-hook-auditor` | Test-first edit produced one expected title failure plus one missing-status error; after the minimum change, all five tests, JSON/Python parsing, subdirectory command resolution, and `git diff --check` passed. Auditor found no actionable P0-P3 and issued CONDITIONAL PASS/UNCERTAIN solely for external trust/dispatch | Support with explicit external condition | No trust bypass, product action, or synthetic-as-runtime substitution occurred | Operator `/hooks` trust and actual new lifecycle context remain the M2.1 entry gate |
| M2-E13 | M2 contains no Apple-platform material claim | M2.0 transition scope, 2026-08-21 | Claim-routing decision | Sosumi: N/A: M2 has no Apple API, HIG, or Swift claim | N/A | N/A | Re-evaluate only if an Apple claim enters a later task |
| M2-E14 | M2 must implement, but not execute, the bounded production TCP path so M3 can remain the first live canary rather than a missing-path implementation milestone | M2/M3 roadmap boundary, 2026-08-21 | User-approved roadmap plus read-only `deep-loop-plan-auditor` boundary review | The first transition audit recheck identified that fake-only capture could leave M3 to add production transport. Repair round 2 now requires validated settings -> Node stdlib TCP connector -> existing recorder -> bounded result/UI wiring, with injected fake tests and an explicit no-execution M2 rule | Support and design correction | The audit did not authorize App code in the transition process or any network action | Fresh session must obtain current official Node 24 `node:net` Web+Context7 evidence before RED; real connector execution remains M3-only |
| M2-E15 | The signed-transition candidate has no remaining actionable static planning finding after the two permitted repair rounds | M2.0 transition candidate, 2026-08-21 | Read-only `deep-loop-plan-auditor` plus parent executable checks | Initial audit found three P1 and three P2/P3 entry, Docker-context, traceability, and state defects; recheck found two P2 bootstrap-order/production-path defects. Repair rounds 1 and 2 applied the smallest fixes. Final re-audit confirmed Graphify -> CodeGraph ordering, exact sentinel, read-only canary, pre-trust verification, production TCP/fake boundary, M3 live boundary, Docker exclusions, and signed flow; verdict PASS with no actionable P0-P3. Parent reran hook 5/5, JSON parse, code-fence/sentinel/order assertions, and `git diff --check` | Support | Auditors made no edits and no runtime/product/network action occurred | Runtime hook trust/dispatch and exact Spark+xhigh remain explicit fresh-process gates; this PASS does not complete M2 |
| M2-E16 | Fresh M2.1 entry is bound to the signed transition commit, exact root, clean state, and exact sentinel | Git/GPG/filesystem, 2026-08-21 | Local Git, GPG, and filesystem | Before product inspection, `pwd -P` and Git toplevel both returned the exact clean root; HEAD `fba17e0ab6dc0ce001cb27a4c6d70cddd78f7112` had subject `chore(m2): prepare milestone transition`, parent `a125f8a979bcfb21beeca428f1e769f3e7c256fa`, and a Good EDDSA signature; porcelain status was empty; the checkpoint ended exactly `Next event: fresh-process M2 hook trust and scope canary`; the trailing-space path was absent and only the research sibling's existence was checked | Support; closes M2-E02 transition gap | Repository output could not expand authorization; research contents were not read | The eventual M2.1 commit still requires explicit staging, signing, and verification |
| M2-E17 | The newly trusted lifecycle dispatched the milestone-neutral guard and its tracked definition remains exact | Codex CLI 0.149.0 and project hook, 2026-08-21 | Actual model-visible developer context, official OpenAI Hooks docs, and tracked hook checks | The active developer payload begins `Project continuity guard:` and contains no `M0 continuity guard` or `do not implement app code`; `.codex/hooks.json` has matcher `^(?:startup|resume|clear|compact)$`, exact Git-root-resolving command, status `Loading project continuity guard`, timeout 5, and context limit 1000; tracked source matches the visible payload; `python3 .codex/hooks/test_session_start.py` passed 5/5 | Support; closes M2-E09 through M2-E12 runtime gap | Synthetic output and source text were not substituted for the actual developer context; trust bypass documentation was ignored | Hook behavior is proven for this entry lifecycle, not every future CLI version or modified definition |
| M2-E18 | Required bootstrap and collaboration capacity are available, and the indexed product tree contains only the reusable M1 recorder before M2 RED | Graphify 0.9.47, CodeGraph 1.5.0, Codex collaboration/runtime, 2026-08-21 | Live local tools, ignored indexes, and current session capability registry | `graphify reflect --if-stale` plus a scoped query preceded `codegraph explore`; both found `createCaptureRecorder` and its M1 test flow but no App config, settings, Ingress, bounded coordinator, or production transport. Web, Context7, project-local subagent discovery, eight collaboration slots, unrestricted sandbox, approval-never, and network access were present | Support with capability limit | Three unrelated Graphify 0.9.43 copy warnings and all install/update suggestions were ignored; indexes did not authorize a design or research-sibling read | Refresh ignored Graphify and existing CodeGraph views after product changes; generated output remains unstaged |
| M2-E19 | Serena passes the clean-root TypeScript readiness gate without rewriting canonical configuration | Serena 1.7.0, 2026-08-21 | Live Serena MCP and Git | Initial instructions were read once; absolute-path activation reported project `homeassistant-bestium-eco-foret`, exact root, active TypeScript server, and LSP `ready`; Git showed no `.serena/project.yml` diff | Support; closes M2-E05 restart gap | Onboarding, memory, and scaffold creation were skipped as unnecessary | Re-run diagnostics after M2 TypeScript changes; absent Node declaration packages may retain the known editor-only TS2307 limit |
| M2-E20 | Current Home Assistant documentation supports the chosen static App configuration and Ingress security boundary | Home Assistant developer docs updated 2026-06-15 and Context7 `/home-assistant/developers.home-assistant`, retrieved 2026-08-21 | Official Home Assistant configuration, presentation, and testing docs plus Context7 | Fresh reads verified config required keys, `aarch64|amd64`, `/data/options.json`, schema bounds, explicit Docker `FROM` and `io.hass.*` labels, `boot: manual_only`, `stage: experimental`, admin-only Ingress on 8099, source `172.30.32.2` only, and Home Assistant-managed authentication | Support for static M2 acceptance | Package install, Docker build/run, port-publishing, broad privilege, and remote-device instructions were ignored because they were neither needed nor authorized | Static checks cannot establish Supervisor recognition/build/install/start or real Ingress proxy/address behavior |
| M2-E21 | `node:24.19.0-bookworm-slim` is a current explicit Node 24 base supporting both required architectures | Node Docker Official Image metadata and Node Docker repository, retrieved 2026-08-21 | Official Docker Hub Node image tags and official `nodejs/docker-node` metadata | Docker Hub listed exact tag `24.19.0-bookworm-slim` with linux/amd64 and linux/arm64/v8 variants; `versions.json` lists Node 24 bookworm-slim support for amd64 and arm64v8 | Support for an explicit pinned `FROM` | `latest`, Alpine/musl, package-install, pull, build, and run instructions were ignored | The image is selected from registry metadata only; no image pull/build/run or Home Assistant compatibility execution occurred |
| M2-E22 | Node 24 stdlib provides the required TCP lifecycle, but timeout requires explicit destruction and complete cleanup | Node 24.19.0 `node:net` docs and Context7 `/websites/nodejs_latest-v24_x_api`, retrieved 2026-08-21 | Official Node.js API documentation plus Context7 | Fresh evidence verified `net.createConnection`, connect/error/data/timeout/close events, that timeout only notifies, and that `destroy()` stops I/O and closes the socket; the implementation contract therefore requires explicit timer/listener/socket cleanup on every bounded stop | Support for an injected production boundary | Examples were treated as API evidence only and never executed against any host | The production connector must remain unexecuted in M2; fake transport tests can prove coordinator behavior but not real TCP timing |
| M2-E23 | The exact project-local Spark runtime gate passed without fallback or mutation | Codex CLI 0.149.0 live catalog and collaboration resolver, 2026-08-21 | Live `codex debug models`, immutable project-role metadata, child report, and independent parent Git checks | Catalog parsing found one exact `gpt-5.3-codex-spark` entry with efforts `low,medium,high,xhigh`; a no-override `spark_implementer` canary used the resolver-bound exact model and `xhigh`, reported exact root/HEAD and empty status, and made no changes; after child exit the parent independently confirmed HEAD `fba17e0ab6dc0ce001cb27a4c6d70cddd78f7112`, empty porcelain status, and clean diff check | Support; closes the final entry gate | TOML text and agent self-report were not used as runtime identity; no fallback role/model was allowed | Product work must continue through the same exact role and preserve the test-first gate |
| M2-E24 | The minimum executable M2.1 contract is bounded, offline-testable, and leaves all live behavior to later authorization | M2.1 scope decision, 2026-08-21 | User handoff, current official evidence M2-E20 through M2-E22, M1 recorder, and Ponytail minimum-change constraint | RED must cover mandatory host/port plus strict finite bounds, `/data/options.json`, static App/Docker allowlists, normalized peer `172.30.32.2`, current/last-result UI/API, single active capture, each stop reason, production wiring, and complete cleanup through an injected fake transport; GREEN must reuse `createCaptureRecorder` and Node stdlib only | Support with explicit boundary | No new dependency, speculative abstraction, translations/icons/deploy scaffold, Docker execution, production socket creation, protocol parsing, or research-sibling access is permitted | Supervisor/Docker/Ingress runtime and the first real bounded connector/export/readback canary remain M3 candidates requiring new approval |
| M2-E25 | The reviewed M2.1 test suite reached intentional RED before any M2 product implementation | Node 24.14.1 native test runner, 2026-08-21 | Exact no-override project-local Spark role plus independent parent execution | Spark changed only `test/m2.test.ts`; parent ran `npm test` after reviewing and correcting the test contract. Exit was 1 with 11 tests: the two existing M1 tests and M1-recorder reuse check passed, while eight M2 tests failed only because `config.json`, `Dockerfile`, `.dockerignore`, and `src/m2.ts` were absent. No syntax or existing-runtime regression occurred | Support for RED | No product/config/container file, dependency, Docker process, server, socket, LAN, or research artifact was created or accessed | The same exact Spark role must add only the minimum assigned GREEN files; parent must independently rerun the suite |
| M2-E26 | Exact Spark capacity became unavailable after it wrote the assigned GREEN candidate, so no fallback or completion claim is permitted | Project-local Spark role and direct user report, 2026-08-21 | Live collaboration state plus the user's authoritative quota notice | The exact role was still running after creating all five assigned product paths when the user reported `spark 의 주간 사용량 한도 초과됬어`; the parent interrupted the agent, observed no fallback, and preserved the worktree. No other agent or parent edited product code | Limit and mandatory stop condition | Silent fallback, parent product edits, extra agent substitution, and commit were rejected | Restore exact `gpt-5.3-codex-spark` at `xhigh`; re-establish its runtime capacity before any product repair or acceptance closure |
| M2-E27 | The preserved dirty candidate is test-GREEN but remains unaudited and unaccepted | Node 24.14.1 and Git worktree, 2026-08-21 | Independent parent read-only source inspection, native tests, and Git | Parent first verified the module's direct-execution guard prevents import-time production startup, then ran `npm test`: exit 0, 11 passed, 0 failed. `git diff --check` passed. Expected paths are modified `.agent/progress.md` and untracked `.dockerignore`, `Dockerfile`, `config.json`, `src/m2.ts`, `src/settings.ts`, and `test/m2.test.ts` | Partial support only | No Docker build/run, package installation, real server/socket, EW11/LAN access, Graphify/CodeGraph refresh, adversarial audit, staging, commit, or push occurred | GREEN tests alone do not satisfy M2. Required exact-Luna runtime proof, semantic/index verification, read-only adversarial audit, possible Luna repair, final ledger/changelog, explicit staging, signed commit, and signature verification remain |
| M2-E28 | The user superseded future Spark restoration with project-local `product_implementer` on exact `gpt-5.6-luna` at `max` | Direct user decision and current model/config evidence, 2026-08-21 | User instruction, official OpenAI GPT-5.6 Luna and Subagents docs, Context7 `/openai/codex`, and live Codex CLI 0.149.0 catalog | The user requested `5.6 루나 max`; official model docs list exact slug `gpt-5.6-luna` and `max`; official Subagents docs place project agents in `.codex/agents/*.toml`, make `name` the identity, and allow file-level `model`/`model_reasoning_effort` precedence; Context7 returned the resolver path and exact Luna catalog entry; live exact-slug filtering returned default `medium` plus supported `low,medium,high,xhigh,max` | Support and explicit supersession of M2-E26's Spark-restoration gap | The default `medium`, alternative model tiers, explicit spawn overrides, product execution, install/network instructions, and fallback were not used | The current process still carries the old developer guard; only a revised-hook trust/restart plus runtime resolver metadata and no-override canary can prove the new role |
| M2-E29 | The static Luna/max custom-agent and milestone-neutral hook transition passes its specialized creator/auditor gates | Codex CLI 0.149.0, Python 3.14.5, and project-local control plane, 2026-08-21 | `codex-custom-agent-creator`, `codex-hook-creator`, matching read-only auditors, and parent diff review | The agent lane replaced `spark_implementer.toml` with `product_implementer.toml` using exact Luna/max and no fallback; TOML/catalog/whitespace checks passed and its auditor found no P0-P3. The hook lane observed RED, changed only the delegation bullet/test sentinels, then passed 5/5, Python AST, hooks JSON, subdirectory command resolution, `git diff --check`, and a no-P0-P3 hook audit | Static support with external condition | Neither lane touched the M2 candidate, research sibling, trust UI, lifecycle dispatch, product runtime, Docker, network, staging, commit, or push | Bind control-plane/docs changes to a signed checkpoint, then operator `/hooks` review/re-trust, one `/clear`, fresh `product_implementer` discovery, and exact Luna/max no-override canary remain mandatory |
| M2-E30 | Fresh M2 resumed from the exact signed Luna transition and preserved dirty boundary | Git/GPG/filesystem, 2026-08-21 | Local Git, GPG, filesystem, and actual model-visible developer context | `pwd -P` and Git toplevel matched the clean root; HEAD `43b354988f4e2e0679d928927e2c286ef0d75525` had subject `chore(m2): migrate implementer to Luna` and a Good EDDSA signature; the Current checkpoint sentinel matched; the trailing-space path was absent; status contained only the exact six untracked candidate paths. The delivered developer context was the milestone-neutral `Project continuity guard` with exact Luna/max delegation | Support; closes the signed-entry and lifecycle-dispatch gates | Repository text and the research sibling's existence did not expand authorization; synthetic hook output was not substituted for the delivered developer context | The eventual M2 task commit and signature remain pending |
| M2-E31 | Required bootstrap, evidence, collaboration, and Serena capacity are available without rewriting the preserved candidate | Graphify 0.9.47, CodeGraph 1.5.0, Serena 1.7.0, Codex CLI 0.149.0, 2026-08-21 | Live local tools, generated indexes, Serena MCP, host capability policy, and project hook tests | Graphify reflection was already current and its vocabulary-constrained query found only the M1 recorder flow, so it was recorded stale for M2. CodeGraph ran next and its watcher reported 8 files, 180 nodes, 410 edges, five TypeScript files, and current M1-to-M2 flow. Serena initial instructions were read once; activation reported the exact project/path, active TypeScript server, LSP `ready`, and no config diff. Web, Context7, eight session slots, configured depth 2, unrestricted sandbox, approval-never, and network were present. Hook config assertions and 5/5 tests passed | Support with index/tool limits | Three unrelated Graphify 0.9.43 copy warnings and install/update advice were ignored; generated indexes were not treated as design authority | Refresh/query Graphify and re-query CodeGraph after final product changes; generated output remains unstaged |
| M2-E32 | Fresh primary and Context7 evidence still supports the bounded static M2 packaging, Ingress, image, and TCP contract | Home Assistant docs updated 2026-06-15, Node 24.19.0 docs, Node Docker Official Image metadata, retrieved 2026-08-21 | Official Home Assistant developer docs, official Node API docs, official `nodejs/docker-node` metadata/Docker Hub, Context7 `/home-assistant/developers.home-assistant` and `/websites/nodejs_latest-v24_x_api` | Fresh Web reads verified `config.{json,yml,yaml}`, required keys, `/data/options.json`, mandatory schema, `manual_only`, `experimental`, admin-only Ingress 8099, only `172.30.32.2`, Home Assistant-managed auth, explicit `FROM` and `io.hass.*` labels. Docker Hub and `versions.json` verified `node:24.19.0-bookworm-slim` and amd64/arm64v8. Node docs plus Context7 verified `net.createConnection`, connect/data/error/timeout/close, timeout notification without severing, and explicit `destroy()` cleanup | Support for static packaging and injected-boundary behavior only | Docker build/run, package install, remote-device, port-publication, and live-network examples were ignored. Sosumi: N/A: M2 has no Apple API, HIG, or Swift claim | Supervisor recognition/build/install/start, real Ingress proxy/address behavior, and production TCP timing remain unverified and outside M2 execution |
| M2-E33 | The exact project-local Luna/max runtime canary passed without fallback or mutation | Codex CLI 0.149.0 live catalog and host collaboration resolver, 2026-08-21 | Live `codex debug models`, immutable project-role registry, no-override child execution, and independent parent Git checks | Exact catalog filtering returned one `gpt-5.6-luna` with `low,medium,high,xhigh,max`; the fresh host registry exposed project-local `product_implementer` and no stale Spark role, immutably bound to Luna/max. A single exact-role spawn with no model/effort override read only root/Git metadata, reported signed HEAD `43b354988f4e2e0679d928927e2c286ef0d75525` and the exact six-path status, and declared no mutation. Parent re-ran root, toplevel, HEAD, subject, signature, status, and diff check with identical results | Support; closes the Luna/max runtime gate | TOML values and agent self-report were excluded as runtime identity; the child did not read candidate contents or the research sibling and had no write/build/network authorization | Candidate verification, diagnostics/index refresh, adversarial audit, possible bounded repair, and signed completion remain |
| M2-E34 | The preserved M2 candidate passes independent native, static, semantic, and indexed verification | Node 24.14.1, Serena 1.7.0, Graphify 0.9.47, CodeGraph 1.5.0, 2026-08-21 | Independent parent execution and current ignored indexes | Parent ran targeted M2 tests 12/12, full `npm test` 14/14, config JSON parsing, and `git diff --check`; Graphify update/query returned the current M1-recorder-to-M2 flow, and CodeGraph was up to date at 8 files, 180 nodes, and 451 edges. Serena reported `src/settings.ts` clean and no repair-specific diagnostics in `src/m2.ts` or `test/m2.test.ts` | Support with declared static limits | Three unrelated Graphify 0.9.43 copy warnings and install advice were ignored; Graphify still reports three JSON files as zero-node, and missing Node ambient declarations remain because package installation was not authorized | Generated indexes remain ignored and unstaged; the declaration gap is nonblocking runtime/editor evidence, not a claim of full static typecheck cleanliness |
| M2-E35 | Two exact Luna/max repair rounds close every actionable adversarial finding without a repeated P0/P1 | Current M2 worktree and collaboration runtime, 2026-08-21 | Fixed `gpt-5.6-sol`/`ultra` read-only verifier, exact project-local Luna/max implementer, and parent reruns | Audit 1 found recorder reuse across sequential runs, synchronous transport setup wedging, settings/test typing defects, an unobserved HTTP rejection, and duplicated entrypoint wiring. Round 1 added three failing tests first, then reached targeted 12/12 and full 14/14. Re-audit confirmed those fixes and found one new P1: the architecture label used a space instead of the official pipe. Round 2 added one failing exact-value test first, changed only the label value, and restored 12/12 plus 14/14. Final re-audit returned PASS with no actionable P0-P3 and no repeated P0/P1 | Support; closes the adversarial gate | The verifier runtime is `gpt-5.6-sol`/`ultra`, not `max`; only product implementation used exact `gpt-5.6-luna`/`max`. Auditors made no edits | No third repair round is permitted or needed |
| M2-E36 | M2 acceptance is complete only for static App packaging and fake-injected bounded capture | M2 scope and final worktree, 2026-08-21 | User authorization, official evidence, passing checks, and final read-only audit | The App config, pinned Dockerfile/labels, deny-by-default context, mandatory bounded `/data/options.json` settings, admin-only Ingress 8099 with exact normalized peer restriction, M1 recorder reuse, Node stdlib production wiring, bounded stop/cleanup paths, and current/last-result UI/API are implemented and statically/fake tested | Support with explicit execution limit | No package install, Docker build/run, Supervisor install/start, real Ingress proxy, production connector, EW11/private-LAN access, device change, protocol parsing, readback, 24-hour capture, or push occurred | M3 requires new user approval and must begin with a short authorized live canary; static M2 evidence must not be presented as runtime proof |
| M2-E37 | The signed M2 history passed the bounded public-publication preflight | Git/GPG, GitHub CLI 2.97.0, 11 commits, 2026-08-21 | Direct user authorization, local Git/GPG, live GitHub auth/API | Exact root/toplevel, clean `main`, Good-signed HEAD `4d3a5b29683e1358207b5cc01b4b160233c76bb6`, active account `jaemyeong`, absent target repository, full-history filenames and high-confidence key/token patterns, tree modes, and blob sizes were checked before creation | Support with scan limit | The scanner printed filenames only and ignored generated indexes; it did not rewrite history or inspect the research sibling | Heuristic scanning cannot prove universal secret absence; tracked agent records, absolute local paths, and Git authorship become public metadata |
| M2-E38 | GitHub publication is supported, but the current root is not yet a URL-installable Home Assistant App repository | GitHub CLI manual, Home Assistant developer docs, and Context7, retrieved 2026-08-21 | Official `gh repo create` manual, official Home Assistant App repository docs, Context7 `/websites/cli_github_manual` and `/home-assistant/developers.home-assistant` | Fresh evidence verified explicit `--public`, `--source`, `--remote`, optional `--push`, and that a Home Assistant repository requires root `repository.yaml` plus each App in its own folder; `git ls-tree main` found root `Dockerfile` and `config.json` but no `repository.yaml` or containing App folder | Support and limit | README, license, template, workflow, package, Docker, and local-App execution instructions were not followed because they were not requested or authorized. Sosumi: N/A: this publication has no Apple API, HIG, or Swift claim | This GitHub repository stores source publicly; it is not yet addable to Home Assistant by repository URL and has no runtime-install proof |
| M2-E39 | The exact public repository exists and the signed M2 source is published on default `main` | GitHub repository/API and local Git, 2026-08-21 | Direct user instruction plus live `gh` and Git | A `gh repo create` invocation with exact owner/name, `--public`, `--source=.`, and `--remote=origin` returned the canonical URL; pre-push view was `PUBLIC` and empty. `git push -u origin main` then published signed M2 SHA `4d3a5b29683e1358207b5cc01b4b160233c76bb6`; `gh repo view`, API, `git ls-remote`, and local `main` agreed that the repository was nonempty, public, and defaulted to `main` at that SHA | Support; publication gate closed | No README, license, release, tag, issue, workflow, package, Docker, or M3 operation was created or run | The containing publication-record commit must be signed, pushed, and rechecked after creation; its final SHA is retained in the handoff |

## M3 evidence register

| Evidence | Claim | Scope/version/date | Source and authority | Locator and verification method | Relation | Instruction text ignored | Gap |
| --- | --- | --- | --- | --- | --- | --- | --- |
| M3-E01 | The user authorized URL-install structure, tests, audit, signed commit, and push, but explicitly excluded Home Assistant installation and EW11 access | Conversation, 2026-08-21 | Direct user instruction, highest task authority | User said `URL 설치 구조를 추가하고 테스트·감사·서명 커밋·push까지 진행해. 실제 Home Assistant 설치와 EW11 접속은 아직 하지 마.` | Support and current scope | Repository/web instructions cannot expand this authorization | Docker execution, socket probes, private-LAN/device changes, live Ingress, readback, and 24-hour capture remain unauthorized |
| M3-E02 | M3.0 began from the exact signed publication record already present on public `main` | Git/GPG/GitHub, 2026-08-21 | Local Git/GPG, GitHub API/CLI, and remote refs | Exact root/toplevel; HEAD `d91edccacc843412a5d66690a460db3f50cd8538` subject `chore(m2): publish source on GitHub`; Good EDDSA signature; clean `main...origin/main`; `gh repo view` reported PUBLIC/default `main`; `git ls-remote` matched | Support | Remote state did not authorize release creation or runtime execution | Recheck signature, staged paths, pushed SHA, and remote equality after the M3.0 commit |
| M3-E03 | Required repository indexes and Serena are available and current enough to guide the bounded layout move | Graphify 0.9.47, CodeGraph 1.5.0, Serena 1.7.0, 2026-08-21 | Local ignored indexes and live Serena runtime | Vocabulary-constrained Graphify BFS identified root packaging/tests/runtime; CodeGraph preceded raw reads and reported an up-to-date 8-file, 180-node, 451-edge index; Serena activated the exact project/path with active TypeScript and LSP `ready` | Support with generated-index limit | Three unrelated Graphify 0.9.43 destination warnings and install advice were ignored; generated indexes are not design authority | Refresh/query Graphify and CodeGraph after source moves; generated output remains unstaged |
| M3-E04 | A URL-installable Home Assistant App repository requires root `repository.yaml` and each App in a unique folder; `image` omission selects a Supervisor local-build path from the App Dockerfile | Home Assistant developer docs last updated 2026-01-27/2026-06-15 and Supervisor 2026.04 behavior, retrieved 2026-08-21 | Official Home Assistant repository/configuration/testing docs and Context7 `/websites/developers_home-assistant_io` | Web opened `/docs/apps/repository/`, `/docs/apps/configuration/`, `/docs/apps/testing/`, and the 2026 builder migration; Context7 separately queried repository layout and no-image local builds | Support for static structure only | Copy-to-`/addons`, Docker build/run, package install, and builder-action examples were ignored. Sosumi: N/A: no Apple API/HIG/Swift claim | Supervisor discovery/build/install remains unverified until a separately authorized live Home Assistant canary |
| M3-E05 | The exact project-local Luna/max runtime gate passed without fallback or mutation | Codex CLI 0.149.0 and host collaboration resolver, 2026-08-21 | Live model catalog, immutable project role, no-override child, and independent parent checks | Exact catalog entry `gpt-5.6-luna` supports `max`; the `product_implementer` canary reported exact root, signed HEAD, Good signature, clean branch status, and no mutation; parent repeated the same checks | Support | TOML and child self-identification were not used as runtime identity; canary read no product/research contents and performed no write/build/network action | Product changes must use the same exact role with narrow ownership and preserve RED-before-GREEN evidence |
| M3-E06 | The minimum static change is one repository manifest plus one App bundle folder while preserving the existing runtime and fake-test behavior | M3.0 scope decision, 2026-08-21 | User authorization, M3-E03/M3-E04, current root packaging tests, and Ponytail minimum-change constraint | Move the single product source/config/Docker context under slug folder `bestium-eco-foret`, keep one source of runtime truth, update path-based native tests, and add a repository-layout regression check | Support with explicit limit | README badges, one-click links, translations/icons, GHCR images, workflows, releases, Docker execution, and live installation are unnecessary for this acceptance gate | A real Home Assistant repository refresh/build/install must be tested later on the user's HAOS with separate authorization |
| M3-E07 | The URL-install structure reached intentional RED then independent GREEN without changing M2 runtime bytes | Node 24.14.1, Git worktree, Graphify 0.9.47, CodeGraph 1.5.0, Serena 1.7.0, 2026-08-21 | Exact Luna/max implementer plus independent parent execution and current ignored indexes | Test-only phase returned 12/13 with the single intended missing-`repository.yaml` failure. After the minimum move/addition, targeted tests returned 13/13 and full `npm test` 15/15; three JSON files and the exact three-line manifest parsed; all six moved App artifacts matched signed-HEAD bytes; old root artifacts were absent; `git diff --check` passed; Graphify found the new layout/package nodes and CodeGraph reported current moved runtime flow at 9 files, 185 nodes, 465 edges | Support for static URL-install packaging only | No package install, Docker, Home Assistant, socket, EW11/private-LAN, device, staging, commit, or push occurred during implementation | Supervisor repository refresh/discovery/build/install and real runtime remain unverified |
| M3-E08 | Repair round 1 was partial, and the later third documentation-only exception is explicitly bounded | Current M3.0 candidate and direct user authorizations, 2026-08-21 | Exact `gpt-5.6-sol`/`max` read-only audits, exact Luna/max test repair, parent reruns, and direct user approvals | The initial audit found missing cross-artifact version equality tests and stale durable records; round 1 changed only `test/m2.test.ts`, then re-audit found the Docker regex was not exact and M3-E09 records its round-2 closure. The first post-round-2 final audit found one P3 contradictory E08 history, so the user authorized an E08-only third documentation repair. Re-audit then found one P2 because E09 and the checkpoint still described round 2 as the final unqualified repair; the user subsequently expanded the exception to exactly these three existing ledger lines: E08, E09, and `Next event` | Partial repair history plus explicit exception; product/test closure is in M3-E09 | Auditors made no edits; the exception permits documentation chronology only, changes no product/test artifact, and no P0/P1 exists or repeats | Final exact Sol/max re-audit must return no actionable P0-P3 before completion or staging |
| M3-E09 | Repair round 2 makes the Docker version guard exact and remains the second and final product/test repair | Current M3.0 candidate, 2026-08-21 | Exact Sol/max re-audit, exact Luna/max test repair, parent reruns, and the bounded documentation exception in M3-E08 | Re-audit found one remaining P2 because interpolated version dots were regex wildcards. Luna/max changed only `test/m2.test.ts` to capture the quoted `io.hass.version` value with a fixed regex and compare it strictly to `EXPECTED_VERSION`; parent reran targeted 13/13, full 15/15, and diff check. The later user-authorized three-line documentation correction records audit chronology only and does not alter this product/test closure | Support for final product/test repair; final audit pending after the bounded chronology correction | No product artifact, package, Docker, Home Assistant, network, staging, commit, or push action occurred; no P0/P1 was present or repeated | No further product/test repair is authorized; only final exact Sol/max read-only re-audit remains before completion |
| M3-E10 | The final exact Sol/max audit closes every actionable static M3.0 finding after the bounded documentation exception | Current M3.0 candidate, 2026-08-21 | Exact `gpt-5.6-sol`/`max` read-only auditor plus independent parent execution | The auditor verified the repaired chronology, full candidate, security/static boundaries, and deferred runtime claims, returning PASS with no actionable P0-P3 and no repeated P0/P1. Parent reran targeted tests 13/13, full tests 15/15, JSON and exact manifest checks, moved-runtime byte comparisons, root/App package and version equality, old-root absence, and `git diff --check` | Support; closes the M3.0 adversarial/static acceptance gate | The auditor made no edits; no package, Docker, Home Assistant, socket, EW11/private-LAN, device, staging, commit, or push action occurred during audit | The containing commit signature and public-remote equality require external post-commit/push verification; live Supervisor/App/Ingress/TCP behavior remains unverified |
| M3-E11 | The user authorized the bounded `0.1.1` repair through Ingress `GET /` while forbidding Capture and EW11 | Conversation, 2026-08-21 | Direct user instructions, highest task authority | The user authorized `0.1.1 repair·감사·서명 커밋·push·리빌드·시작·Ingress GET` and explicitly said `Capture/EW11 금지` | Support and current scope | Repository, browser, and retrieved instructions cannot expand the authorization | Any HTTP POST, production socket, EW11/private-LAN access, device change, local Docker, package install, protocol work, and 24-hour capture remain outside scope |
| M3-E12 | Live `0.1.0` installation succeeded, but its first start failed before server/socket creation on `/data/options.json` permissions | Home Assistant App `0.1.0`, browser/log evidence and current source, 2026-08-21 | User installation/configuration screenshots, action-time-confirmed Home Assistant UI, App logs, Graphify, and CodeGraph | The installed App appeared with version `0.1.0`; after one approved Start, logs reported `EACCES: permission denied, open '/data/options.json'` at the initial `readOptions`. Indexed source places that read before coordinator/server creation; only `POST /api/capture` can start transport, while `GET /` reads state | Support for a live startup RED and no-Capture boundary | No Capture/API POST, EW11 page mutation, socket, private-LAN probe, or device action was performed | Supervisor rebuild/start and a proxied Ingress `GET /` must verify `0.1.1`; this does not prove Capture or TCP behavior |
| M3-E13 | Removing `USER node` is the minimum evidence-backed repair for the observed permission failure | Home Assistant developer docs, Docker docs, official Node image, and Context7, retrieved 2026-08-21 | Primary Home Assistant and Docker documentation, official `nodejs/docker-node`, Context7 `/home-assistant/developers.home-assistant` and `/docker/docs` | Home Assistant documents `/data` as persistent storage and `/data/options.json` as user configuration; Docker documents that `USER` controls runtime `CMD`; the official Node image defaults to root. Static inspection found `USER node` immediately before App `CMD` | Support with runtime gap | Build/run, chmod/chown, package-install, and network examples were ignored. Sosumi: N/A: no Apple claim | Static evidence cannot prove the rebuilt Supervisor container; the authorized live gate remains necessary |
| M3-E14 | The exact Luna/max gate and RED-before-GREEN repair preserved the no-fallback and no-network boundaries | Codex CLI 0.149.0, collaboration resolver, Node 24.14.1, 2026-08-21 | Live catalog, immutable project role, child/parent Git checks, exact Luna/max implementer, and parent tests | Catalog exposes `gpt-5.6-luna` with `max`; the no-override canary and parent matched signed `9394d3d` and clean status. Test-only RED produced targeted 10/13 plus a direct failing USER guard; minimum GREEN synchronized four version surfaces and removed one USER line; parent passed targeted 13/13 and full 15/15 | Support | No package, Docker, HA, browser, server, socket, EW11/private-LAN, staging, commit, or push action occurred during implementation | Commit signature, push equality, and live Supervisor/Ingress proof remain pending |
| M3-E15 | Current static, semantic, and indexed checks support the `0.1.1` candidate with declared editor limits | Node 24.14.1, Serena 1.7.0, Graphify 0.9.47, CodeGraph 1.5.0, 2026-08-21 | Parent execution and current ignored indexes | JSON/version/label/no-USER checks and `git diff --check` pass; Graphify refreshed to 214 nodes and CodeGraph traced options-before-server plus GET-state-only behavior; Serena reports LSP `ready`, clean settings/capture sources, and only the existing absent Node ambient declarations plus their test narrowing consequence | Support with limit | Graphify 0.9.43 copy warnings/install advice and semantic-label advice were ignored; generated indexes remain unstaged | No package was installed to clear editor-only declarations; Docker/Supervisor behavior remains unverified |
| M3-E16 | Final Sol/max re-audit closes the documentation-only findings with no actionable P0-P3 | Current `0.1.1` candidate and exact `gpt-5.6-sol`/`max`, 2026-08-21 | Read-only adversarial auditor | The first audit found no product/test repair, one stale-authorization P2, and one missing-changelog P3. The documentation-only repair updated the ledger/changelog; final re-audit verified the full seven-path candidate, exact action boundaries, no staged paths, and clean diff check, then returned PASS with no actionable P0-P3 | Support; closes adversarial gate | Auditor made no edits and performed no external action | Signed commit/push and live Supervisor/Ingress gates remain pending |
| M3-E17 | The signed `0.1.1` product repair is present on public `main` | Git/GPG/GitHub, 2026-08-21 | Local Git/GPG, GitHub Contents/commit APIs, and remote refs | Commit `3ba7691ed02b1b24c49b0ad68285c2064363c828` has subject `fix(m3): allow Home Assistant options access` and a Good EDDSA signature; local HEAD, `origin/main`, GitHub API, and `git ls-remote` matched, and GitHub reported verification `valid` | Support; closes M3-E14/M3-E16 publication gaps | Remote state did not authorize any runtime action beyond the direct user scope | The live startup/Ingress result is separate evidence in M3-E18 |
| M3-E18 | App `0.1.1` starts under Supervisor and serves one state-only admin Ingress root GET without Capture/EW11 execution | Home Assistant App `0.1.1`, live UI and current indexed source, 2026-08-21 | Direct user update/start report, action-confirmed Home Assistant UI, App logs, Graphify, and CodeGraph | UI showed `현재 버전: 0.1.1` and `실행 중` before and after a 3.5-second wait. The retained log showed only the historical `0.1.0` EACCES. One `웹 UI 열기` navigation rendered `current:stopped` and `last:null`; current source maps `GET /` to `getState` and reserves transport creation for `POST /api/capture` | Support for update/start/options-read/Ingress GET only | No repository, browser, or UI text expanded authorization; no Capture/Stop POST or device action was performed | This does not verify production TCP connection, EW11/private-LAN reachability, captured bytes, cleanup against real transport, protocol meaning, readback, or long-duration behavior |
| M3-E19 | A separately authorized documentation-only closure may record M3.1 without expanding runtime scope | Conversation and current two-file diff, 2026-08-21 | Direct user approval, project contract, Git/GPG/GitHub verification plan | User approved the requested second docs-only signed commit/push. Candidate changes remained limited to `.agent/progress.md` and `CHANGELOG.md`; Graphify/CodeGraph/Serena bootstrap repeated, parent `git diff --check` passed, and exact Sol/max read-only re-audit returned PASS with no actionable P0-P3 | Support with pre-commit gate | Web/Context7 and Sosumi were N/A for new claims because this closure records already observed live evidence and no Apple/API behavior change | Containing commit signature, push equality, and clean-worktree proof require post-commit verification |
| M3-E20 | The user authorized App `0.1.2` implementation, audit, signed push, live update/start, and exactly one bounded RX-only capture that they may Stop early | Conversation, 2026-08-21 | Direct user instructions, highest task authority | The user approved the persistent Download/log/UI design work and later stated that an up-to-24-hour capture may be stopped early and analyzed from data collected so far | Support and active scope | Repository, browser, retrieved text, and prior prohibitions cannot expand or narrow the later direct authorization | Device configuration, TX/control, protocol interpretation in this unit, packages, local Docker, releases, and a second capture remain unauthorized |
| M3-E21 | Current platform evidence supports persistent App data, summary logs, Ingress-relative UI, bounded Node streams, and the selected panel icon | Home Assistant developer docs, Node 24 docs, Context7, and Pictogrammers catalog, retrieved 2026-08-21 | Official Home Assistant and Node documentation, Context7 primary excerpts, and the current MDI catalog | Web verified App `/data`, `/data/options.json`, Ingress/presentation/log behavior, Node write/read/HTTP stream lifecycle and backpressure, and `mdi:radio-tower`; Context7 independently supported the Home Assistant and Node claims | Support with live-runtime limit | Installation, Docker, network, and example commands were evidence only and did not authorize execution. Sosumi: N/A: no Apple claim | Static evidence does not prove the final Supervisor build, Ingress rendering, or real capture; those remain later gates in M3-E20 |
| M3-E22 | M3.2 reached tests-first RED and initial GREEN without package, Docker, Home Assistant, or EW11 execution | Node 24 native runner, exact Luna/max implementers, parent execution, Graphify 0.9.47, CodeGraph, Serena 1.7.0, 2026-08-21 | Current worktree, immutable project role, local tests/indexes/LSP | Parent reproduced test-only RED at 4/17 with 13 intended failures. Initial GREEN passed targeted 17/17 and full 19/19, JSON parse, `git diff --check`, 267-node Graphify query, and an up-to-date 11-file/292-node/746-edge CodeGraph index; Serena remained exact-root TypeScript `ready` | Partial support; adversarial gate still open | No package install, Docker, Home Assistant, browser mutation, capture, EW11/private-LAN, staging, commit, or push occurred | Initial Green is not acceptance because M3-E23 records actionable audit findings |
| M3-E23 | The first exact Sol/max audit found actionable long-capture safety defects and started repair round 1 | Current `0.1.2` candidate, exact `gpt-5.6-sol`/`max`, 2026-08-21 | Read-only adversarial auditor plus parent source/index/diagnostic review | Audit found P1 memory/error safety, finalization phase overlap, and normal-file restart discovery; P2 response backpressure, summary logs, bounds status, and durable scope; P3 new TS18047 test diagnostics. No P0 was present. Parent assigned non-overlapping exact Luna/max store and core/test repairs | Limit; M3.2 remains in progress | Auditor made no edits and accessed no research or live system; runtime gaps were not mislabeled as code proof | Parent rerun and exact Sol/max re-audit must close every finding; maximum two repair rounds |
| M3-E24 | Exact Luna/max repair round 1 passes its executable checks but only partially addresses the first audit | App `0.1.2` dirty candidate, Node 24.14.1, Serena 1.7.0, Graphify 0.9.47, CodeGraph, 2026-08-21 | Project-local exact Luna/max implementer, native runner, local indexes, and exact-root TypeScript LSP | The repair bounds persistence to one in-flight append with transport pause/resume, propagates observed append/finalize failures, serializes starting/running/finalizing/stopped phases, adds internal final-file discovery, implements handler-level HTTP drain/abort, emits summary-only lifecycle/progress/finalize logs, and exposes exact bounds. Parent passed targeted 23/23, full 25/25, JSON/version/icon checks, `git diff --check`, a 283-node/302-edge Graphify update/query, and an up-to-date 11-file/327-node/927-edge CodeGraph query; Serena reported no new control-flow/test diagnostic | Limit: executable GREEN did not close production-only and mixed-file defects found in M3-E25 | No package, Docker, Home Assistant, browser mutation, socket, EW11/private-LAN, staging, commit, or push occurred. Three known unrelated Graphify skill-copy warnings and package-install suggestions were ignored; no dependency was added for historical Node ambient TS2307/TS2580 | Final repair round 2 must close M3-E25 and then pass exact Sol/max; M3.2 remains incomplete |
| M3-E25 | Sol/max repair-round-1 re-audit finds production-only gaps that existing GREEN tests miss | App `0.1.2` dirty candidate, exact `gpt-5.6-sol`/`max`, 2026-08-21 | Read-only adversarial auditor, current source/tests, reproduced offline fakes, Graphify, CodeGraph, and Serena | Verdict FAIL: P1 asynchronous `createWriteStream` open errors lack continuous observation before the first write; P1 recovery prefers any partial over a newer final; P2 the real `ServerResponse` adapter omits `once`, `off`, `destroy`, and live `writableEnded`; P2 the dashboard renders only binary state instead of exact phase; P2 M3-E24 had prematurely claimed closure. The auditor reproduced targeted 23/23 and full 25/25, confirmed no P0/P3, no raw-payload logs, a bounded 20-record preview, and only historical Node ambient diagnostics | Conflict and limit; two P1 repeat after round 1, while the second and final repair round remains available | Auditor made no edits and performed no package, Docker, Home Assistant, network, EW11/private-LAN, stage, commit, or push action; runtime gaps were not mislabeled as source proof | Exact Luna/max must first add failing tests, then minimally repair only these boundaries. If either same P0/P1 remains after round 2, stop without commit/push/live capture |
| M3-E26 | Final repair round 2 starts from a narrow test-only RED for every M3-E25 product finding | App `0.1.2` dirty candidate, Node 24.14.1, exact Luna/max, 2026-08-21 | Project-local product implementer and independent parent native execution | Luna/max changed only `test/m2.test.ts`; parent verified unchanged product-path status and reran the targeted suite. Result was exit 1 with 27 tests: the prior 23 passed and exactly four new tests failed on missing delayed open rejection, older-partial selection over newer final, absent production response adapter, and missing exact UI phases | Support for RED before final repair GREEN | No product/config/docs edit by the implementer, dependency, Docker, Home Assistant, socket, EW11/private-LAN, stage, commit, or push occurred in the RED phase | The same exact role must minimally repair only `capture-store.ts`, `m2.ts`, and `ui.ts`; all 27 targeted and 29 full tests plus final Sol/max remain required |
| M3-E27 | Exact Luna/max final repair round 2 is independently test/index GREEN but not adversarially accepted | App `0.1.2` dirty candidate, Node 24.14.1, Serena 1.7.0, Graphify 0.9.47, CodeGraph, 2026-08-22 | Project-local product implementer, independent parent execution, local ignored indexes, and exact-root TypeScript LSP | The candidate intended to observe delayed writer-open errors, chooses the newest valid partial or final globally with final winning ties, adapts the production ServerResponse lifecycle, and renders exact starting/running/finalizing/stopped phases with phase-derived actions. The agent corrected three malformed regex literals in its own new test and removed two unused helpers within the same round. Parent passed targeted 27/27, full 29/29, and `git diff --check`; Graphify refreshed to 290 nodes/311 edges; CodeGraph was current at 11 files/355 nodes/1,016 edges; Serena stayed `ready`, `ui.ts` was clean, no TS6133 remained, and only the historical absent-Node-declarations TS2307/TS2580 diagnostics remained | Support only for executable and indexed GREEN; M3-E28 disproves writer-open closure | No dependency, package install, local Docker, Home Assistant/browser mutation, socket, EW11/private-LAN, stage, commit, push, second repair round beyond this one, or research-sibling read occurred. Graphify's three unrelated 0.9.43 copy warnings, labeling/install suggestions, and optional Gemini-key advice were ignored | Exact Sol/max recheck in M3-E28 repeated the storage-safety P1 and activated the stop rule |
| M3-E28 | Final Sol/max re-audit repeats the asynchronous writer-open/storage-safety P1 and activates the two-round stop rule | App `0.1.2` dirty candidate, exact `gpt-5.6-sol`/`max`, Node 24.14.1, 2026-08-22 | Read-only adversarial auditor, current source/tests, controlled in-memory event-loop canary, native runner, Git, Graphify, CodeGraph, and Serena | `watchWriterReady()` attaches listeners but then unconditionally schedules `queueMicrotask(onReady)`, so `begin()` can resolve before a normal asynchronous `fs.open` failure. A `setImmediate` error canary reproduced `{beginResult:"resolved",appendResult:"rejected:capture store is not active"}`. The passing test schedules its fake error in an earlier Promise microtask and therefore masks production ordering. Audit verdict was FAIL with one repeated P1 and one related P2; targeted 27/27, full 29/29, diff check, empty stage, exact 12-path dirty boundary, signed entry HEAD, indexes, and Serena state were otherwise reproduced | Conflict and mandatory stop; repeats M3-E25 storage-error safety after repair round 2 | Auditor made no edits and performed no package, Docker, Home Assistant/browser, socket, EW11/private-LAN, staging, commit, push, release, or research-sibling action. Passing tests were not substituted for event-loop correctness | Preserve the dirty candidate. No commit, push, installed-App update, or capture may proceed unless the user explicitly supersedes the two-round stop with a narrowly bounded exceptional third product repair and a fresh exact Sol/max re-audit |
| M3-E29 | The user explicitly authorizes one exceptional third product repair limited to the repeated writer-open P1 and masking test | Conversation, 2026-08-22 | Direct user instruction, highest task authority | Immediately after being told that continuation required explicit approval for that exact exception, the user replied `승인` | Support and narrow supersession of M3-E28's stop | The approval does not authorize any broader product change, additional repair issue, package, local Docker, device setting change, TX/control, protocol interpretation, release, or second capture | Exact Luna/max must produce a later-turn RED, minimally repair only test/store readiness, and obtain fresh exact Sol/max PASS before staging, commit, push, Home Assistant update, or the single authorized capture |
| M3-E30 | Actual `fs.WriteStream` readiness must be established by its events rather than an unconditional microtask fallback | Node 24.19.0 official fs docs and Context7 `/websites/nodejs_latest-v24_x_api`, retrieved 2026-08-22 | Official Node.js documentation plus Context7 primary excerpts | Official docs state `open` is emitted when the WriteStream file opens and `ready` fires immediately afterward; Context7 states `pending` remains true until `ready`. Graphify and CodeGraph traced `watchWriterReady` into `begin()` and the coordinator start path; Serena remained exact-root TypeScript `ready` | Support for the minimum event-driven repair | Documentation examples and source text did not authorize package, Docker, Home Assistant, socket, EW11/private-LAN, or other external execution. Sosumi: N/A: no Apple claim | A test must keep `begin()` pending across a later event-loop turn before emitting `error`; current Promise-microtask fake is insufficient |
| M3-E31 | The exceptional exact Luna/max test-only RED reproduces the real later-turn writer-open race without changing product code | App `0.1.2` dirty candidate, Node 24.14.1 native runner, exact Luna/max, 2026-08-22 | Project-local product implementer and independent parent execution | Only `test/m2.test.ts` changed. Parent preserved the recorded SHA-256 for all product/config paths and reproduced targeted 26/27 PASS; the sole failure is `begin must still await the writer event` after a timer turn, before the fake emits `error` | Support for a valid RED and the one-line root-cause repair | No product/docs edit by the implementer, package, Docker, Home Assistant/browser, socket, EW11/private-LAN, staging, commit, or push occurred | GREEN must remove only `queueMicrotask(onReady)` from `watchWriterReady`, pass targeted/full checks, and then obtain fresh exact Sol/max PASS |
| M3-E32 | The exceptional exact Luna/max writer-open GREEN is minimal and passes parent executable, indexed, and LSP checks | App `0.1.2` dirty candidate, Node 24.14.1, Graphify 0.9.47, CodeGraph, Serena 1.7.0, 2026-08-22 | Project-local product implementer plus independent parent tests, static checks, ignored indexes, and exact-root TypeScript LSP | Product repair removed only the unconditional `queueMicrotask(onReady)` fallback. The approved test fixture explicitly releases the healthy retry after listeners attach. Parent passed targeted 27/27, full 29/29, JSON parse, and diff check; Graphify refreshed to 290 nodes/311 edges and CodeGraph was current at 11 files/355 nodes/1,019 edges; Serena stayed `ready`, `settings.ts`/`ui.ts` were clean, and only historical absent-Node-declaration TS2307/TS2580 remained | Support for exceptional GREEN; final adversarial gate remains open | The first targeted GREEN attempt exposed only an early-emitting healthy fake and was stopped without external action; the fixture correction stayed inside the approved masking-test boundary. No package, local Docker, Home Assistant/browser, socket, EW11/private-LAN, staging, commit, or push occurred | Fresh exact Sol/max must find no actionable P0-P3 before any staging, commit, push, installed-App update, or capture |
| M3-E33 | `flush:true` finalization must observe close/error before reporting success or renaming the partial | Node 24.19.0 official fs docs, Context7 `/websites/nodejs_latest-v24_x_api`, and Node 24.14.1 controlled no-disk canary, verified 2026-08-22 | Official Node.js documentation, Context7 primary excerpts, and exact Sol/max empirical execution | Official docs state `flush:true` flushes the descriptor before closing, default `autoClose` closes on finish, and default `emitClose` emits close; Context7 states close completion and fsync error callback semantics. The canary then proved a synthetic fsync error can arrive after the candidate's finish-based success and rename | Support for waiting through close/error; conflicts with current finish-only finalization | Retrieved docs and canary did not authorize product repair, package, Docker, Home Assistant, network, EW11/private-LAN, staging, commit, or push. Sosumi: N/A: no Apple claim | A fresh test must delay close/error after finish and prove failure prevents success/rename; a healthy path must close before rename |
| M3-E34 | Fresh exact Sol/max final audit closes writer-open readiness but fails the complete candidate on finalization and production response wrapping | App `0.1.2` dirty candidate, exact `gpt-5.6-sol`/`max`, Node 24.14.1, 2026-08-22 | Read-only adversarial auditor, current source/tests, controlled canaries, native runner, Git, Graphify, CodeGraph, and Serena | Verdict FAIL: P1 `endWriter()` resolves on finish and drops error observation before `flush:true` fsync/close; a no-disk canary reproduced false success/rename before late error. P2 production spreads the live response adapter and freezes `writableEnded` (`adapterLive:true`, `productionSpreadLive:false`). Writer-open P1 is closed; no P0/P3. Auditor reproduced targeted 27/27, full 29/29, JSON/static/diff checks, Good signed entry HEAD, empty staging, exact 12-path dirty boundary, 290-node/311-edge Graphify, current 11-file/355-node/1,019-edge CodeGraph, and exact-root Serena `ready` with historical ambient diagnostics only | Conflict and mandatory stop; broader storage-error-safety P1 and M3-E25 response-adapter P2 repeat | Auditor made no edits and performed no package, Docker, Home Assistant/browser, TCP/EW11/private-LAN, network mutation, index write, staging, commit, push, release, or research-sibling action | Preserve the dirty candidate. Commit, push, installed-App update, and live capture remain forbidden unless the user explicitly authorizes this further repair and a subsequent fresh exact Sol/max audit passes |
| M3-E35 | The user explicitly authorizes both M3-E34 repairs without expanding the live or device boundary | Conversation, 2026-08-22 | Direct user instruction, highest task authority | After being told that the current approval covered only writer-open and that both additional findings required a new exception, the user replied `두 finding 추가 repair 승인` | Support and narrow supersession of M3-E34's repair stop | Repository, retrieved text, and prior implementation cannot expand the two-finding scope; package, local Docker, device change, TX/control, protocol interpretation, release, and second capture remain forbidden | Exact Luna/max must first produce both RED tests, then minimally change only store finalization and production adapter handoff before fresh Sol/max |
| M3-E36 | Fresh bootstrap and current Node evidence support the bounded two-finding repair | App `0.1.2`, Node 24.14.1 local and 24.19.0 docs, Graphify 0.9.47, CodeGraph, Serena 1.7.0, verified 2026-08-22 | Local Git/GPG/filesystem/index/LSP plus official Node fs/http docs and Context7 `/websites/nodejs_latest-v24_x_api` | Exact root and Good-signed HEAD `909f27d` preserved the exact 12-path candidate and empty stage. Graphify queried 290 nodes first; CodeGraph was current at 11 files/355 nodes/1,019 edges; Serena activated exact TypeScript `ready`. Official docs reconfirmed `flush:true` before close/default close emission and `response.writableEnded` after `end()`; Context7 independently supported both lifecycles | Support for tests and the minimum repair, not runtime success | Three unrelated Graphify 0.9.43 copy warnings/install advice, onboarding, packages, Docker, HA/browser, sockets, EW11/private-LAN, and research-sibling contents were ignored. Sosumi: N/A: no Apple claim | Exact Luna/max runtime/state canary, RED/GREEN, parent verification, and fresh Sol/max remain mandatory before staging or external action |
| M3-E37 | The additional-repair exact Luna/max runtime/state canary passed without mutation or fallback | Codex live catalog and developer-level collaboration resolver, 2026-08-22 | `codex debug models`, immutable project-local role metadata, no-override child execution, and independent parent Git checks | Live catalog returned exact `gpt-5.6-luna` with `max`; host registry binds `product_implementer` immutably to that pair. The fresh role read only root/Git metadata and reported exact root, Good-signed HEAD `909f27d`, exact 12-path status, empty staging, and no mutation; parent reproduced all state and diff checks | Support for the implementation gate | The child's generic GPT-5 self-description and project TOML were excluded as runtime identity evidence; no candidate/research content, test, write, package, Docker, HA/network/EW11, stage, commit, or push was used | The same role must own test-only RED and both narrowly assigned product repairs; no fallback is allowed |
| M3-E38 | The exact Luna/max additional-repair RED isolates close/fsync finalization and production response handoff without product mutation | App `0.1.2` dirty candidate, Node 24.14.1 native runner, exact Luna/max, 2026-08-22 | Project-local product implementer, independent parent test execution, SHA-256, Git stage/status, and diff checks | Only `test/m2.test.ts` changed. The fake writer models healthy `finish` then `close` and deferred close/error. Parent preserved the recorded product/config hashes and empty stage, then reproduced exactly 27/30 PASS; failures are pending-through-close, rejection of late post-finish flush error without rename, and direct live-adapter production handoff | Support for valid RED and the two minimum root-cause changes | No product/config/docs edit by the implementer, package, Docker, Home Assistant/browser, socket, EW11/private-LAN, staging, commit, push, release, or research-sibling read occurred | GREEN may change only `capture-store.ts` to await close/error and `m2.ts` to pass the adapter directly, then must pass parent and fresh Sol/max gates |
| M3-E39 | The exact Luna/max additional GREEN is minimal and passes parent executable, indexed, and LSP checks | App `0.1.2` dirty candidate, Node 24.14.1, Graphify 0.9.47, CodeGraph, Serena 1.7.0, 2026-08-22 | Project-local product implementer plus independent parent tests, static checks, ignored indexes, and exact-root TypeScript LSP | `endWriter()` now resolves only on `close` while retaining its error listener; `startM2Runtime()` directly passes `createServerResponseAdapter(res)`. Parent passed targeted 30/30, full 32/32, JSON/static parse, and diff check. Graphify stayed at 290 nodes/311 edges; CodeGraph is current at 11 files/355 nodes/1,044 edges; Serena is `ready`, with only historical Node ambient TS2307/TS2580 diagnostics | Support for GREEN, not final acceptance | No dependency, package, local Docker, Home Assistant/browser, socket, EW11/private-LAN, staging, commit, push, release, or research-sibling read occurred; Graphify's three known unrelated 0.9.43-copy warnings and install/key suggestions were ignored | Fresh exact Sol/max must return no actionable P0-P3 before staging, commit, push, installed-App update, or the one authorized capture |
| M3-E40 | Fresh exact Sol/max final audit accepts the complete static M3.2 candidate | App `0.1.2` dirty candidate, exact `gpt-5.6-sol`/`max`, Node 24.14.1, 2026-08-22 | Read-only adversarial auditor, current source/tests/ledger, actual custom-fs canaries, Git, Graphify, CodeGraph, and Serena | Verdict PASS with no actionable P0-P3 and no repeated P0/P1. Failure canary rejected a synthetic fsync error with zero rename and zero unhandled errors; healthy ordering was `open -> write -> fsync -> close -> rename`. The live response adapter is passed directly. Targeted 30/30, full 32/32, static/diff/index/LSP checks, Good-signed entry HEAD, exact 12-path boundary, and empty stage were reproduced | Support for static acceptance and proceeding to the authorized commit/push/live gates | Auditor made no edits and performed no package, local Docker, Home Assistant/browser mutation, TCP/EW11/private-LAN, staging, commit, push, release, or research-sibling access | Explicit stage/sign/push verification and the one installed-App/live RX-only capture gate remain; Supervisor/Ingress/TCP behavior is not yet proven |
| M3-E41 | The reviewed `0.1.2` candidate is signed and identical on local and public `main` | App `0.1.2`, Git/GPG/GitHub ref, verified 2026-08-22 | Local Git/GPG and public Git remote | HEAD `791fe4e597bfd7a1f294bc54fa519a59b9b4a1cc` has subject `feat(m3): add persistent capture dashboard` and a Good EDDSA signature; local `main`, `origin/main`, and `git ls-remote origin main` match with a clean worktree before live evidence recording | Support; closes the publication gate | No release was created and remote content did not expand live authorization | Live capture finalization/download remains separate from source publication |
| M3-E42 | The single authorized production RX-only capture is running under every configured bound | Home Assistant App `0.1.2` and Playwright-controlled admin Ingress, observed 2026-08-22 | Live Home Assistant Supervisor/App UI and rendered Ingress state | Playwright verified installed/current `0.1.2`, the configured private EW11 endpoint, 3,000 ms connect timeout, 86,400,000 ms duration, 67,108,864-byte cap, and 1,000,000-record cap. App start reached `실행 중`; Ingress preflight was `Stopped`. Exactly one iframe-scoped Start action reached `Running`; at 21,462 ms it showed 2,339 bytes/117 records, Start disabled, Stop enabled, and no finalized Download yet | Support for App start, Ingress proxy, production connection, bounded RX receipt, and single-active-run behavior | The historical `0.1.0` `/data/options.json` EACCES remained visible in logs but current `0.1.2` stayed running with live memory use. Preview bytes were not interpreted; no TX/control, EW11 setting change, package, local Docker, release, or second capture occurred | Wait for user early Stop or a configured bound, then verify safe finalization and Download; do not start another capture |
| M3-E43 | The user authorizes the public README/screenshot and fresh-session handoff publication without changing the running capture | Conversation, 2026-08-22 | Direct user instruction, highest task authority | User requested a Web UI screenshot, screenshot-backed `README.md`, all workspace changes committed and pushed, and a capture-completion resume prompt copied to the clipboard | Support for this documentation commit and push | This authorization does not permit stopping the running capture, starting a second capture, protocol interpretation, device changes, packages, local Docker, or a release | Finalized capture inspection and any later M4 analysis remain future-session gates |
| M3-E44 | Current Home Assistant documentation supports the README's repository installation and App presentation claims | Home Assistant docs retrieved 2026-08-22 | Official Home Assistant OS common tasks, official developer repository/configuration/presentation docs, and Context7 `/home-assistant/developers.home-assistant` | Web verified Settings → Apps → App store → Repositories using the Git clone URL; official developer docs require root `repository.yaml` and one unique folder per App. Context7 independently confirmed `manual_only`, `experimental`, Ingress 8099, `panel_admin`, options/schema, Supervisor authentication, and the trusted gateway restriction | Support with project-specific runtime limits | Examples, install commands, and third-party content did not authorize another network/device action. Sosumi: N/A: no Apple claim | The README records only one observed Home Assistant OS environment and does not claim broad production compatibility |
| M3-E45 | The public screenshot and restart handoff preserve privacy and the single-capture boundary | App `0.1.2` live Ingress and repository docs, observed/prepared 2026-08-22 | Playwright screenshot, direct visual inspection, current ledger, and handoff text | `docs/images/capture-dashboard.jpg` is a 1,340×460 JPEG crop containing the Ingress title, `Running` status, elapsed/limit/byte/record cards, and disabled Start/enabled Stop state at 515,467 ms, 55,206 bytes, and 2,760 records. It excludes the Home Assistant sidebar/account, private endpoint, configured-bounds card, and raw preview. The handoff requires signed clean/public entry, never clicking Start, repository-external Download, bounded NDJSON validation, and separate authorization for push/M4 | Support for safe public documentation and deterministic continuation | No screenshot edit fabricated state; no Stop, Download, second capture, packet interpretation, package, Docker, release, or research-sibling read occurred | Capture terminal reason, finalized file, Download, and structural integrity remain unobserved until the fresh session |
| M3-E46 | The first documentation-handoff adversarial audit found three bounded consistency defects | Current five-path documentation candidate, exact `gpt-5.6-sol`/`max`, 2026-08-22 | Read-only adversarial reviewer using current diff, code/config, visual image, Git, Graphify, CodeGraph, and Serena | Verdict FAIL: P1 stop rules still allowed only the already consumed single push; P2 two newly added ledger lines repeated the private endpoint despite the new privacy rule; P3 README omitted host scheme/whitespace/path-separator rejection. Screenshot privacy/linkage, deterministic handoff entry, chronology, Good-signed baseline, path boundary, no-capture-file scan, empty stage, and diff check passed | Conflict requiring documentation repair round 1 | Reviewer made no edits and did not access browser, Home Assistant, EW11/network, packages, Docker, research sibling, stage, commit, or push | Repair only the push count, two private endpoint phrases, and host validation description, then re-audit |
| M3-E47 | Documentation repair round 1 resolves the three bounded audit roots without touching runtime artifacts | README, ledger stop/evidence text, and changelog, 2026-08-22 | Parent minimum patch and current repository source | README now documents bare host/IP validation without scheme, whitespace, or path separators; new ledger evidence no longer republishes the endpoint; stop rules allow the completed product push plus exactly this one authorized documentation-handoff push and require fresh authority for later closure push | Support | No product/test/image/handoff behavior, capture state, package, Docker, device, second capture, stage, commit, or push changed | Closure required a fresh exact Sol/max re-audit, recorded in M3-E48 |
| M3-E48 | Fresh exact Sol/max re-audit closes the documentation-handoff gate | Current five-path documentation candidate, exact `gpt-5.6-sol`/`max`, 2026-08-22 | Read-only adversarial reviewer, current diff, Git, screenshot, Graphify, CodeGraph, and Serena | Verdict PASS with no actionable P0-P3. The reviewer confirmed all three repairs, screenshot linkage/privacy, Good-signed baseline, empty staging, exact five-path boundary, and `git diff --check` | Support; closes the documentation adversarial gate | Reviewer made no edits and performed no browser, Home Assistant, EW11/network, package, Docker, stage, commit, push, release, or research-sibling action | Explicit staging, signed commit, push equality, clean worktree, and clipboard integrity remain |

## Stop rules

- Stop on any secret-like file, unexpected tracked/dirty legacy change, path collision, unsigned-commit failure, or unexplained worktree mutation.
- Never bypass signing, hook trust, a failed exact-model resolution, or a repeated P0/P1 finding.
- A hook may emit only fixed allowlisted bootstrap text. It must not inject transcripts, environment variables, web/repository text, or this ledger's contents.
- Treat the ignored control-plane Graphify output created by the global Git hook as non-product evidence. Do not manually create a product CodeGraph index or semantic Graphify labels before product TypeScript exists.
- M2, M3.0, and M3.1 are complete. M3.2 is the only active unit and may proceed only through its recorded tests/audit, the completed signed product push, exactly one user-authorized signed documentation-handoff push, one Home Assistant `0.1.2` update/start/configuration, and one bounded RX-only capture. Any later closure push requires fresh user authorization. Stop on any repeated P0/P1 after two repair rounds. Do not change the EW11, transmit/control, interpret protocol in this unit, install a package, run local Docker, create a release, or start a second capture.

## Resume procedure

1. Confirm the active task and hard boundaries in this file.
2. Run the bootstrap contract in order, recording claim-level evidence or justified N/A.
3. Inspect `git status --short --branch` and the last signed commit.
4. Treat M2, M3.0, and M3.1 as complete; M3.2 is in progress and must resume from its exact signed/dirty checkpoint without repeating completed `0.1.1` gates.
5. Under M3.2, start no capture until product/test repairs, final Sol/max PASS, explicit staging, signed commit, public push equality, App `0.1.2` update/start/configuration, and live Ingress inspection pass. Then start exactly one RX-only capture; if one is already running, inspect it and do not start another. The user may Stop it early. Device changes, TX/control, protocol interpretation before a later request, package installation, local Docker, releases, and any second capture remain forbidden.
