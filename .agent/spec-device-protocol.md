# 기기별 프로토콜 명세 · 0.2.7 계획

작성: 2026-08-25 (Asia/Seoul)

근거 자료 두 가지를 대조해 만들었습니다.

- **캡처**: `capture-1787635354221.ndjson` — 306.8초, 32,429바이트, 프레임 1,957개.
  XOR 불일치 0개, 해석 실패 37바이트(0.11%). 사용자가 캡처 도중 월패드를 직접
  조작해 주신 덕분에 **실제 명령과 그 응답이 함께** 들어 있습니다.
- **레거시**: 연구용 사본의 Python 구현. `AGENTS.md` 7항에 따라 소스를 옮기지 않고
  동작 명세만 추출했습니다.

각 항목에는 **캡처가 증명한 것**과 **레거시만 주장하는 것**을 구분해 표시했습니다.
코드의 evidence 등급은 이 구분을 그대로 따라야 합니다.

## 0. 한 문장 요약

조명이 동작하고 난방·승강기가 동작하지 않는 이유는 전송 경로나 게이트가 아니라
**프레임 자체가 틀렸기 때문**입니다. 조명 프레임은 우연히 맞았고, 난방 프레임은
사람이 조명 패턴에서 유추해 만든 것이며 버스 어디에도 존재하지 않습니다.

## 1. 공통 프레임 구조 (캡처 검증)

```
F7 <길이> 01 <기기> <종류> <하위명령> <주소> <값> 00 <XOR> EE
```

- `<길이>`는 `F7`을 포함한 전체 바이트 수입니다. 설정 명령은 예외 없이 `0x0B`(11)입니다.
- `<종류>`: `01` 조회 · `02` 설정 명령 · `04` 상태 응답.
- `<XOR>`은 마지막 두 바이트를 제외한 전 바이트의 XOR입니다. 캡처 1,957개 전부 일치합니다.

## 2. 기기별 명세

### 2.1 난방 `0x18` — **캡처 검증 완료**

캡처에 설정 명령 10개와 그 응답 10개가 쌍으로 들어 있습니다.

| 동작 | 프레임 | 캡처 근거 |
| --- | --- | --- |
| 조회 | `F7 0B 01 18 01 46 10 00 00 B2 EE` | 138회 관측 |
| On | `F7 0B 01 18 02 46 1<zone> 01 00 <XOR> EE` | zone 1·2·3·4 각 1회 |
| Off | `F7 0B 01 18 02 46 1<zone> 04 00 <XOR> EE` | zone 1·2·3·4 각 1회 |
| 목표 온도 | `F7 0B 01 18 02 45 1<zone> <섭씨> 00 <XOR> EE` | zone 1, 21°C·23°C |
| **전체 끄기** | **`F7 0B 01 18 02 46 10 04 00 B5 EE`** | **`capture-1788009200284`에서 2회 관측** |

> **정정.** 이 문서는 아래 6.1절 11항에서 "레거시에도 난방 일괄 명령은 없다"고
> 적었습니다. **그 서술은 반증되었습니다.** 주소 `0x10`은 존이 아니라 그룹이며,
> 월패드 자신이 단일 프레임으로 네 존을 끕니다. `+88.8s`에 `01 01 01 01`이던 상태가
> `+92.2s`의 그 한 프레임 뒤 `+92.9s`에 `04 04 04 04`가 되는 것으로 확인했습니다.
> 자세한 근거는 `.agent/analysis-ew11-timing-and-group-frames.md` §4.2에 있습니다.
> 그룹 **켜기** `F7 0B 01 18 02 46 10 01 00 B0 EE`는 캡처와 레거시 어디에도 관측된 적이
> 없지만, 2026-08-30에 우리가 보내 네 존이 한 번에 켜지는 것을 폴링으로 확인했습니다
> (`04040404` → `01010101`). 관측이 아니라 우리 송신이 근거라는 점에서 그룹 끄기와
> 등급이 다릅니다.

관측된 여덟 개의 On/Off 프레임과 두 개의 온도 프레임을 이 규칙으로 생성해
**열 개 모두 바이트 단위로 재현**했습니다. 조회 프레임도 재현됩니다.

하위명령이 두 개로 갈립니다. `0x46`이 On/Off, `0x45`가 목표 온도입니다.
주소는 `0x10 + zone`이고 zone은 1..4입니다.

> **실측 (2026-08-30, `heat-zones`).** 열 개의 프레임을 우리가 직접 보내고 폴링으로
> 확인했습니다. 22회 무장 송신 전부가 침묵 질의 게이트를 통과했고 손상은 0바이트입니다.
> **존 2~4의 Off는 여기서 규칙 유도에서 관측으로 올라갔습니다.**
>
> **꺼진 존에 `0x45` 목표 온도를 쓰면 그 존이 켜집니다.** 네 존 모두, 여덟 번 모두
> 그랬습니다. 응답의 상태가 전부 `01`이었고 폴링도 켜진 것으로 보고했습니다. 목표만
> 되돌리면 존이 켜진 채 남으므로, 복원은 **목표를 되돌린 다음 존을 끄는** 순서여야 합니다.
>
> **다만 남은 교란이 하나 있습니다.** 여덟 번 모두 목표 값(21·23 °C)이 그때의 실내
> 온도(24~27 °C)보다 낮았습니다. "목표를 쓰면 켜진다"와 "실내보다 낮은 목표를 쓰면
> 켜진다"를 아직 가를 수 없습니다. 가르려면 실내보다 높은 목표가 필요하고, 그것은 실제로
> 보일러를 돌리는 일이라 이 계절에는 해소할 수 없습니다. 존에 따라 다를 가능성은 네 존을
> 모두 시험해 배제했습니다.
>
> 애드온의 난방 분기는 어느 해석이든 이 효과를 전혀 반영하고 있지 않습니다.
> 근거는 `.agent/analysis-live-heating-measurement.md`에 있습니다.

> **목표 온도의 실제 범위 (2026-08-30, `heat-range`).** 사용자가 말한 월패드의 5~40 °C를
> 네 존 모두에서 확인했습니다. 17회 송신에 직접 응답 99~115 ms, 1,051 프레임에서 손상
> 0바이트, **폴링 확인 16/17**입니다.
>
> | 값 | 결과 |
> | --- | --- |
> | 5 °C | 네 존 모두 적용 |
> | 40 °C | 네 존 모두 적용 |
> | 23 °C 복원 | 네 존 모두 적용 |
> | **4 °C** | **기기가 거부** |
>
> **기기가 범위를 스스로 강제합니다.** 존 1에 4 °C를 보냈더니 직접 응답은 `04`를 그대로
> 에코했는데, 폴링의 목표는 23에 머물렀습니다.
>
> **그런데 존은 켜졌습니다.** 쓰기 2.2초 뒤 상태가 `04`에서 `01`로 바뀌고 목표는 23
> 그대로입니다. 즉 명령이 **부분 적용**되었습니다. 존을 켜는 부분은 먹고 값은 버립니다.
>
> **위쪽도 같습니다 (사용자 확인).** 난방기가 허용하는 구간이 5~40 °C이고, 월패드에서
> 그 밖을 설정해도 되돌아갑니다. 우리는 아래쪽만 실측했고 위쪽은 도구가 막아 두었는데,
> 설비를 아는 쪽에서 같은 거동을 확인해 주었습니다. 실측과 증언은 근거의 성격이 다르므로
> 그대로 구분해 둡니다.
>
> **이것이 "직접 응답은 효과를 말하지 않는다"의 가장 강한 사례입니다.** 가스에서는 응답이
> 두 경우에 같기만 했는데, 여기서는 **기기가 채택하지 않은 값을 응답이 에코했습니다.**
> 응답만 믿는 구현은 목표가 4 °C로 바뀌었다고 보고하게 됩니다. 폴링이 판정해야 합니다.

**응답은 두 종류입니다.**

- 직접 응답 `F7 12 01 18 04 <46|45> 1<zone> <값> <상태> <현재온도> <목표온도> 00×5 <XOR> EE`
  — 명령 직후에 옵니다. 18바이트 고정이고, `상태`는 `01` On, `04` Off입니다.
  마스크를 걸 때 8번 바이트를 `00`으로 두면 응답이 와도 걸리지 않습니다. 그 자리가 상태입니다.
- 전체 응답 `F7 4A 01 18 04 46 10 00` + zone당 8바이트 × 8슬롯
  — 폴링 주기마다 옵니다. 슬롯은 `<상태> <현재온도> <목표온도> 00 00 00 00 00`이고
  `00`이면 없는 기기입니다. 이 회선에서 주기는 2.0~2.3초입니다.

> **그룹 명령에는 직접 응답이 없습니다 (2026-08-30, `groups3`).** 존별 명령은 100 ms 안에
> 위의 18바이트 직접 응답을 돌려주는데, 주소 `0x10`으로 보낸 그룹 명령은 돌려주지
> 않습니다. 실행 전체에서 관측된 `0x18` 프레임은 조회와 전체 응답 두 종류뿐이었습니다.
>
> 대신 **그룹 명령은 예정에 없던 전체 응답을 앞당깁니다.** 새 상태를 실은 첫 폴링이 쓰기
> 후 161 ms와 162 ms에 왔고, 폴링 간격 67개 가운데 1초 미만인 둘이 모두 그 두 송신
> 직후였습니다. 같은 실행의 조명 송신 네 번은 짧은 간격을 만들지 않았습니다. 확인 창을
> 잡을 때 존별과 그룹을 다르게 다루어야 합니다. 표본은 두 번입니다.

### 2.2 조명 `0x19` — **물리 검증 완료, 캡처에는 명령 없음**

| 동작 | 프레임 |
| --- | --- |
| 조회 | `F7 0B 01 19 01 40 10 00 00 B5 EE` (138회 관측) |
| On | `F7 0B 01 19 02 40 1<n> 01 00 <XOR> EE` |
| Off | `F7 0B 01 19 02 40 1<n> 02 00 <XOR> EE` |
| **전체 끄기** | **`F7 0B 01 19 02 40 10 02 00 B4 EE`** (`capture-1788009200284`에서 2회 관측) |

> **추가 (2026-08-29).** 아래 문단은 "이 캡처 동안 버스에 조명 설정 명령이 흐르지
> 않았다"고 적고 있으며, 그것은 `capture-1787635354221`에 대해 여전히 참입니다.
> 그러나 `capture-1788009200284`에는 조명 설정 명령 여섯 개가 있고, 그중 개별
> 켜기 세 개가 현재 구현의 프레임과 바이트 단위로 같습니다. 주소 `0x10`은 그룹이며
> 전체 끄기 한 프레임으로 세 조명이 모두 꺼지는 것을 상태 응답으로 확인했습니다.
> 개별 **끄기**와 그룹 **켜기**는 두 캡처 어디에도 없습니다.

이 캡처 동안 버스에 조명 설정 명령이 흐르지 않았습니다. 사용자가 웹 UI에서
조작하셨고 **우리 송신은 되돌아오지 않기 때문**입니다. 그러나 현재 구현이 내보내는
여섯 개 프레임은 레거시 명세와 정확히 일치하고, 사용자가 조명 1·2·3의 켜기·끄기를
실증하셨습니다. 근거가 있습니다.

주의: On은 `01`, Off는 `02`입니다. 난방(`01`/`04`)과 값이 다릅니다.

응답도 두 종류입니다. 단일 기기 `F7 0B 01 19 04 40 1<n> <상태> <상태> <XOR> EE`와
전체 `F7 0D 01 19 04 40 10 00 <조명1> <조명2> <조명3> <XOR> EE`.

### 2.3 가스 `0x1B` — 현재 구현이 맞음

`F7 0B 01 1B 02 43 11 03 00 B5 EE`가 차단입니다. 현재 구현과 일치합니다.
상태 응답에서 `03`은 잠김, `04`는 열림입니다. **여는 명령은 레거시도 지원하지
않습니다.** 레거시 `makePacketSetState`가 주석에 `04 = ON (지원되지 않음)`이라고
적어 두었습니다.

**전체 41,504 프레임에서 관측된 `0x1B` 프레임은 다섯 가지뿐입니다.**

| 프레임 | 뜻 | 횟수 |
| --- | --- | ---: |
| `f70b011b0143110000b5ee` | 질의 | 3,896 |
| `f70d011b04431100030000b5ee` | 상태 응답, 8번 바이트 `03` 잠김 | 3,843 |
| `f70d011b04431100040000b2ee` | 상태 응답, `04` 열림 | 46 |
| `f70b011b0243110300b5ee` | 차단 명령 | 1 (월패드가 보낸 것) |
| `f70b011b0443110303b0ee` | 차단의 **직접 응답** | 1 |

**직접 응답 형태**는 `F7 0B 01 1B 04 43 11 <보낸 값> <상태> <XOR> EE`입니다. 조명의
응답과 같은 골격입니다.

> **실측 A단계 (2026-08-30, `gas-a`).** 이미 잠긴 밸브에 차단 명령을 보냈습니다.
> **127 ms 만에 직접 응답이 왔고, 그 바이트가 캡처에서 월패드의 명령에 온 응답과
> 완전히 같습니다.** `matchingFrameAgoMs`가 `None`이므로 거짓 양성이 아닙니다.
>
> 이로써 **"이미 그 상태인 명령에도 기기가 응답한다"**가 확인되었습니다. 애드온의
> 상태 일치 확인 전략이 가스에서 성립한다는 뜻입니다. 상태는 34회 폴링 내내 `03`으로
> 유지되었고, 461 프레임에서 손상은 0바이트입니다.
>
> 이 밸브는 **주방 전용입니다 (사용자 확인).** 보일러 급기와 무관합니다.
>
> **실측 B단계 (2026-08-30, `gas-b`).** 사용자가 밸브를 손으로 열어 `04`를 16회 확인한 뒤
> 차단을 보냈습니다.
>
> | 시각 | 일어난 일 |
> | ---: | --- |
> | −69 ms | 마지막 폴링이 `04` 열림 |
> | 0 | 차단 송신 |
> | +118 ms | 직접 응답 `f70b011b0443110303b0ee` |
> | +1,518 ms | 첫 폴링이 `03` 잠김 |
>
> 사용자가 다시 손으로 열자 `03` → `04` 전이도 관측되었습니다. **양방향이 확인되었습니다.**
>
> **직접 응답은 효과를 구분하지 못합니다.** A단계(상태 변화 없음)와 B단계(실제로 잠김)의
> 직접 응답이 바이트 단위로 같습니다. 명령이 먹혔는지 알려면 폴링을 봐야 합니다.

### 2.4 승강기 `0x34` — **레거시 주장뿐, 캡처 근거 없음**

레거시 명세는 호출을 `F7 0B 01 34 02 41 10 <05=상행|06=하행> 00 <XOR> EE`로 적고,
일부 환경용 변형 `F7 0B 01 34 04 41 10 00 <05|06> <XOR> EE`도 함께 둡니다.

**캡처에는 승강기 설정 명령이 한 개도 없습니다.** 41,504 프레임에서 `0x34`는 전부
`kind=01` 조회 형태이고, `kind=02`도 `kind=04`도 0회입니다. 따라서 두 변형 중 어느 쪽이
이 건물에서 맞는지 알 수 없습니다.

> **정정 (2026-08-30).** 이 절은 앞서 "레거시 주석이 상행 `05`는 발견되지 않는다고
> 적었으므로 상행이 하행보다 더 불확실하다"고 썼습니다. **그 추론은 잘못되었습니다.**
>
> - 그 레거시 주석은 `kind=04` **응답 방향**에 관한 것이고, 이 회선에는 그 방향이 아예
>   없습니다. 호출 명령과는 다른 이야기입니다.
> - 상태 필드의 `05`는 `capture-1788009200284`에 **실제로 있습니다**(아래 §2.4.1).
>   상행 호출 상태는 관측되었습니다.
> - 불확실한 것은 상태가 아니라 호출 명령이고, 그 불확실성의 크기는 상행과 하행이
>   같습니다. 둘 다 관측 0회입니다.
>
> 레거시에는 **호출 취소** 프레임도 있습니다. 변형 0은
> `F7 0B 01 34 02 41 10 00 00 9A EE`, 변형 1은 `F7 0B 01 34 04 41 10 00 00 9C EE`입니다.
> 실측 시나리오는 `.agent/plan-elevator-measurement.md`에 있습니다.

조회 프레임의 해독은 확정입니다.

```
F7 0D 01 34 01 41 10 00 <상태> <층> <호기> <XOR> EE
상태 상위 4비트: 0=정지, A=상승 중, B=하강 중
상태 하위 4비트: 0=대기, 1=도착, 5=상행 호출, 6=하행 호출
```

캡처의 실제 순서가 이 해독과 정확히 맞습니다. **두 건이 있고 출처가 다릅니다.**

#### 2.4.1 상행 호출 — `capture-1788009200284`, 지금 보유

| 시각 | 상태 | 뜻 | 층 | 호기 |
| ---: | --- | --- | ---: | --- |
| 141.1s | `05` | 정지 / 상행 호출 | 1 | `0b` |
| 143.0s | `a5` | 상승 중 | 1 | `0b` |
| 145.0s | `a5` | 상승 중 | 2 | `0b` |
| 146.8s | `a5` | 상승 중 | 4 | `0b` |
| 148.8~153.9s | `a5` | 4층에서 5회 정체 | 4 | `0b` |
| 155.9s | `01` | 정지 / 도착 | 4 | `0b` |
| 157.8s | `00` | 대기 | 0 | `00` |

호출 등록에서 도착까지 **14.8초**, 도착에서 대기 복귀까지 **1.9초**입니다. 프레임 간격은
중앙값 1,806 ms입니다.

**값은 호출 중에만 옵니다.** 평소에는 `00 00 00`이고 도착 뒤 1.9초면 그리로 돌아갑니다.
별도의 질의 프레임은 없습니다. 평소 흐르는 그 프레임이 곧 상태입니다.

#### 2.4.2 하행 호출 — 우리 실측 (2026-08-30, `elev-a`)

사용자가 **복도의 하행 버튼**을 누르자 **버스에 아무것도 나타나지 않았습니다.** 145초
동안 승강기 프레임 89회가 전부 대기였고, 다른 기기도 하나도 움직이지 않았습니다.

**그런데 그 호출은 실제로 동작했습니다 (사용자 확인).** 차량이 4층으로 왔고, 그래서
뒤이은 월패드 호출이 2초 만에 도착으로 끝난 것입니다. 즉 **승강기가 실제로 움직이는
동안 이 회선은 침묵했습니다.** 처음에 저는 "아무 일도 없었을 가능성"을 후보로 두었는데,
그 가능성은 사라졌고 결론이 오히려 강해졌습니다. 이 회선은 복도에서 건 호출을 보고하지
않습니다.

같은 사용자가 **월패드의 하행 버튼**을 누르자 즉시 나타났습니다.

| 시각 | 상태 | 뜻 | 층 | 호기 |
| ---: | --- | --- | ---: | --- |
| 06:26:10 | `06` | 정지 / **하행 호출** | 4 | `0b` |
| 06:26:12 | `01` | 정지 / 도착 | 4 | `0b` |
| 06:26:14 | `00` | 대기 | 0 | `00` |
| 06:26:49 | `b6` | **하강 중** / 하행 호출 | 4 | `0b` |
| 06:26:51 | `01` | 정지 / 도착 | 4 | `0b` |
| 06:26:53 | `00` | 대기 | 0 | `00` |

호출에서 도착까지 2초뿐인 것은 **차량이 이미 4층에 있었기 때문**입니다. §2.4.1의 14.8초는
1층에서 올라온 경우입니다.

**이로써 하행 호출 상태 `6`과 하강 중 `B`가 처음 관측되었습니다.** 이전에는 상행만
있었습니다.

#### 2.4.2b 하행 호출, 1층에서 올라오는 전체 이동 (`elev-a2`)

차량이 1층에 있을 때 월패드에서 하행을 눌렀습니다. 처음으로 **이동 구간 전체**를
관측했습니다.

| 시각 | 상태 | 뜻 | 층 |
| ---: | --- | --- | ---: |
| 06:35:09 | `06` | 정지 / 하행 호출 | 1 |
| 06:35:11 | `a6` | **상승 중** / 하행 호출 | 1 |
| 06:35:13 | `a6` | 상승 중 | 1 |
| 06:35:14 | `a6` | 상승 중 | **3** |
| 06:35:15 | `b6` | **하강 중** / 하행 호출 | 4 |
| 06:35:17~23 | `b6` | 4층에서 5회, 약 8초 | 4 |
| 06:35:24 | `01` | 도착 | 4 |
| 06:35:26 | `00` | 대기 | 0 |

**호출 등록에서 도착까지 15.0초**로 §2.4.1의 상행 14.8초와 사실상 같습니다.
도착에서 대기 복귀까지 1.9초도 같습니다.

**층 필드는 2층을 건너뜁니다.** 1 → 1 → 3 → 4로 찍힙니다. 프레임 간격이 1.3~2.0초여서
차량이 그보다 빠릅니다. **연속된 층을 다 볼 수는 없습니다.**

**`a6` 상승 중이 우리 손으로 처음 확인되었습니다.** 하행 호출인데 차량이 올라오는
구간입니다. §2.4.5의 옛 캡처가 보인 `a6` → `b6` 패턴이 재현되었습니다.

> **상위 4비트는 "가고 있거나 곧 갈 방향"입니다 (§2.4.2c·§2.4.2d).**

**호기 필드는 이동 중에도 `0b`로 고정입니다.**

이 실행에서도 `0x34`의 `kind=02`와 `kind=04`는 0회입니다(126개 전부 `kind=01`).

#### 2.4.2c 상행 호출로 상위 4비트를 가름 (`elev-up`)

같은 조건에서 **상행**을 눌러 하행과 대조했습니다.

| 시각 | 상태 | 뜻 | 층 |
| ---: | --- | --- | ---: |
| 06:40:51 | `05` | 정지 / 상행 호출 | 1 |
| 06:40:53 | `05` | 정지 / 상행 호출 | 1 |
| 06:40:55~06:41:16 | `a5` | 상행 / 상행 호출 | 1 (**12회, 약 21초**) |
| 06:41:18 | `a5` | | 2 |
| 06:41:19~26 | `a5` | | 4 (**5회, 약 7초**) |
| 06:41:28 | `01` | 도착 | 4 |
| 06:41:30 | `00` | 대기 | 0 |

**결정적 대조.** 차량이 4층에 도달한 뒤 `01 도착`이 나오기 직전까지, 물리적으로는 두
경우가 같은 상태입니다.

| 호출 | 차량 | 상위 4비트 | 유지 |
| --- | --- | --- | --- |
| 하행 `06` | 4층 정지 | **`b`** | 약 8초 |
| 상행 `05` | 4층 정지 | **`a`** | 약 7초 |

**같은 물리 상태인데 호출 방향에 따라 값이 갈립니다.** 그러므로 레거시의 "moving
upside/downside" 해석은 반증되었습니다. 상위 4비트는 **이 차가 가고 있거나 곧 갈
방향**입니다. 상행 호출이면 `a`, 하행 호출이면 `b`이고, 이동 중에도 정차 중에도 같습니다.

**정차 중에 `0`으로 떨어지지 않습니다.** 애드온의 상태 기계가 "정지"를 상위 4비트로
판정하면 안 됩니다. 진짜 정지는 `00`(대기)과 `01`(도착)뿐입니다.

**두 가지가 더 나왔습니다.**

- **`05`/`06`(호출 등록, 아직 출발 전) 구간이 실재하고 여러 프레임 지속될 수 있습니다.**
  이번에는 2회 약 3.4초입니다. 앞선 하행에서는 1회였습니다.
- **`a5`가 1층에서 21초 유지되었습니다.** 층이 바뀌지 않는데 `a`입니다. 이것도 "곧 갈
  방향"이라는 읽기와 맞습니다.

**소요 시간은 일정하지 않습니다.**

| 호출 | 등록 → 도착 | 도착 → 대기 |
| --- | ---: | ---: |
| 캡처의 상행 | 14.8초 | 1.9초 |
| `elev-a2` 하행 | 15.0초 | 1.9초 |
| `elev-up` 상행 | **37.0초** | 1.9초 |

**도착에서 대기 복귀는 세 번 모두 1.9초로 일정합니다.** 등록에서 도착까지는 차량 사정에
따라 두 배 이상 벌어집니다. 타임아웃을 둔다면 후자에 맞춰야 합니다.

#### 2.4.2d 우리가 호출했습니다 — 변형 1 상행만 동작 (`elev-call`)

2,252 프레임, 손상 0바이트. 다섯 번 보냈습니다.

| 프레임 | 변형 | 방향 | 결과 |
| --- | --- | --- | --- |
| `f70b013402411006009cee` | 0 | 하행 | 무반응 (25초 대기) |
| `f70b013404411000069aee` | 1 | 하행 | 무반응 (25초 대기) |
| `f70b013402411005009fee` | 0 | 상행 | 무반응 (20초 대기) |
| **`f70b0134044110000599ee`** | **1** | **상행** | **등록됨, 1,582 ms 뒤 `05`** |
| `f70b013404411000009cee` | 1 | 취소 | **시험되지 않음** (아래 참조) |

**동작한 호출의 전체 경로**입니다. 차량이 7층에 있었습니다.

| 시각 | 상태 | 뜻 | 층 |
| ---: | --- | --- | ---: |
| 18:34:54.204 | | **우리 송신** | |
| 18:34:55.786 | `05` | 정지 / 상행 호출 | 7 |
| 18:34:57.720 | `b5` | **하강 중** / 상행 호출 | 6 |
| 18:34:59.683 | `b5` | 하강 중 | 5 |
| 18:35:01.583 | `a5` | **상승 중** / 상행 호출 | 4 |
| 18:35:02~09 | `a5` | 4층에서 6회, 약 8초 | 4 |
| 18:35:11.271 | `01` | 도착 | 4 |
| 18:35:13.017 | `00` | 대기 | 0 |

**등록에서 도착까지 15.5초**입니다. `matchingFrameAgoMs`가 `None`이므로 우연이 아닙니다.

**이것이 상위 4비트를 확정합니다.** 필요하다고 적었던 판별 사례가 바로 이것입니다.
차량이 4층보다 **위**에 있고 **상행** 호출을 받은 경우입니다.

| 구간 | 실제 이동 | 호출 방향 | 관측 |
| --- | --- | --- | --- |
| 7층 → 5층 | **하강** | 상행 | `b5` |
| 4층 정차 | 정지 | 상행 | `a5` |

이동 중에는 **실제 이동 방향**이고, 목적층에 서면 **곧 갈 방향**입니다. 하나의 규칙
"가고 있거나 곧 갈 방향"으로 네 사례가 모두 설명됩니다. 레거시의 "moving upside/downside"는
이동 중에만 맞습니다.

> **취소는 시험되지 않았습니다.** 18:35:21에 보냈는데 호출은 18:35:13에 이미 끝나 대기로
> 돌아간 뒤였습니다. 도구가 보고한 `reply=polling latency=328ms`는 거짓 양성이고, 같은
> 기록의 `matchingFrameAgoMs=1550.451`이 그것을 증언합니다. 마스크에 맞는 대기 프레임이
> 쓰기보다 1.55초 앞서 있었습니다. **취소가 듣는지는 여전히 모릅니다.**

#### 2.4.2e 판정을 마스크가 아니라 상태로 다시 하니 결론이 뒤집힙니다 (`elev-revoke`)

`--expect f70d01340141100006`은 상태 바이트가 **정확히** `06`이기를 요구합니다. 그런데
호출이 등록될 때 `06` 단계를 건너뛸 수 있습니다. 차량이 이미 움직이기 시작했으면 `a6`,
이미 그 층에 있으면 `01`로 바로 갑니다. **접두 마스크로는 "하위 4비트가 6"을 물을 수
없습니다.** 그래서 §2.4.2d의 "무반응" 판정 일부가 틀렸습니다.

상태 스트림으로 다시 판정한 전체 결과입니다.

| 시각 | 프레임 | 20초 안의 첫 비대기 | 판정 |
| ---: | --- | --- | --- |
| 18:32:52 | 변형 0 하행 | 없음 | 무반응 |
| 18:33:31 | 변형 1 하행 | 없음 | 무반응 |
| 18:34:29 | 변형 0 상행 | 없음 | 무반응 |
| 18:34:54 | 변형 1 상행 | `05` 층 7 (+1,582 ms) | **등록** |
| 18:43:05 | 변형 1 하행 | `a6` 층 `b1` (+1,588 ms) | **등록** |
| 18:43:17 | 변형 1 상행 | — | **판정 불가** (앞선 호출이 진행 중) |
| 18:44:31 | 변형 1 하행 | `01` 층 4 (+1,838 ms) | **등록** |

**변형 1은 하행도 동작합니다.** §2.4.2d가 "하행 무반응"이라고 적은 것은 반증되었습니다.
판정 가능한 4회 중 3회가 등록되었고, 실패한 것은 18:33:31 한 번뿐입니다. 왜 그때만
실패했는지는 모릅니다.

**등록 지연은 1,560~1,838 ms로 일관됩니다.** 네 번의 등록이 모두 그 범위입니다.

**변형 0은 2회 모두 무반응입니다.** 레거시 기본값이 0인데도 그렇습니다. 표본이 2회라
확정은 아니지만, 변형 1과 대조가 뚜렷합니다.

#### 2.4.2f 지하층 인코딩 실측

18:43:05의 호출에서 차량이 **지하 1층**에 있었고 층 필드가 **`b1`** 로 나왔습니다.
레거시가 "층수는 문자 인코딩, 지하3층 = B3"라고 적은 그대로입니다. **미해결이던 지하층
인코딩이 확인되었습니다.** 차량은 `b1`에 약 23초 머문 뒤 1층, 3층을 거쳐 4층에
도착했습니다. 등록에서 도착까지 39.2초입니다.

> **취소 기능은 이 설비에 없습니다 (사용자 확인, 2026-08-30).** 월패드에 호출을 취소하는
> 기능 자체가 없습니다. 레거시의 `makePacketRevokeCall`은 이 단지에 해당하지 않습니다.
>
> 이것이 우리 관측과도 맞습니다. 취소 프레임을 세 번 보냈고 세 번 다 회선에 아무 반응이
> 없었습니다. 그때는 "취소할 대상이 없어서 판정 불가"로 적었는데, **기능 자체가 없다면
> 그 무반응이 곧 답입니다.** 다만 두 근거의 성격이 다릅니다. 우리 관측은 세 번의 무반응이고,
> 기능 부재는 사용자가 설비를 아는 데서 옵니다. 어느 쪽도 "취소 프레임이 다른 단지에서도
> 무의미하다"는 말은 아닙니다.
>
> **따라서 8단계의 취소 프레임 두 개는 보낼 이유가 없습니다.** 목록에 남기되 쓰지
> 않습니다.

#### 2.4.3 월패드는 이 회선으로 호출하지 않습니다 (읽기 전용이라는 뜻은 아님)

가장 중요한 발견입니다. **월패드가 자기 버튼으로 호출했는데도 이 회선에는 호출 명령이
나가지 않았습니다.**

| 종류 | 횟수 (44,986 프레임 검사) |
| --- | ---: |
| `kind=01` 상태 | 4,224 |
| `kind=02` 설정 | **0** |
| `kind=04` 기기 → 월패드 | **0** |

호출 시각 전후 3초를 바이트 단위로 훑어도 평소 폴링뿐이고, 상태 프레임만 그 순간에
바뀝니다. 즉 **월패드는 승강기를 이 회선이 아닌 다른 경로로 호출하고, 이 회선에는 그
결과만 중계합니다.** 레거시 주석이 이 프레임의 방향을 "월패드 → 복도 미니패드"라고 적은
것과 일치합니다.

이것이 뜻하는 것은 **월패드가 이 회선으로 호출하지 않는다**는 것뿐입니다. **이 회선으로
호출할 수 없다는 뜻이 아닙니다.** 두 주장은 다릅니다.

> **정정 (2026-08-30).** 이 절은 앞서 "두 변형은 관측적 근거가 없으므로 성공할 사전
> 확률이 낮다"고 적었습니다. **사용자가 레거시 애드온으로 승강기 호출이 실제로
> 되었다고 확인했습니다.** 그러므로 이 회선으로 들어오는 호출 프레임을 시스템이 받아
> 준다는 근거가 있습니다. 관측이 없는 것은 **월패드가 그 프레임을 쓰지 않기 때문**이지
> 프레임이 통하지 않기 때문이 아닙니다.
>
> 레거시의 `packet_call_type` 기본값은 **0**이고 애드온 설정의 기본값도 0입니다. 즉
> 사용자가 값을 바꾸지 않았다면 동작한 것은 **변형 0**, 곧 현재 애드온이 만드는
> `f70b013402411006009cee`와 같은 골격입니다.

#### 2.4.3b 호출 없이 층만 묻는 방법은 없습니다

| 상태 | 층 | 호기 | 횟수 |
| --- | --- | --- | ---: |
| `00` 대기 | `00` | `00` | **4,687** |
| 호출 중 | 실제 층 | `0b` | 78 |

**상태가 `00`인데 층이나 호기가 0이 아닌 경우는 한 건도 없습니다.** 위치는 호출이 살아
있는 동안에만 실립니다.

**레거시에도 승강기 질의 패킷이 없습니다.** 다른 모든 기기 클래스에는
`makePacketQueryState`가 있는데 `Elevator`에만 없고, 만드는 것은 호출·상행·하행·취소
넷뿐입니다. 취소 프레임이 질의 역할을 하지도 않습니다. 세 번 보냈고 회선에 아무 반응이
없었습니다.

**애드온 설계에 주는 뜻**은 이렇습니다. "현재 층" 센서는 대부분의 시간 값이 없고, 호출이
진행 중일 때만 유효합니다. 그것도 **월패드나 우리가 건 호출**에 한하며, 복도 버튼이나
이웃이 부른 것은 보이지 않습니다.

#### 2.4.3c 레거시의 "상행 호출 미지원"도 이 건물에서는 틀립니다

레거시 `Elevator` 클래스의 `state_call` 주석은 `5 = 상행호출중(미지원)`이라고 적어
두었습니다. 그런데 우리가 변형 1 상행으로 실제 호출에 성공했고(§2.4.2d) 차량이
왔습니다. **레거시의 승강기 관련 주장 가운데 이 건물에서 반증된 것이 이로써 셋입니다:**
상위 4비트의 의미, 상행 호출 미지원, 그리고 변형 0이 기본이라는 전제입니다.

#### 2.4.3d 필요한 범위는 이것으로 충족되었습니다 (사용자 확인)

사용자가 이 건물에서 필요한 승강기 기능을 **상·하행 호출과 호출 중 모니터링(상승·하강·
도착·대기·층수)** 으로 한정했습니다. 그 범위는 전부 실측되었습니다.

따라서 아래 두 미상은 **무시해도 되는 것으로 정리합니다.** 지우지는 않되, 더 파지
않습니다.

- 호기 필드 `0b`의 의미 (§2.4.4)
- 변형 0이 왜 동작하지 않는지 (§2.4.2e)

#### 2.4.4 호기 필드는 호기가 아닐 수 있습니다

관측된 값이 언제나 `0b`인데 **이 건물의 승강기는 1대입니다 (사용자 확인).** 레거시는 이
바이트를 `ev_dev_idx`, 즉 "n호기"로 읽지만 11호기가 있을 수 없습니다. 대기 중에는 `00`이
되므로 무의미한 고정값도 아닙니다. **무엇을 뜻하는지 모릅니다.**

#### 2.4.5 하행 호출 — 이전 캡처, 현재 디스크에 없음

```
+226.1s  06  → 하행 호출, 아직 정지
+227.5s  a6  → 상승 중          층 01
+229.1s  a6  → 상승 중          층 02
+231.1s  a6  → 상승 중          층 03
+233.0s  b6  → 하강 중          층 04   (5회)
+241.8s  01  → 도착             층 04
```

차량이 아래에서 올라와(`a6`) 4층에서 태우고 내려가는(`b6`) 모습입니다.

### 2.5 출입구 — **가장 불확실함**

현재 구현은 상태를 `0x1E`에서 읽고 제어는 `0x7F` 매크로로 내보냅니다.

**이 캡처에는 `0x7F` 프레임이 306.8초 동안 한 개도 없습니다.** 반면 `0x1E`는
조회 181개와 **설정 명령 6개**가 있습니다.

```
+273.1s +273.9s +274.6s   F7 0E 01 1E 02 43 11 04 00 04 FF FF B6 EE
+279.0s +279.6s +280.1s   (동일 프레임)
```

세 개씩 두 묶음, 묶음 안 간격 약 0.7초입니다. 월패드가 3프레임 매크로를 보내는
형태입니다. 다만 여섯 개가 **전부 같은 바이트**라 필드를 유추할 변화가 없고,
레거시 파서도 `0x1E`를 해독하지 않고 로그만 남깁니다. 지금 이 프레임을 재생하는 것은
추측입니다.

> **실측 조사 (2026-08-30).** 44,986 프레임 전수 조사에서 `0x1E` 프레임은 **두 종류뿐**입니다.
> 질의 `f70b011e016211000091ee`가 4,865회로 **전부 바이트 단위 동일**하고, 설정
> `f70e011e024311040004ffffb6ee`가 3회입니다. **질의에 상태를 실을 자리가 없습니다.**
>
> **벨 호출은 이 회선에 나타나지 않습니다.** 캡처의 문열기 순간(121.9·122.6·123.1초)
> 앞뒤로 여덟 기기 가운데 무엇도 바뀌지 않았습니다. 20초 창의 드문 프레임은 문열기
> 자신과 19초 뒤 승강기 호출뿐입니다. 복도 승강기 버튼과 같은 구조로, 월패드가 내부에서
> 처리하고 회선에는 결과만 내놓습니다.
>
> **레거시는 이 프레임을 보내지 않습니다.** `DoorLock.makePacketOpen()`이 같은 바이트를
> 만들지만 그것을 보내는 `set_doorlock_open()`이 통째로 주석 처리되어 있고, 레거시는
> 문을 **GPIO 핀**으로 엽니다. 레거시의 진짜 문열기 경로는 서브폰 매크로
> `0xB4`(현관)와 `0x61`(공동현관)이며, 두 주석 모두 **"카메라 영상이 우회된 상태에서"**
> 라고 적어 호출 중에만 유효함을 시사합니다. 다만 이 회선에 `0x7F`는 0회입니다.
>
> **실측 A단계 (2026-08-30, `entrance-a`).** 사용자가 세대현관 벨을 눌렀고, **월패드에는
> 문 앞 영상이 스트리밍되었습니다.** 그동안 이 회선은 **203초 내내 프레임 12종 그대로**
> 였습니다. 벨 전 33초의 기준선과 종류가 하나도 다르지 않습니다.
>
> **벨과 영상은 이 회선 밖입니다.** 캡처의 문열림 순간 관측(§위)과 합쳐, 이제 근거가 두
> 건입니다. 앞의 것은 사후 분석이었고 이번은 예측을 세우고 확인한 것입니다.
>
> **A단계 두 번째 시행 (2026-08-30, 같은 실행).** 사용자가 밖으로 나가 대문을 닫고, 벨을
> 누르고, 직접 문을 열고 들어와, 월패드의 스트리밍 영상을 확인한 뒤 **월패드의 문열림
> 버튼을 눌렀습니다.** 128초 창에서 새로 나타난 프레임은 딱 둘입니다.
>
> | 프레임 | 횟수 | 정체 |
> | --- | ---: | --- |
> | `f70e011e024311040004ffffb6ee` | **3** | 문열림 버튼 |
> | `f74a0118…041b17…` | 1 | 난방 존 2의 실내 온도가 26 → 27로 오른 것 |
>
> **벨은 이번에도 아무것도 만들지 않았습니다.** 첫 시행과 독립된 두 번째 근거입니다.
>
> **문열림 버튼은 세 프레임을 0.69초 간격으로 내보냅니다.** 묶음 전체가 1.38초이고,
> 원본 캡처의 0.7초 간격과 같습니다.
>
> | 시각 | 간격 |
> | --- | ---: |
> | 19:37:31.272 | |
> | 19:37:31.959 | +0.69s |
> | 19:37:32.647 | +0.69s |
>
> **그 세 프레임은 `0x1E` 질의의 자리를 대신합니다.** 평소 2~3초마다 오는
> `f70b011e0162…` 질의가 묶음 동안에는 나오지 않고, 폴링 회전의 그 칸을 문열기가
> 차지합니다. **응답은 없습니다.** 다른 기기가 반응하지도 않습니다.
>
> **이로써 이 프레임의 정체가 우리 실행 안에서, 알려진 조작으로 확정되었습니다.**
> 원본 캡처는 사후 관찰이었지만 이번은 "이 버튼을 눌렀다"와 프레임이 직접 이어집니다.

> **애드온에 주는 뜻은 분명합니다. 초인종을 감지할 수 없습니다.** 누가 왔다는 사실도,
> 통화가 열렸다는 사실도 이 회선에는 나타나지 않습니다. 현재 구현이 세대현관 호출을
> 표시한다면 그 근거는 `0x1E 02` 문열기 프레임뿐이고, 그것은 **문이 열린 뒤에야** 옵니다.
> 호출의 시작이 아니라 끝입니다.

> **실측 B단계 (2026-08-30, `entrance-b`). 호출 없이 보낸 문열기는 문을 열지 못했습니다.**
>
> | 시도 | 간격 | 결과 |
> | --- | --- | --- |
> | 1회 | — | **안 열림** |
> | 3연속 | 0.34s · 0.47s (묶음 0.81초) | **안 열림** |
>
> 회선은 두 번 다 조용했습니다. `0x1E`는 응답하지 않고 다른 일곱 기기도 움직이지
> 않습니다. 3연속의 마지막 한 발은 게이트 기기가 `0x1E` 자신이었으므로 **월패드가 쓰는
> 것과 같은 칸**에 나갔습니다.
>
> 월패드는 0.69초씩 세 번(묶음 1.38초)인데 우리는 0.34·0.47초(묶음 0.81초)였습니다.
> 게이트가 창을 잡는 대로 나가므로 간격을 우리가 정할 수 없습니다.
>
> **C단계까지 마쳤고 전부 열리지 않았습니다.** 통제한 변수는 이렇습니다.
>
> | 시행 | 호출 | 형태 | 간격 | 결과 |
> | --- | --- | --- | --- | --- |
> | B3 | 없음 | 1회 | — | 안 열림 |
> | B5 | 없음 | 3연속 | 0.34 · 0.47 | 안 열림 |
> | C | **있음** | 1회 | — | 안 열림 |
> | C2 | **있음** | 3연속 | 0.34 · 0.33 | 안 열림 |
> | C3 | **있음** | 3연속 | **0.50 · 0.68** | 안 열림 |
>
> 총 11회 송신했습니다. C3은 월패드의 0.69초 간격과 사실상 같습니다. **호출 유무,
> 프레임 개수, 간격, 송신 위치를 모두 바꿔 봤고 바이트는 월패드가 보내는 것과 완전히
> 동일합니다.** 남는 차이는 "누가 보냈는가"뿐이고 그것은 우리가 바꿀 수 없습니다.
>
> **결론: 이 프레임은 명령이 아니라 통지로 보입니다.** 월패드가 다른 경로로 문을 열고
> 회선에는 결과를 알립니다. 세 갈래 근거가 한곳을 가리킵니다. 응답이 44,986 프레임에서
> 0회, 11회 송신이 모두 무효, 그리고 레거시가 `makePacketOpen`을 정의만 해 두고 **보내는
> 코드를 주석 처리한 채 문을 GPIO 핀으로 엽니다.**
>
> **사용자 판단 (2026-08-30): 출입문 기능은 서브폰이 연동되어야 캡처와 구현이
> 가능합니다.** 레거시의 실제 문열기 경로가 서브폰 매크로 `0xB4`·`0x61`이고 이 회선에
> `0x7F`가 0회인 것과 일치합니다. **이번 범위에서는 여기까지입니다.**
>
> **다만 얻은 것이 하나 있습니다. 월패드가 문을 열면 우리는 알 수 있습니다.**
> `0x1E 02` 세 프레임이 통지로 흐르므로 "문이 열렸다"는 이벤트 센서는 만들 수 있습니다.
> 누가 언제 열어 줬는지 기록이 됩니다.

> **따라서 거부 문구의 근거가 바뀌었습니다.** `guard.ts`는 이 프레임을 "의미가 미상이며
> 문을 여는 명령일 수 있다"는 이유로 거부하는데, **레거시가 `makePacketOpen`이라 이름
> 붙였고 캡처에서 문이 열리는 순간에 나왔으므로 문열기 명령이 맞습니다.** 미상은
> 해소되었고 남는 것은 위험 판단뿐입니다. 시나리오는
> `.agent/plan-entrance-measurement.md`에 있습니다.

**모니터링 쪽에 결함이 두 개 있습니다.** 캡처를 실제 모니터에 흘려 넣어 측정했습니다.

- **세대 현관의 호출 표시가 꺼지지 않습니다.** `0x1E` `02` 프레임을 보면
  `call: true`로 표시하는데, 이를 되돌리는 경로가 없습니다. +274초에 켜진 뒤
  캡처가 끝나는 +306초까지 32초 동안 켜진 채였습니다. 세대를 다시 조회해도
  꺼지지 않으므로, 한 번 호출이 오면 세대 현관은 계속 호출 중으로 보입니다.
- **공동 현관은 아무 값도 받지 못합니다.** `0x1E` 조회 프레임은 길이가 11이라
  payload가 7바이트인데 분기 조건이 `>= 8`이므로 걸러집니다. 181개가 전부
  버려지고, 공동 현관은 처음부터 끝까지 `lastSeenAtMs: 0`, `stale: true`로 남습니다.
  화면에 값이 있는 것처럼 보이더라도 그것은 초기 표시일 뿐 관측값이 아닙니다.

### 2.6 조회 전용

콘센트 `F7 0B 01 1F 01 40 10 00 00 B3 EE`(139회)와
환기 `F7 0B 01 2B 01 40 11 00 00 86 EE`(141회)는 현재 구현과 일치하며 캡처에 있습니다.
레거시는 두 기기 모두 설정 명령을 정의해 두었으나 이 캡처에는 없습니다.

일괄소등 `0x2A`는 조회 180개·응답 181개가 있습니다. 현재 구현은 이 계열을 통째로
`ambiguous`로 분류해 디버그 화면의 미확인 묶음에만 보여 줍니다. 레거시에는
`BatchOffSwitch` 정의가 있으므로 해독 여지가 있으나 이번 범위 밖입니다.

> **해독 (2026-08-30, 실측).** 사용자가 현관의 일괄소등 스위치를 네 번 누르는 동안
> 관측했습니다. 자세한 근거는 `.agent/analysis-live-light-measurement.md` §11에 있습니다.
>
> **일괄소등 스위치는 월패드가 아니라 현관 입구에 따로 있습니다 (사용자 확인).** 이것이
> 관측을 설명합니다. `0x2A`는 그 스위치 자체이고, 스위치를 누르면 그 기기의 상태가 바뀌며,
> 월패드가 폴링으로 그것을 읽고 조명 명령을 내보냅니다. 월패드가 `0x2A`에게 명령하지
> 않는 이유가 여기 있습니다. 명령할 대상이 아니라 읽을 대상이기 때문입니다.
>
> ## **`0x2A`에는 설정 명령이 있습니다 (2026-08-30, `batchoff2`).**
>
> 이 문서는 이전에 "설정 명령이 없다"고 적었습니다. **그 서술은 반증되었습니다.**
>
> | 동작 | 프레임 | 응답 | 레이턴시 |
> | --- | --- | --- | ---: |
> | 걸기 | `F7 0C 01 2A 02 40 11 01 19 00 9B EE` | `f70c012a0440110119019cee` | 133 ms |
> | 풀기 | `F7 0C 01 2A 02 40 11 02 19 00 98 EE` | `f70c012a0440110219029cee` | 130 ms |
>
> **명령 응답은 12바이트로 질의 응답(14바이트)과 형태가 다릅니다.**
> `F7 0C 01 2A 04 40 11 <보낸 값> 19 <상태> <XOR> EE`이고, 두 응답 모두 9번 바이트가
> 상태입니다.
>
> **우리 명령이 버튼과 같은 연쇄를 일으켰습니다.** 걸기를 보낸 뒤 회선의 시간표입니다.
>
> | 시각 | 일어난 일 |
> | ---: | --- |
> | +133 ms | `0x2A`가 직접 응답하며 상태를 `01`로 보고 |
> | +1,473 ms | 월패드의 정기 폴링이 바뀐 상태를 읽음 |
> | +1,546 ms | 월패드가 `f70b01190240100200b4ee` 조명 그룹 끄기를 내보냄 |
>
> 폴링이 읽고 **73 ms 뒤**에 조명 명령이 나갑니다. `M4-E134`가 세운 인과 모형 그대로이고,
> 이번에는 우리가 그 사슬의 첫 고리를 직접 당겼습니다. 풀기도 같은 방식으로
> `f70b01190240110100b6ee` 조명 1 켜기를 유발했습니다.
>
> **출처는 레거시 구현입니다.** 같은 파일이 만드는 질의 프레임
> `f70e012a0140100019001b0382ee`가 이 버스에 흐르는 질의와 바이트 단위로 같아서 근거가
> 됩니다.
>
> **먼저 시도한 유도 후보는 무시되었습니다.** `f70b012a024010010084ee` 등은 세 군데가
> 틀렸습니다. 길이 `0x0B`(실제 `0x0C`), 주소 `0x10`(실제 `0x11`), 그리고 값 뒤의 `19 00`
> 페이로드 누락입니다. **교훈은 설정 주소가 질의 주소와 같을 필요가 없다는 것입니다.**
> 조명이 `10`=그룹·`11~13`=개별인 것과 같은 구조를 `0x2A`도 따르며, 질의는 `10`이고
> 설정은 `11`입니다.
>
> **이 기기는 다른 방 조명에 이르는 유일한 경로입니다 (사용자 확인).** 월패드는 그 조명을
> 제어하지 못합니다. **우리가 건 일괄소등으로 다른 방 조명이 실제로 꺼진 것도 사용자가
> 확인했습니다 (2026-08-30).** `M4-E143`이 남겨 둔 가장 큰 공백이 닫혔습니다. 버스로는
> 볼 수 없는 사실이고 집에 있는 사람만 알 수 있습니다. 따라서 `0x19`의 그룹 프레임은 세 등을 끄는 것이지 집 전체 소등이
> 아닙니다.
>
> 아래는 이 반증 이전에 적힌 서술이며, 역사로 남깁니다.
>
> **캡처에 설정 명령이 없습니다.** 이 기기는 상태만 보고하고, 버튼의 실제 동작은
> 전부 `0x19` 조명 계열로 나갑니다.
>
> | 항목 | 값 |
> | --- | --- |
> | 응답 | `F7 0E 01 2A 04 40 10 00 19 <조명 일괄소등> 1B <가스> <XOR> EE` |
> | 9번 바이트 | `01` 일괄소등 걸림 · `02` 풀림 |
> | 11번 바이트 | **가스 밸브 상태.** `03` 잠김 · `04` 열림 |

> **`0x2A`는 두 기기의 상태를 함께 나릅니다 (2026-08-30, `gas-b`).** 이 문서는 앞서
> `1B 03`을 고정 꼬리로 적었는데, **그것은 가스가 내내 잠겨 있어서 움직이지 않았을
> 뿐입니다.** 바이트 10은 기기 주소 `1B`이고 바이트 11은 그 기기의 상태입니다.
> 268회 관측 전부에서 바이트 11이 같은 시점의 `0x1B` 상태와 일치했고, 가스를 잠글 때와
> 다시 열 때 양방향으로 따라 움직였습니다. 반영 지연은 1,470~1,758 ms로 이 기기의 자체
> 폴링 주기와 같은 크기입니다.
>
> 마찬가지로 바이트 8의 `19`도 고정값이 아니라 **조명 기기의 주소**입니다. 즉 이 프레임은
> `<기기> <상태>` 쌍을 두 개 실은 것입니다. 레거시가 이 기기를 `multi function switch`로
> 부르는 이유이고, 현관 패널이 일괄소등과 가스차단을 함께 달고 있는 형태와 맞습니다.
>
> **`1B` 쌍을 설정하는 프레임은 시험하지 않았습니다.** 레거시에도 없고, 가스는 `0x1B`로
> 직접 제어할 수 있으므로 필요도 없습니다. 유도해 보내는 것은 발명입니다.
> | 걸 때 나가는 명령 | `F7 0B 01 19 02 40 10 02 00 B4 EE` (조명 그룹 전체 끄기) |
> | 풀 때 나가는 명령 | `F7 0B 01 19 02 40 11 01 00 B6 EE` (조명 1 켜기) |
>
> **`0x2A`의 상태는 언제나 조명 명령보다 먼저 바뀌어 보고됩니다.** 네 번의 전이 모두
> 그랬고, 바뀐 상태를 실은 응답과 뒤이은 조명 명령 사이는 84·186·192·200 ms였습니다.
>
> 이 84~200 ms를 **상태가 바뀐 시점과 조명 명령 사이의 간격으로 읽으면 안 됩니다.**
> `0x2A` 응답은 균일한 폴링이고, 12.5분 동안 453회에 응답 간격의 중앙값이 1,860 ms이며
> 452개 간격 가운데 250 ms 이하가 하나도 없습니다. 즉 상태 변화 자체는 그 응답보다
> 최대 약 1.9초 앞설 수 있습니다. 확정할 수 있는 것은 **상태 변화가 조명 명령보다
> 앞선다**는 순서뿐입니다.
>
> 그 대신 84~200 ms라는 좁은 폭이 기전을 가리킵니다. 월패드가 `0x2A`를 폴링하다가 바뀐
> 상태를 읽고, 그 응답으로부터 일정한 지연 뒤에 조명 명령을 내보내는 모습입니다. 다만
> 이 밀리초 값도 TCP 도착 시각의 차이라서 전선 위의 간격과 같지 않습니다. EW11이
> `Gap Time` 50 ms 동안 회선이 조용해지기를 기다렸다가 밀어내기 때문입니다. **순서만은
> 정확합니다.** TCP가 전선 위의 차례를 보존하기 때문입니다.
>
> **우리가 조명 그룹 끄기를 보내도 `0x2A`는 움직이지 않습니다 (2026-08-30, `groups3`).**
> 버튼이 걸릴 때 내보내는 바로 그 프레임을 우리가 보냈고, 실행 내내 `0x2A` 응답 91회의
> 9번 바이트가 전부 `02`였습니다. 버튼이 먼저 이 기기를 세우고 조명 명령이 그 결과로
> 나간다는 해독이 예측한 그대로입니다. 뒤집어 말하면 **버스로 조명을 전부 꺼도 월패드의
> 일괄소등 표시는 풀린 채로 남으므로**, 두 상태는 갈릴 수 있습니다.
>
> **푸는 동작은 이전 상태를 복원하지 않습니다.** 직전 상태가 `010101`일 때도 `020101`일
> 때도 결과는 똑같이 `010202`였습니다. 조명 1만 켜는 고정 동작입니다.

## 3. 난방이 실패한 진짜 이유

현재 구현이 난방 On에 대해 내보내는 프레임은 이것입니다.

```
F7 0C 01 18 02 40 11 01 01 00 00 B1 EE      ← 현재 구현
F7 0B 01 18 02 46 11 01 00 B1 EE            ← 버스에서 관측된 실제 명령
```

길이가 다르고(`0C` 대 `0B`) 하위명령이 다릅니다(`40` 대 `46`). 그리고 주소 뒤에
zone을 **한 바이트 더** 넣습니다. 월패드는 이 프레임을 해석할 수 없으므로 무시합니다.
사용자가 겪으신 "아무 일도 일어나지 않음"이 정확히 이것입니다.

온도 프레임은 더 분명합니다. 현재 구현은 온도를 두 번 넣습니다.

```
F7 0C 01 18 02 40 11 01 01 17 17 B1 EE
                              ↑  ↑ 온도가 두 번
```

이 배치는 명령이 아니라 **응답 프레임의 배치**(`상태 · 현재온도 · 목표온도`)입니다.
즉 인코더는 명령을 관측해 만든 것이 아니라, 상태 응답의 모양을 흉내 내어 만든 것입니다.

### 3.0 직접 응답을 전부 버립니다

난방 프레임과 별개로, 디코더가 **명령 직후에 오는 직접 응답을 한 건도 받아들이지
않습니다.** 원인은 길이 조건입니다. `payload = raw.slice(2, -2)`이므로 payload는
프레임 길이에서 4바이트가 빠집니다.

| 프레임 | 길이 | payload | 요구 조건 | 결과 |
| --- | --- | --- | --- | --- |
| 조명 단일 응답 `F7 0B 01 19 04 40 1<n> …` | 11 | 7 | `>= 9` | 버려짐 |
| 난방 직접 응답 `F7 12 01 18 04 46 1<zone> …` | 18 | 14 | `>= 38` | 버려짐 |
| 조명 전체 응답 `F7 0D 01 19 04 40 10 00 …` | 13 | 9 | `>= 9` | 통과 |
| 난방 전체 응답 `F7 4A 01 18 04 46 10 00 …` | 74 | 70 | `>= 38` | 통과 |

프레임을 하나씩만 넣어 격리 시험했습니다.

```
기준 상태 (전체 응답으로 1,2,3 = off)
  조명 3번 ON 단일 응답 투입 → 1=off 2=off 3=off   변화 없음 · 미확인 1건
  조명 1번 ON 단일 응답 투입 → 1=off 2=off 3=off   변화 없음 · 미확인 1건
  조명 2번 ON 단일 응답 투입 → 1=off 2=off 3=off   변화 없음 · 미확인 1건
  전체 응답 1,2,3 = ON 투입  → 1=on  2=on  3=on    반영됨

난방 zone4 ON 직접 응답 투입 → zone4 상태 없음 (lastSeenAtMs 0, stale) · 미확인 1건
```

그래서 상태는 **오직 폴링 전체 응답으로만** 갱신됩니다. 4절에서 다루는 지연의 원인이
바로 이것입니다. 조건을 길이가 아니라 **주소 바이트**로 갈라야 합니다.

### 3.1 자기 확인 고리 — 이게 테스트를 통과한 이유

디코더에도 같은 발명품이 들어 있습니다. `0x18`의 `02` 프레임을 상태 원천으로 읽으면서
`payload[5]=zone, payload[6]=상태, payload[7]=현재온도, payload[8]=목표온도`,
길이 14 이상을 요구합니다. 이 배치는 **우리가 발명한 프레임에만 맞고**, 길이 11인 진짜
명령에는 맞지 않습니다.

인코더와 디코더가 같은 발명품을 공유하므로 둘은 서로를 확인해 줍니다. 테스트는
이 고리 안에서만 돌았고, 버스와 한 번도 대조되지 않았습니다.

### 3.2 evidence 등급이 틀렸습니다 — 0.2.6에서 안전 문제로 승격됨

난방 zone 1의 On·Off·온도 세 가지가 `observed`로 표기되어 있습니다. 그러나

- 그 프레임은 어떤 캡처에도 없고,
- 레거시 명세와도 다르며,
- 물리적으로 아무 동작도 하지 않고,
- zone 2~4는 **바이트가 한 니블만 다른 동일한 생성식**인데 `inferred_candidate`입니다.

zone 1만 관측되었다고 볼 근거가 없습니다. 이 표기는 만들어진 것입니다.

어제 공개한 0.2.6에서 `observed`는 **확인 없이 한 번 눌러 바로 전송**을 뜻합니다.
따라서 지금 이 세 가지는 잘못된 프레임을 한 번의 탭으로 버스에 올리는 경로입니다.
월패드가 무시하므로 실제 피해는 없지만, 등급 자체가 틀렸으므로 고쳐야 합니다.

대조 결과 나머지는 문제가 없습니다. 조명 6건은 레거시 일치 + 물리 실증으로 근거가
있고, 콘센트·환기 조회 2건은 캡처에 그대로 있습니다.

## 4. 더 빠르게 — 확인 신호를 바꾸는 것이 핵심

### 4.1 측정값

| 항목 | 측정 |
| --- | --- |
| 명령 → 직접 응답 | 10쌍 전부 **같은 TCP 읽기 안**. 캡처 해상도 미만 |
| 계열별 폴링 주기 | 중앙값 1,855~2,194 ms (최대 6,333 ms) |
| 프레임 간 간격 중앙값 | 77 ms |
| 간격 ≥ 50 ms 비율 | 82.2% |
| 간격 ≥ 100 ms 비율 | 46.8% |

### 4.2 우리가 느린 이유

우리는 명령을 보낸 뒤 **다음 폴링 응답**을 기다립니다. 그래서 확인까지 최대 한
폴링 주기(약 2.2초, 최악 6.3초)가 걸리고, 관측 창을 초 단위로 잡을 수밖에 없었습니다.
3초 창이 아슬아슬한 것도 이 때문입니다.

그런데 **직접 응답은 명령 즉시 옵니다.** 3.0절에서 보았듯 현재 디코더는 이것을
전부 버립니다.

**우리 송신에도 직접 응답이 온다는 것이 이미 증명되어 있습니다.** 이 캡처에는
조명 설정 명령(`0x19` `02`)이 **한 개도 없습니다.** 그런데 단일 조명 직접 응답은
12개가 있습니다.

```
+  7.4s 조명1 on    + 10.8s 조명3 on    + 13.6s 조명2 off   + 19.3s 조명1 off
+ 21.0s 조명3 off   + 27.0s 조명2 off   + 31.6s 조명2 on    + 35.5s 조명1 on
+ 37.5s 조명3 on    + 41.1s 조명1 off   + 42.1s 조명2 off   + 43.0s 조명3 off
```

버스에서 이 조명들을 명령한 주체가 없습니다. **우리가 웹 UI에서 보낸 것**이고,
우리 송신은 되돌아오지 않으므로 명령만 보이지 않는 것입니다. 즉 월패드는
우리가 쓴 프레임에 대해서도 즉시 직접 응답을 냅니다. 확인 신호를 직접 응답으로
바꾸는 것은 "될 것 같다"가 아니라 **오늘 제어되는 기기에서 이미 관측된 사실**입니다.

### 4.3 권고

1. **직접 응답을 확인 신호로 삼습니다.** 난방은 `0x18 04 <46|45> 1<zone>`,
   조명은 `0x19 04 40 1<n>`. 이것만으로 관측 창이 초에서 수십 밀리초로 줄고,
   3초 창의 지터 문제도 함께 사라집니다.
2. **확인될 때까지 재전송합니다.** 레거시는 상태가 목표와 같아질 때까지 최대
   재시도하며, 소켓(EW11) 환경에서는 간격 100 ms · 최대 50회를 권고합니다.
   레거시가 남긴 이유가 명확합니다. EW11은 무선 지연 때문에 RS485의 유휴 구간을
   정확히 알 수 없으므로, 짧은 간격으로 여러 번 보내는 편이 낫다는 것입니다.
   다만 직접 응답을 쓰면 재전송은 **충돌 시 보조 수단**이지 주 수단이 아닙니다.
   보수적으로 간격 150 ms · 최대 5회를 제안합니다.
3. **재전송은 하나의 논리적 전송 안에 두어야 합니다.** 이 부분이 어제 작업과 충돌합니다.
   `tx_cooldown_ms`(250)와 `speculative_tx_cooldown_ms`(5000)는 시도마다 부과되고,
   어제 후보 연타를 막는 유일한 장치로 기록했습니다. 재시도 루프를 서버의 한 전송
   안에 두고 **쿨다운은 전송 단위로 한 번만** 부과해야 합니다. 그렇게 하지 않으면
   첫 재시도가 자기 쿨다운에 걸려 죽거나, 반대로 연타 제한이 뚫립니다.

## 5. 노란 버튼과 `0x7F` 매크로의 정체

### 5.1 정정 — 문을 여는 버튼이 맞습니다

앞선 보고에서 "문을 여는 버튼이 아닙니다"라고 말씀드렸는데 **틀렸습니다.**
레거시를 끝까지 읽으니 세 버튼 모두 문을 여는 시퀀스였습니다.

### 5.2 `0x7F` 프레임의 구조

레거시의 서브폰 프레임은 5바이트 고정입니다. XOR 검사가 없습니다.

```
7F <헤더> 00 00 EE
```

의미는 전부 `<헤더>` 한 바이트가 결정합니다.

| 헤더 | 의미 |
| --- | --- |
| `B7` | 현관 초인종 카메라 영상을 서브폰으로 우회 **시작** |
| `B8` | 같은 우회 **종료** |
| `5F` | 공동현관문 영상 우회 **시작** |
| `60` | 같은 우회 **종료** |
| `B9` | 단순 문열기용 주방 서브폰 **활성화** |
| `BA` | 같은 것 **비활성화** |
| `B4` | **세대 현관 도어락 해제** |
| `61` | **공동 현관문 열림** |

### 5.3 레거시의 문열기 절차

레거시는 문을 열 때 반드시 세 단계를 밟습니다. 도어락 해제 프레임은 **영상이
우회된 상태에서만** 받아들여지기 때문입니다.

```
1. 영상 우회 시작        → 0.2초 대기
2. 도어락 해제           → 0.2초 대기
3. 영상 우회 종료
```

어떤 헤더로 우회를 시작하느냐가 **현재 호출 상태에 따라 달라집니다.** 세대 초인종이
울리는 중이면 `B7`, 공동현관 호출 중이면 `5F`, 아무 호출도 없으면 `B9`입니다.

### 5.4 우리 매크로 세 개의 정체

| 현재 UI 이름 | 프레임 | 실제 의미 |
| --- | --- | --- |
| 세대 비활성 고정 매크로 | `7FB9` `7FB4` `7FBA` | **호출이 없을 때 세대 현관문 열기** |
| 세대 호출 고정 매크로 | `7FB7` `7FB4` `7FB8` | **초인종이 울리는 중에 세대 현관문 열기** |
| 공동 호출 고정 매크로 | `7F5F` `7F61` `7F60` | **공동현관 호출 중에 공동 현관문 열기** |

레거시 절차와 프레임·순서·200 ms 간격까지 정확히 같습니다. 이름의 "비활성/호출"은
결과가 아니라 **전제 조건**, 즉 어떤 우회 헤더를 쓸지를 가리킵니다.

이름이 상태를 흉내 내는 것처럼 읽히므로 **안전 문제입니다.** "세대 비활성 고정
매크로"를 눌러 보는 사용자는 현관문이 열릴 것이라고 예상하지 않습니다.
0.2.7에서 이름을 실제 동작대로 바꿉니다.

### 5.5 노란색의 의미

`class="warning"`은 **관측으로 확인되지 않은 제어**를 뜻합니다. 승강기 호출 버튼도
같은 색입니다. 초록·기본색이 근거 있는 제어입니다.

### 5.6 그런데 지금은 보낼 수 없습니다

세 매크로 모두 현재 **전송이 막혀 있습니다.** 살아 있는 애드온의 게이트를 읽으면
`sevenFProof: false`입니다. 서버는 `0x7F` 프레임을 보내기 전에 현재 세대에서
완전한 `0x7F` 시퀀스를 **관측했을 것**을 요구하는데, 그 조건이 한 번도 충족된 적이
없습니다.

충족될 수 없는 이유가 기록에 남아 있습니다. `.agent/progress.md` 276행은 분석한
모든 캡처에 대해 "표현된 모든 바이트가 정규 `F7` 제어 프로토콜에 속하며 `7F` 서브폰
패킷은 나타나지 않는다"고 적고, 레거시가 서브폰 제어를 **별도의 `7F...EE` 파서**로
보낸다는 점을 함께 기록했습니다. 이번 306.8초 캡처도 같습니다. `7F` 프레임 0개입니다.

레거시 설정 구조가 이를 확인해 줍니다. RS485 포트마다 `packettype`이 따로 있고
`0`이 정규, `1`이 서브폰이며 포트마다 별도의 EW11 주소를 가집니다. 즉 **서브폰은
설계상 별개의 물리 회선**입니다. 지금 우리 EW11이 그 회선에 붙어 있다는 증거가 없습니다.

로드맵도 이미 그렇게 잡혀 있습니다. 43행은 첫 제품 범위에서 서브폰 캡처를 제외했고,
105행은 서브폰을 **M7**으로 미뤄 두었습니다.

### 5.7 사용자가 원하는 동작과의 거리

| 원하시는 것 | 현재 상태 |
| --- | --- |
| 세대 현관 호출을 HA가 인식 | `0x1E` `02`로 인식됨. 다만 **꺼지지 않음**(2.5절) |
| 공동 현관 호출을 HA가 인식 | **전혀 안 됨.** 조회 프레임 181개가 전부 버려짐 |
| 호출 후 문열기 전송 | 프레임은 정확히 갖고 있으나 **회선이 확인되지 않음** |

즉 문열기 프레임 자체는 이미 올바릅니다. 막힌 것은 **그 프레임이 닿을 회선**입니다.
6.2의 실측이 이 질문에 답해야 합니다.

## 6. 0.2.7 계획

### 6.1 즉시 고칠 것 (캡처 근거 있음)

**전송**

0. `makeF7`의 길이 바이트를 고칩니다. `payload.length + 3`으로 계산하는데 실제
   프레임은 `payload.length + 4`바이트이므로, **이 함수가 만든 모든 프레임이 길이를
   하나 적게 선언합니다.** 조명은 하드코딩 16진수라 무사했고 난방만 이 함수를 씁니다.
   난방 실패의 세 번째 독립 원인입니다.
1. 난방 On/Off/온도 프레임을 관측된 형식으로 교체합니다.
2. 난방 zone 1~4를 **전부 `inferred_candidate`로 통일**합니다. zone 1만 `observed`인
   비대칭은 근거가 없으므로 없애야 하지만, 반대 방향으로 올려서도 안 됩니다.
   프레임은 캡처로 검증되었으나 **우리가 그 프레임을 보내 실제로 난방을 켠 적은
   아직 없습니다.** 0.2.6에서 `observed`는 확인 없이 한 번에 전송을 뜻하므로,
   한 번도 성공한 적 없는 기기에 그 권한을 주는 것은 이번에 진단한 실패를
   그대로 반복하는 일입니다. 순서를 지킵니다.

   > 프레임 교체 → `inferred_candidate`로 공개 → 사용자가 한 번 눌러 물리 결과와
   > `0x18 04 46` 응답을 확인 → 다음 릴리스에서 `observed`로 승격

   한 릴리스 동안 탭이 한 번 더 필요한 대신, 같은 실패를 세 번째로 반복하지 않습니다.

**모니터링**

3. 디코더의 `0x18` `02` 분기를 제거합니다. 명령 프레임은 상태의 원천이 아니며,
   현재 조건은 우리가 발명한 프레임에만 맞습니다.
4. 난방 직접 응답(길이 0x12)을 받아들입니다. 길이 조건 `>= 38` 대신 주소 바이트로
   전체 응답과 직접 응답을 갈라야 합니다.
5. 조명 `04` 분기를 주소 바이트로 갈라 단일 응답(길이 0x0B)을 해당 조명에 반영합니다.
   `0x10`이면 전체, `0x11`~`0x13`이면 단일입니다.
6. 승강기 상태 해독에 `0xA*`(상승)와 `0xB*`(하강)을 추가합니다. 현재는 `0xA5`만
   받아들이는데 캡처에 `0xA5`는 없고 `0xA6`·`0xB6`가 있습니다. 실제 이동 중에는
   화면이 미확인으로 남습니다.
7. 세대 현관의 `call: true`에 해제 경로를 만듭니다. 지금은 한 번 켜지면 꺼지지 않습니다.
8. 공동 현관의 `0x1E` 조회 프레임이 길이 조건에 걸려 전부 버려지는 것을 고칩니다.
   다만 이 프레임에서 **무엇을 읽어야 하는지는 아직 모릅니다.** 6.2의 실측 전까지는
   버리지 않고 디버그 화면에 원문으로 보여 주는 데까지만 합니다.

**표시 (안전)**

9. 출입구 세 버튼의 이름을 실제 동작대로 바꿉니다. 5.4절에서 확인했듯 세 개 모두
   **문을 여는 매크로**인데 현재 이름은 상태를 흉내 내는 것처럼 읽힙니다.
10. 같은 카드에 `0x7F` 회선이 이 버스에서 한 번도 관측되지 않았다는 사실과,
    그래서 `sevenFProof` 게이트가 전송을 막고 있다는 사실을 적습니다.
11. `heat all off`의 발명 프레임을 버리고, 검증된 zone별 off 프레임 네 개를
    순차 전송으로 바꿉니다.

    > **정정 (2026-08-29).** 이 항목에 딸려 있던 "레거시에도 난방 일괄 명령은
    > 없습니다"라는 문장은 사실이 아니었습니다. `capture-1788009200284`가 월패드
    > 자신이 보내는 단일 전체 끄기 프레임 `F7 0B 01 18 02 46 10 04 00 B5 EE`를
    > 두 번 담고 있습니다. 존별 네 프레임을 순차 전송하는 현재 구현은 개별
    > 프레임이 옳으므로 유효하지만, 반이중 버스에 넷을 밀어 넣는 것이 하나를
    > 보내는 것보다 충돌에 취약합니다. `bestium-eco-foret/src/protocol-debug.ts`
    > 551행의 같은 취지 주석도 함께 틀렸으며, 전선에서 확인한 뒤 제품과 함께
    > 고칩니다. 근거는 `.agent/analysis-ew11-timing-and-group-frames.md` §4.2입니다.

### 6.2 실측이 필요한 것

| 항목 | 필요한 실험 |
| --- | --- |
| 승강기 호출 | 캡처를 켠 채 **월패드의 승강기 호출 버튼**을 누릅니다. 상행·하행 각각 |
| 출입구 회선 | **가장 중요합니다.** 실제 초인종을 누른 뒤 월패드에서 문을 열면서 캡처합니다. `7F` 프레임이 잡히면 같은 회선이므로 문열기를 구현할 수 있고, 안 잡히면 별도 EW11이 필요합니다 |
| 공동 현관 호출 | 공동현관에서 호출한 상태로 캡처해 `0x1E` 조회 프레임의 어느 바이트가 변하는지 봅니다. 지금은 181개가 전부 같은 값이라 필드를 알 수 없습니다 |
| 세대 호출 해제 | 호출이 끝날 때 무엇이 오는지 봐야 `call` 표시를 끌 수 있습니다. 지금은 끄는 근거가 없어 시간 경과로만 끕니다 |
| 콘센트·환기 제어 | 레거시에 명세는 있으나 이 캡처에 없습니다. 월패드에서 조작해 캡처합니다 |

이 세 가지는 실측 전에는 구현하지 않는 편이 낫습니다. 승강기는 레거시 자체가
두 변형을 두고 갈팡질팡하므로 특히 그렇습니다.

### 6.3 하지 않을 것

`0x7F` 출입구 매크로는 이번에 손대지 않습니다. 근거가 늘지 않았고, 캡처에 계열
자체가 없으므로 지금 바꾸면 또 한 번 추측을 쌓는 일이 됩니다.

## 7. 이 문서의 한계

- 승강기 호출과 출입구 제어는 **검증되지 않았습니다.** 레거시 주장과 우리 추측일 뿐입니다.
- 캡처는 306.8초 한 구간입니다. 이 시간에 일어나지 않은 일은 아무것도 증명하지 않습니다.
- 조명 프레임의 근거는 캡처가 아니라 레거시 일치와 사용자의 물리 실증입니다.
- 3절의 분석은 이 문서를 쓴 시점의 `bestium-eco-foret/src/protocol-debug.ts`를
  기준으로 합니다. 3.0절과 3.2절은 실제 디코더·인코더를 실행해 재현했고,
  3절 본문의 프레임 불일치는 생성한 프레임과 캡처를 직접 대조한 결과입니다.
